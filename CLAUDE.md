# fedora-dev — agent rules for editing this repo

## BEFORE ANY CHANGE

Read README.md for human-facing context (what fedora-dev is, what it
provides, how operators use it, the design summary). THIS file carries the
binding agent-facing tables (BUILD PRINCIPLES, BASE PACKAGES, BOX PACKAGES,
REPO FILE PURPOSES) and the in-repo procedures.

`policy/CLAUDE.md` + `policy/managed-settings.json` are the law stamped into
the in-container claudebox at runtime — editing them in THIS repo is the ONLY
way they change.

## TWO LAYERS, TWO CADENCES, TWO SOURCES OF TRUTH

- **fedora-dev base image** — `Containerfile` + `install.sh` + `entrypoint.sh`
  + `bin/` wrappers + the baked seed at `/usr/local/share/fedora-dev/`.
  Rebuilt **monthly** on the 15th by CI (`--no-cache`). Changes flow: edit →
  PR → merge → CI build → GHCR `:latest` (UNSIGNED) → host-side refresh
  recreates the running container.

- **claudebox (in-container Distrobox)** — `distrobox.ini` +
  `claudebox-init.sh` + `box-rebuild.sh` + `claudebox-daily.sh` +
  `claudebox-assemble.sh` + `policy/`. The runtime source of truth is the
  LIVE git clone at `/home/core/.local/share/fedora-dev/` inside the running
  fedora-dev container, NOT the baked seed. Rebuilt **daily** in-container
  plus on-demand. Changes flow: edit (in the live clone, inside the agent's
  session) → PR → merge → next rebuild applies.

The live spec on the home volume persists across BOTH box rebuilds AND
fedora-dev container recreations. That's the design that lets mid-cycle
edits survive monthly base recreates without losing work.

## PROPOSE-AND-COMMIT (binding for the in-box agent)

Durable changes to fedora-dev itself flow through git:

1. Edit the LIVE clone at `~/.local/share/fedora-dev/` (inside the agent's
   session, on the home volume — persists across rebuilds).
2. `gh pr create` from the clone.
3. `fedora-dev` merges on Arthur's clickable APPROVE (or Arthur merges) — see THE FLEET in policy/CLAUDE.md.
4. Next claudebox rebuild applies the merged change. CI rebuilds the base
   image with the new baked seed on its own monthly cadence.

**Host live-gate — label a PR `live-validate`.** To have the host build + live-test a candidate
BEFORE it reaches Arthur, label the PR `live-validate` — this works for **any repo in the org**, not
just fedora-dev. The host (fedora-bootstrap) DISCOVERS labelled PRs ORG-WIDE by that label (no repo
list to maintain), fetches the head on-demand, applies a **structural guard** (builds only a
candidate carrying a `Containerfile`/`.live-gate`, else skips cleanly), builds it DISPOSABLY per the
repo's own in-repo **`.live-gate` contract** (PARSED as a declarative contract, never executed as a
script; absent → host default fence/probe), runs Gate B (health + access-probe) under loopback-only
fences, and posts a **GREEN/RED verdict comment** on the PR automatically — while you keep working
(it runs a throwaway container and never touches your session, so an active dev session never blocks
it). Iterate on RED (push a fix → the new commit re-gates; or SUPERSEDE the branch if the approach
was wrong; on GREEN, build upon it); present only a GREEN PR for Arthur's APPROVE. The host
**comments, NEVER merges**. Enrolment is fully dynamic: create/rename/merge/delete repos freely —
enroll one just by labelling its PR `live-validate` and shipping a `.live-gate`.

Ad-hoc `dnf install` or `dnf remove` inside the running box works for the
current session but VANISHES on the next rebuild — by design. That's the
discipline that keeps the box reproducible.

## TICKETS & THE DEV LOOP (my role)

I am **DEVELOP · BUILD · MERGE** and the fleet's **SOLE merge authority**. The **PR is the ticket** — there is no separate tracker; an open PR against `main` is both the work item and the handoff token. My place in the loop (the full fleet map is `FLEET.md`):

1. **Develop → open PR.** Edit the LIVE clone, `gh pr create` (see PROPOSE-AND-COMMIT). The PR is the ticket. All pushes and PR operations run autonomously (UNSHACKLED 2026-07-11 — no gate hook, no classifier); `main` itself is unreachable except through a PR (require-PR ruleset) and `gh pr merge` is a managed-settings deny.
2. **Request a host verdict — label `live-validate`.** This enrolls the PR in the host pre-merge live-gate — for **any repo in the org**, not just fedora-dev (the host discovers labelled PRs ORG-WIDE by the label, no repo list to maintain). `fedora-bootstrap` runs `live-gate-watch.sh` (its `live-gate-watch.timer`, 10 s poll), which for each new head SHA (deduped once via `~/.local/state/live-gate/<WL>-<sha>.done`, and ONLY once the verdict comment is actually delivered) applies a **structural guard** (builds only a candidate carrying a `Containerfile`/`.live-gate`, else skips cleanly), builds a **disposable** candidate (`build-candidate.sh` → `localhost/disposable/<name>:val-<sha>-<target>`, never pushed) per the repo's own in-repo **`.live-gate` contract** (PARSED, never executed; absent → host default fence/probe) and runs **Gate B** (`validate-candidate.sh`: health + access-probe) under loopback-only fences, then posts a `Host live-gate (Gate B): VERDICT GREEN|RED` comment on the PR. My own `build.yml` runs build-only on the PR (`push=false`) — that proves it *builds*; the host verdict proves it *runs*.
3. **Iterate on RED.** Push a fix commit (or SUPERSEDE the branch if the approach was wrong); the new head SHA has no `.done` marker, so the host re-gates exactly once and re-comments. On GREEN, build upon it. Loop until GREEN — the human is OUT of this per-iteration loop.
4. **A GREEN + fitness-PASS PR auto-merges.** (UNSHACKLED / ZERO-GATE, 2026-07-11.) The headless **poller** (`bin/pr-poller.sh` → `bin/auto-merge.sh`, plain shell) merges every host-GREEN + independent-fitness-PASS PR — any tier, any author, control-plane included — with no human click. A fitness ESCALATE surfaces to Arthur instead of merging (the reviewer deferring a judgment call, e.g. a change to its own oversight).
5. **The interactive agent never merges.** `gh pr merge` is a hard managed-settings **deny** for the in-session agent, and `main` accepts nothing outside a PR (require-PR ruleset) — so the sanctioned path is always: push a branch → label `live-validate` → the poller's two independent gates (host live-gate App + fitness App, distinct identities, full-sha-bound) decide the merge. Arthur may still merge on GitHub himself.
6. **Hands off after merge.** Push to `main` triggers CI build + GHCR publish (UNSIGNED); `fedora-bootstrap` pulls + redeploys via `workload-refresh@<name>`. I never operate, deploy, or `podman build` a shipping image — those are STOP-AND-SURFACE to `fedora-bootstrap` / CI.

**Paused-work / cross-box requests** ride a **GitHub Issue** in the target repo (a convention, not automation): when work parks mid-flight or another box surfaces a fix for a repo I own, the Issue carries the request + proposed diff; I turn it into a branch + PR, re-entering at step 1.

## HEADLESS (binding prerequisite)

fedora-dev runs **fully headless** — no physical monitor, GPU, or local login seat is ever
attached or required. The harness (the claudebox + the supervised services) needs no display, and
every desktop image built ON this base (the fedora-desktop **xrdp** and **grd** lineages) is
likewise headless — a *virtual* display rendered by software GL (llvmpipe), reached only over the
network (ssh / RDP / VNC / web). A change that makes any part depend on a real display, GPU, or
physical seat is a **defect**, not an option.


## DOC ARCHITECTURE — DRY rule (binding)

One authoritative home per concept; every other mention is a one-line pointer or deleted. Evidence and benchmarks live only in the principle they prove. Fleet-wide identical blocks are enforced by CI (`bin/fleet-guard-parity.sh`); intra-repo concept duplication across the Trinity + this file + `policy/CLAUDE.md` + `FLEET.md` is enforced by REVIEW, not by CI (the `bin/doc-dry-audit.sh` drift-audit was deleted in #274 STEP 5 — it matched verbatim clauses only; see `00-BUILDPRINCIPLE.md` BP9).

**Layer roles.** `policy/CLAUDE.md` = binding law (stamped at every box rebuild; always in context) — owns autonomy mandate, two-tier validation, DoD, merge gate, and control-plane class. `CLAUDE.md` (this file) = per-repo build rules (BUILD PRINCIPLES table) + per-file purpose map. `FLEET.md` = cross-box map (human + agent-accessible); shared sections fleet-wide. `README.md` = human-only; not an authoritative source for any agent rule.

## BUILD PRINCIPLES (binding for every code change)

**Principle 0 — the self-sustaining apparatus.** Autonomy mandate, two-tier validation, and Definition of Done live in `policy/CLAUDE.md` (THE SELF-SUSTAINING APPARATUS section); that law is always in context. BUILD PRINCIPLES 1–11 below are this repo's per-build rules.


| # | Principle | Rule |
|---|---|---|
| 1 | BASE | Build only from the official `registry.fedoraproject.org/fedora:${FEDORA_VERSION}` image. Version is a Containerfile `ARG` — never inlined. |
| 2 | SOURCES | Every package/artifact from an official source, exactly one of: (a) Fedora's own repos via dnf; (b) the vendor's/developer's own RPM or dnf repo (`.repo` with `gpgcheck=1`); (c) an **official-upstream binary release artifact with NO class-(a)/(b) source** — bounded by the **Class-(c) rules** below (last-resort/zero-base; take the vendor's directly-published raw binary — never apt/`.deb`; provenance-GRADED c1 GPG-sig / c2 checksum / c3 resolve-log, strongest-available, fail-closed, pinned; shape is a within-grade tiebreaker — a persistent runtime binary IS permitted; disclosed per-artifact). Never: COPR or other third-party repos, pip/npm/cargo/gem/brew installs, curl-pipe-sh, mirror/aggregator binaries, flatpak, snap. **Applies to BOTH the base image AND claudebox's `additional_packages`.** Anything outside (a)/(b)/(c)-as-scoped needs an explicit user waiver row. **Class-(c) artifacts in use: none.** |
| 3 | MINIMAL | dnf only with `--setopt=install_weak_deps=False`. Every package gets a justifying row in the relevant Packages table (BASE or BOX); a package without a row is a violation. **Install the most specific (leaf) package, never a convenience metapackage, unless a recorded architectural reason. `install_weak_deps=False` blocks weak Recommends but NOT a metapackage's hard Requires — so a metapackage can silently pull unused components (e.g. the `fail2ban` metapackage hard-pulls `fail2ban-firewalld`→`firewalld` + `fail2ban-sendmail`→`esmtp`; install `fail2ban-server`). If unsure whether a name is a metapackage, verify (`dnf repoquery --requires <pkg>`) and flag before adding.** **"MINIMUM" IS RELATIVE TO THE CHOSEN CAPABILITY, not the absolute package count.** Once a capability is decided (e.g. a working desktop; an RDP-grade web gate), install the minimal LEAF footprint that makes THAT capability work, and accept + DISCLOSE the irreducible hard-dependency closure it entails (e.g. a `gnome-shell` desktop→webkit + `gnome-control-center`; a KDE desktop→samba/codec). Between options that deliver the SAME capability, prefer the smaller-footprint / built-in / class-(a) one. A lighter option that REDUCES the capability is NOT "more minimal" — it is a lesser function, and choosing it is a recorded capability trade-off, NOT a minimalism win. (Worked decision in fedora-desktop: Guacamole [RDP-grade web gate — H.264/audio/clipboard/file-transfer in the browser, strong password + brute-force lockout] is the SOLE web gate; noVNC [VNC-grade] was removed fleet-wide — a public, non-tailnet door needs strong auth and noVNC's 8-char VncAuth is unacceptable there — so Guacamole's Tomcat + JVM + `.war` footprint IS the minimum for full strongly-authed RDP-in-the-browser.) |
| 4 | VERIFY FIRST | Before adopting or bumping any source/version, fact-check it against the live source (web). Gate risky installs (version-mismatched vendor RPMs, new repos) in a scratch container before editing build files. |
| 5 | NO SECRETS / NO IDENTITY | No passwords, keys, or personal usernames in any layer, file, or commit. Container user is the generic `core` (uid 1000). Credentials enter only as runtime env vars; entrypoint fails fast when missing. |
| 6 | PINS | Vendor artifact versions are Containerfile `ARG`s or pinned in `distrobox.ini` — bump there only, after rule 4. |
| 7 | DEPLOY CONTRACT | Every image ships a `run.sh` — the only sanctioned **non-interactive** way to run it: runtime `--health-cmd` (OCI drops Containerfile HEALTHCHECK), devices, volumes, restart policy, port set. An optional **`spin-up.sh`** wizard wraps it to ASK for `TS_AUTHKEY`/`IMAGE` interactively (blank key = `login.tailscale.com` web-login), but only ever delegates to `run.sh` — run.sh stays the single source of runtime truth. Sensitive ports (ssh/RDP/VNC) stay tailnet-only — never `-p`. The Quadlet is the systemd-managed equivalent. Never hand-roll `podman run`. |
| 8 | CI + LAYERED CADENCE | `.github/workflows/build.yml` publishes the base image to GHCR **unsigned** (image signing was dropped fleet-wide as unenforced theatre — no host cosign-verifies): on push, on the 15th monthly (`--no-cache`), and on manual dispatch. Built-in token only. The IN-CONTAINER claudebox refreshes daily on its own timer + ad-hoc triggers; it never touches CI. |
| 9 | VALIDATE | After any change: build, deploy via `run.sh`, confirm `(healthy)` plus a functional probe of each access path. Final proof is CI green + a host deploy from claudebox-on-the-host. **Prove runtime/terminal behaviour empirically, not by reasoning:** for tmux multi-client geometry, TUI redraw, and the like, drive multiple sized PTY clients with a real harness and assert the actual bytes each client renders (a naive byte VT model mis-reads UTF-8 fills like `·` as garbage) — not just a reported window size. **Validate at the REAL execution boundary, not the component in isolation:** a change spanning the base image ↔ in-box claudebox (credentials, PATH tools, env, sockets the in-box agent consumes) MUST be exercised end-to-end FROM INSIDE the box (an actual in-box `git push` / `git credential fill`), never just the component's `bash -n` + unit logic — reasoning about cross-context correctness is not validation. (2026-06-28: a credential helper wired to base-only `/usr/local/bin` + `/run/secrets` passed isolated Tier-1 but never authenticated in-box `git push`; ultra-verify caught it, component tests did not.) |
| 10 | THROWAWAY TREE & CHURN | Use the LIVE tree where possible; for anything that must DIFFER from it, bolt on a SEPARATE, TEMPORARY throwaway tree that (a) **NEVER mutates the IMMUTABLE live tree** (host + dev-container base are immutable — the throwaway tree + all build caches live on the WRITABLE home volume), (b) **STILL obeys PROVENANCE** (Principle 2 — class a/b/c, GPG/signature/checksum verified; NO loosening because it's a throwaway), (c) is **THROWN AWAY after the build** (disposable `localhost/disposable/<name>:val-<sha>` tag, never pushed, `--rm` + `rmi`; temp tree removed on teardown). **CHURN BALANCE:** persist the durable INPUTS — the dnf PACKAGE CACHE and the PINNED-DOWNLOAD CACHE (plain BIND dirs on the home volume, **NOT image layers**, so they survive `rmi` and EVERY disposal; the download cache and its fetch contract are Principle 2's *Pinned fetches* section, which owns them) — and let everything else (candidate image, its layers, temp tree, run container) be EPHEMERAL by design; the throwaway image is the OUTPUT, those two caches are the PERSISTENT INPUTS. Structure Containerfiles **HEAVY/STABLE-EARLY** (base, dnf install, class-(c) fetch+verify) and **CHURN-LATE** (COPY'd scripts/config); **NEVER `--no-cache`/prune during churn** (reserved for the monthly clean `--no-cache` rebuild). **CHURN — NO re-download across N PRs/iterations (proven in-box):** the per-PR/per-SHA disposal removes the disposable image + temp tree — and, when it was the sole referrer, its intermediate layers too — **but NEVER either persistent cache** (neither is keyed to PR/SHA; both are **SHARED across all iterations**). **TWO persistent things, everything else ephemeral by design:** (1) the persistent dnf **PACKAGE CACHE — the ROBUST mechanism** (bind-mounted `-v <home>/.cache/fd-dnf:/var/cache/libdnf5:rw`; a plain dir, NOT an image layer, surviving `rmi` and every disposal): churn that changes the dnf install LINE (an add-on PR) re-runs the layer but **serves RPMs FROM CACHE, not re-downloaded** — PROVEN a forced dnf re-run downloaded **0 B (vs 9.4 MiB cold), 3.7× faster**; only a genuinely-new package downloads once. (`--mount=type=cache` does NOT work under the required `--isolation=chroot`, verified — the **bind-`-v` package cache is the mechanism**.) (1b) the persistent **PINNED-DOWNLOAD CACHE** (`-v <home>/.cache/fd-dl:/var/cache/fd-dl:rw`), the same mechanism for the other class of durable input — a vendor artifact fetched by URL. It exists because the LAYER cache does not cover one: buildah reports a cache HIT on `ADD <url>` and **still transfers the whole file** (MEASURED: 10 378 KiB pulled under `--> Using cache`; **0 asset bytes** through the contract) — so `ADD <url>` is banned for a pinned asset and Principle 2's *Pinned fetches* section is the authority on the contract, its fail-closed verification and its caps. (2) **EPHEMERAL LAYERS — ephemeral BY DESIGN, an ADVANTAGE:** a throwaway's layers are pruned with its sole candidate's `rmi`, so (a) layer storage **self-bounds** (no accumulation / no layer-cache to GC), (b) each throwaway **rebuilds FRESH** from the package cache → CURRENT package versions, no stale-frozen-layer risk (freshness for free), (c) the only cost is a few local **CPU-seconds (~3.6 s warm), never bandwidth**. While a candidate image lives (LATE-layer churn, or a kept image), its layer cache also lets the rebuild skip the dnf RUN entirely → ZERO work — a free accelerator — but nothing relies on layers surviving disposal. **ISOLATION:** each build has its OWN throwaway tree + unique disposable tag (`val-<sha>`) + unique run container (`vcand-$$`) → no cross-build contamination; the dnf package cache (and any live layer cache) is content-addressed so it cannot serve a wrong version. **STORAGE SAFETY (limited VPS):** (a) the disposable image+tree self-destruct via `trap … EXIT` (fires on GREEN/RED/error); (b) an ORPHAN SWEEPER reaps anything a `kill -9`/crash leaks (stale `localhost/disposable/*`, `vcand-*`, orphan temp dirs) at watcher start + periodically; (c) a BOUNDED cache-GC caps EACH persistent cache age-then-size (dnf: RPMs >45 days pruned first, then LRU size-prune to ≤15 GB; downloads: entries unused >45 days, then LRU-evict to ≤10 GB, plus stale partial downloads reaped — both sets of caps overridable env, both enforced from the same sweeper) so neither can exhaust the quota — layers self-bound via `rmi`, dangling ones swept opportunistically. |
| 11 | PROPOSE-AND-COMMIT | The in-box agent grows `distrobox.ini`/`policy/`/`box-rebuild.sh`/etc. only by editing the LIVE clone at `/home/core/.local/share/fedora-dev/` and opening a PR. `fedora-dev` merges on Arthur's clickable APPROVE; the next box rebuild applies. Ad-hoc installs vanish on rebuild. |

### Class-(c) sources — the bounded last-resort exception (fleet-wide; identical in fedora-desktop + fedora-dev + fedora-bootstrap)

**(c)** ONLY when **no class-(a) Fedora package and no class-(b) vendor `.repo`** exists for the
needed artifact — a **last-resort, zero-base check, re-confirmed at every version bump**; the
moment it appears in Fedora or a vendor `.repo` it MUST move to (a)/(b): an **official-upstream
binary release artifact**, fetched over TLS from the project's **own canonical release channel**
— whose exact host + org/repo (or release-API URL) is **pinned in the disclosure row and
changeable only as a control-plane change** — never a mirror, aggregator, COPR, PPA, OBS home
project, language-package-manager registry (Maven Central/npm/PyPI/crates.io/RubyGems), or
third-party rebuild. Each artifact MUST be **(1) version-pinned** via a Containerfile `ARG` (or
`distrobox.ini`/setup pin), the SOLE exception being an artifact Principle 6 designates
latest-at-build; and **(2) integrity-verified FAIL-CLOSED before any use, GRADED by what is
verifiable on the RAW BINARY via the direct-download path** (not by a signed repo we do not
consume):
- **c1** — a detached **GPG signature on the binary** (`<artifact>.asc`/`.sig`), verified against
  the vendor's key fingerprint pinned in-repo (`gpg --verify`). Strongest.
- **c2** — a vendor-published **checksum** (`sha256/sha512`) with **no binary signature
  available**; `sha*sum -c`, acceptable ONLY because no signature is offered.
- **c3** — a **latest-at-build** artifact with no pre-pinnable hash: TLS-authenticated fetch from
  the vendor's own release API + **resolve-and-log** (auditable record, NOT a fail-closed gate);
  reserved to artifacts that GENUINELY cannot be pinned, each **individually named AND
  control-plane-approved** — never a general unpinned escape hatch.

Take the **strongest grade the direct channel offers** — you may NOT use c2 when a c1 signature
exists, nor c3 when a hash can be pinned. The build **fails closed** on any mismatch / missing /
unfetchable check.

**Consume the BINARY, never a foreign-distro package manager.** Where a vendor ships a signed
**apt** (or other non-dnf) repo but no dnf repo, do NOT add or invoke that package manager on
Fedora, and do NOT unpack its `.deb`/`.pkg` to reach the binary — take the vendor's
**directly-published raw binary** from the same canonical channel and verify it per the grades
above. A signed foreign-distro repo raises confidence the vendor's key is genuine but does NOT set
the grade; the grade is what we can verify on the binary we install.

**Shape is a within-grade TIEBREAKER, not a gate.** A **persistent runtime-layer binary IS
permitted** (the former absolute "$PATH binary NEVER permitted" ban is removed — the rule gates
PROVENANCE, not shape). When two artifacts share a grade, prefer strongest→weakest: (i) a
**build-time-only tool** (runs once, installs nothing on `$PATH`, deleted) → (ii) a webapp/archive
**deployed into a class-(a) runtime** (an Apache `.war` into Fedora's Tomcat) → (iii) a
self-contained **AppImage from `/opt`** → (iv) a **persistent runtime-layer binary** (e.g. an OCI
runtime such as `runsc`) registered with the engine. Each (c) artifact gets a **disclosure row**
in the Packages table (pinned canonical URL + version + **grade c1/c2/c3** + shape); the table's
**enumeration line lists every (c) artifact in use**.

**ANTI-THEATER (doctrine — do not rebuild the sieve).** A static SCRIPT-SCAN for "bad" fetch/install
patterns is **NOT** a valid 2(c) backstop — it is the exact pattern the fleet already de-theatered
(v1.2.48 dropped the inert `--cap-add` denylist; `managed-settings.json`'s deny-list "is best-effort
only… not the boundary"). Detecting bad patterns in arbitrary shell is a sieve — a seventh evasion
always exists — and a guard that implies coverage it can't deliver is **worse than none**. For a
fetched binary the boundary is the **installer's OWN fail-closed verification** (sha/GPG; exit non-zero
+ install nothing on any mismatch/missing) **+ the disclosure row + the click** — never a scan.
(A host static fetch-guard was built and closed for exactly this reason.)

**Class-(c) artifacts in use: none.** This repo ships no upstream binary artifact today; the rule
is carried for fleet parity so any future need inherits the identical bounded definition. (The
only repo with class-(c) artifacts is fedora-desktop: `guacamole.war` + Obsidian.)

### Pinned fetches — the download-cache contract (never `ADD <url>`)

**THE RULE: a PINNED asset is fetched with `bin/fd-fetch.sh <https-url> <sha256> <dest>` — never with
`ADD <url>`, never with a bare `curl`.** This governs HOW a pinned artifact arrives; WHICH sources are
allowed is unchanged (the class a/b/c rules above).

**MEASURED, in the dev box's own nested engine, 2026-07-30** — this is a rule and not a preference
because the numbers say so, and because no "a cache exists" assertion could have found it:

| build | podman says | bytes actually pulled (eth0 rx delta) |
|---|---|---|
| `ADD <url>` #1 | `STEP 2/2: ADD …` | 10 378 KiB |
| `ADD <url>` #2 (image kept, layer cache live) | `--> Using cache e17eb17e7b87` | **10 378 KiB** |
| `RUN fd-fetch` #1 (cold download cache) | `fd-fetch: MISS … fetching` | 10 414 KiB |
| `RUN fd-fetch` #2 (fetch layer deliberately re-run) | `fd-fetch: HIT … 0 bytes fetched` | **12 KiB** |
| `RUN fd-fetch` #3 (fetch layer deliberately re-run) | `fd-fetch: HIT … 0 bytes fetched` | **4 KiB** |

Noise floor on this box: **425 KiB per 25 s idle** (tailnet + poller chatter), so the 4–12 KiB rows are
background traffic, not the asset. The asset was FreeRDP 3.14.0's 10.18 MiB source tarball — a real
pinned vendor asset in the fleet (`knowledge-desktop/lineage-*/Containerfile`).

**Buildah reports a layer-cache HIT on `ADD <url>` and still transfers the whole file**: it downloads the
URL in order to compute the cache key, then throws the bytes away. So the layer cache buys nothing for a
remote asset, and Principle 10's "a re-build re-downloads NOTHING" held for RPMs only. Rows #2/#3 above
deliberately INVALIDATE the fetch layer (a `--build-arg` churn), because a rebuild that skips the RUN
step proves only that the layer cache works — the cache under test is the one that must serve when the
layer cache CANNOT.

**THE CACHE.** Content-addressed by the pin's sha256, `$HOME/.cache/fd-dl` on the writable home volume,
bound read-write at the FIXED `/var/cache/fd-dl` by BOTH Tier-1 builds (`bin/build-throwaway.sh`,
`bin/validate.sh`). That bind is **parity-critical** (`bin/check-build-parity.sh`) exactly like the dnf
one — a Tier-1 build missing a bind the Tier-2 host build has is the #53 false-GREEN shape.

**PROVENANCE IS STRENGTHENED, NOT LOOSENED.** The pin is verified fail-closed on EVERY path: a MISS
verifies before publishing (a mismatch publishes nothing and leaves `<dest>` absent), and a HIT re-hashes
the cached bytes before serving them — a cache is not a trust boundary. It is therefore strictly stronger
than the `ADD <url>` it replaces (which verifies nothing) or a bare `curl -fsSL` (which verifies the
channel, never the artifact). Pins live where Principle 6 says: a Containerfile `ARG`, bumped there only.

**OPT-IN THROUGH THE CONTRACT, NEVER BY REWRITING A TREE.** The cache reaches a build only because that
repo's own Containerfile calls `fd-fetch.sh`. No build helper edits a Containerfile or injects a
`--build-arg` — a validation build that silently differs from the build CI runs is not a validation. A
repo that keeps `ADD <url>` simply keeps paying the download.

**BOUNDS** (BP10 storage safety, since this is a persistent input): `fd-fetch.sh --gc` age-prunes
entries unused >`FD_DL_CACHE_MAX_AGE_DAYS` (45), then LRU-evicts to `FD_DL_CACHE_CAP_GB` (10), then
reaps stale partial downloads — enforced from `build-throwaway.sh`'s existing sweeper, so every
throwaway build applies it. **In use today:** the Tailscale `.repo` in `install.sh` (the apparatus's own
proof-of-use). Tenant Containerfiles (`knowledge-desktop`, `fedora-desktop`) still use `ADD <url>`;
migrating them is separate work in those repos.

## BASE PACKAGES

The fedora-dev image itself. Refreshed monthly via CI.

| Package | Pin | Source (rule 2 class) | Why required |
|---|---|---|---|
| podman | Fedora current | distro (a) | the container ENGINE — claudebox runs on it; the agent's `podman build` lands here via CONTAINER_HOST |
| shadow-utils | Fedora current | distro (a) | newuidmap/newgidmap setuid helpers — mandatory for nested rootless podman |
| fuse-overlayfs | Fedora current | distro (a) | nested rootless storage driver (kernel forbids native overlay-on-overlay) |
| passt | Fedora current | distro (a) | pasta — podman 5 default rootless network backend |
| nftables | Fedora current | distro (a) | the firewall backend. tailscaled programs its rules via the nftables **Netlink API** (no binary needed; it falls back to nftables and never crashes if iptables is absent — verified on the bootstrap host, which runs tailscaled with zero iptables); netavark's default firewall driver (nftables since Fedora 41). (`iptables-nft` dropped — the prior "tailscaled crashes without the binaries" rationale was false.) |
| openssh-server | Fedora current | distro (a) | the login door (key-only since v1.1.9; ALL keys from `github.com/<user>.keys` synced by entrypoint at every start — the GitHub account is the single trust root, no in-image allowlist; key trust is managed centrally on the account). Two paths into :22 — Tailscale SSH on tailnet (keyless) AND public ssh on host :4444 → container :22. mosh bootstraps over either ssh path. |
| mosh | Fedora current | distro (a) | roaming-resilient remote shell (UDP, AEAD-authenticated; bootstraps over ssh). v1.1.9: public UDP range 61001-62000 (non-default, to avoid colliding with the bootstrap host's own public mosh which uses 60000-61000 on the same kernel UDP namespace); clients invoke with `mosh -p 61001:62000 --ssh="ssh -p 4444"` for the public path. |
| tailscale | Tailscale dnf/rpm repo | vendor (b) | tailnet node + keyless Tailscale SSH on the tailnet IP (primary access path). Co-exists with public ssh :4444 + public mosh 61001-62000 added in v1.1.9. |
| tmux | Fedora current | distro (a) | session multiplexer; every interactive login gets its OWN session in the shared `main` group (shared windows/work, independent per-client current-window selection), with a `/etc/tmux.conf` tuned for multi-device co-view: `default-terminal tmux-256color`, `window-size latest` (the session follows whichever device most recently sent input — seamless macOS↔iPad handoff over mosh; **`prefix+g` cycles latest→smallest→largest**), `fill-character ' '` (idle larger device blank-letterboxes instead of `·`-garble), `aggressive-resize on`, `client-attached`/`-resized`→`refresh-client`. NB tmux gives a window ONE size, so differently-sized devices on the SAME tab can't each be full-size — `latest` makes the active device win; the rest degrade cleanly (crop/letterbox, never garbled). Trigger-and-detach is the operator pattern. |
| distrobox | Fedora current | distro (a) | declaratively bootstraps the in-container claudebox via `distrobox assemble create --file distrobox.ini` |
| inotify-tools | Fedora current | distro (a) | `inotifywait` in the entrypoint watches `rebuild.request` flag from the in-box agent (replaces systemd `.path` unit; no systemd here by design) |
| sudo | Fedora current | distro (a) | break-glass escalation for the operator (`core` in `wheel`). v1.1.9: no password set on `core` (chpasswd dropped), so sudo is effectively unreachable for the human; break-glass is `podman exec -u 0 fedora-dev bash` from the VPS host. Kept for completeness; near-zero footprint. |
| procps-ng | Fedora current | distro (a) | `pgrep` for the entrypoint watchdog AND for `run.sh`'s `--health-cmd` |
| glibc-langpack-en | Fedora current | distro (a) | UTF-8 rendering for tmux and the terminal observing Claude's output |
| openssl | Fedora current | distro (a) | RS256-signs the GitHub App JWT in `bin/gh-app-auth.sh` — the standing-credential minter runs in `entrypoint.sh` at the BASE layer, where `gh` is absent. The `openssl` CLI is not otherwise pulled into the base (only `openssl-libs` is). ~1MB |
| git-core | Fedora current | distro (a) | base-layer git for `entrypoint.sh`'s first-boot live-spec clone + the `seeded-no-git` self-heal (clone / ls-remote / config run at the BASE layer, before/without the claudebox) AND the standing-credential's `credential.helper store` wiring. The LEAF package (the `git` metapackage pulls perl-Git + git-{email,svn,…}); `git-core` provides the `git` binary + clone/config/ls-remote. The agent's own richer `git` is the claudebox copy. |
| nano | Fedora current | distro (a) | one break-glass editor (Fedora 44's minimal base doesn't reliably ship `vi`). ~600KB |

## BOX PACKAGES

Inside claudebox (`distrobox.ini`'s `additional_packages`). Refreshed daily from Anthropic's `latest` channel + Fedora repos.

| Package | Pin | Source (rule 2 class) | Why required |
|---|---|---|---|
| claude-code | Anthropic dnf/rpm repo (`latest` channel) | vendor (b) | the agent — claudebox's purpose; refreshed daily so new model releases are accessible day one. In-place self-update is LOCKED OFF (`DISABLE_UPDATES`/`DISABLE_AUTOUPDATER` — set in `/etc/profile.d` by `claudebox-init.sh` + policy-tier in `managed-settings.json`) so a self-managing "native build" can't plant `~/.local/bin/claude` and shadow the RPM on the home volume; updates flow ONLY via the box rebuild's `dnf install` (the documented "Pick up newer claude-code: run `claudebox-rebuild`" path). `claudebox-init.sh` also self-heals any pre-existing native shadow off the home volume |
| git | Fedora current | distro (a) | VCS engine the agent drives. Inside the box because the agent's shell is in the box |
| gh | GitHub dnf/rpm repo | vendor (b) | GitHub/GHCR auth, PRs (propose-and-commit lifecycle), releases. Auth state at `~/.config/gh/` lives on the home volume — shared across box rebuilds |
| openssh-clients | Fedora current | distro (a) | outbound ssh for git-over-ssh from inside the box |
| podman (client) | Fedora current | distro (a) | the agent's `podman build/run/exec/healthcheck`. Engine is at fedora-dev's level; CLI in the box drives it via `CONTAINER_HOST` |
| bubblewrap | Fedora current | distro (a) | Linux user-namespace sandbox; Claude Code's Bash tool uses it for isolated execution |
| socat | Fedora current | distro (a) | paired with bubblewrap for IPC socket relay into/out of the sandbox |
| host-spawn | Fedora current | distro (a) | distrobox container-side host-exec component. Headless = no flatpak-session-helper, so the actual shims are deliberately NOT wired up; presence prevents distrobox-create from `curl`'ing it from GitHub releases (rule 2 source-control) |
| rclone | Fedora current | distro (a) | the agent's cloud/SMB/SSH/object-store reach for build files on other network drives |

## REPO FILE PURPOSES

| File | Purpose |
|---|---|
| README.md | human-facing project doc (purpose, deploy, operate, design appendix) |
| CLAUDE.md | this file — agent rules for editing this repo |
| Containerfile | base image build spec (FROM fedora:ARG; runs install.sh; COPY's entrypoint + scripts + bin/). Also carries `ARG TAILSCALE_REPO_SHA256` — the Principle-6 home of that pinned asset's sha256, bumped here only — and COPY's `bin/fd-fetch.sh` beside install.sh so the pinned fetch contract is available at install time |
| install.sh | base image install (Fedora repo + Tailscale repo dnf installs; nested-podman config; sshd config; defensive setcap on newuidmap/newgidmap). The Tailscale `.repo` is fetched through the PINNED CONTRACT (`bin/fd-fetch.sh`, #320 — the apparatus's own proof-of-use), not a bare `curl`: served from the build's download cache when one is bound, fetched once when not (the CI base build), and sha256-verified on EVERY path against `ARG TAILSCALE_REPO_SHA256` in the Containerfile (Principle 6 — the pin lives there and is bumped there only). Provenance class is unchanged (b — the vendor's own dnf repo, `gpgcheck=1`+`repo_gpgcheck=1` inside the file) and STRENGTHENED: the `curl -fsSL` it replaces verified the TLS channel and nothing about the bytes. DISCLOSED TRADE: the pin is FAIL-CLOSED, so an upstream edit to that file STOPS the build (a changed `baseurl`/`gpgkey` is precisely the change worth failing on) — remedy is Principle 4 then 6, re-check live and bump the ARG |
| entrypoint.sh | PID 1 (root): syncs ALL ssh keys from `github.com/<user>.keys` (key-only sshd; the GitHub account is the trust root) + supervises sshd + tailscaled + rootless podman API socket + inotify rebuild-flag watcher + daily-tick + first-boot live-clone-or-seed + every-boot claudebox assemble via `claudebox-assemble.sh --if-stale` (self-deciding: no-ops fast when already assembled for the CURRENT image; reassembles when never-assembled / a prior assemble FAILED / the container was recreated on a NEW image — an R17 rebuild / monthly refresh / redeploy — see the claudebox-assemble.sh row); **provisions the STANDING GitHub credential** (App token via `gh-app-auth.sh install`, or a static `GH_TOKEN`) + **ferries the FITNESS App token** (mints a <=1h token from `/run/secrets/gh_app_key_fitness` → `~core/.config/fitness/env` 0600; the fitness KEY never enters the box — only the token; independent-reviewer identity for `bin/fitness-review.sh`) + **self-heals a `seeded-no-git` live spec into a real clone** once GitHub is reachable + 40-min refresh ticks for BOTH App tokens; pgrep+kill-0 watchdog; SIGTERM trap for clean shutdown. **Supervises the in-box poller + apparatus-deadman + dev-loop authoring service (ARMED BY DEFAULT — `DEV_LOOP_ENABLED` gate defaults ON so the loop self-arms with the apparatus; set `DEV_LOOP_ENABLED=0` to disable; front-half of R3 — and the POLLER is likewise FULLY SELF-ARMING BY DEFAULT: `POLLER_ENABLED:-1` + `POLLER_ARMED:-1` (G7), self-arming across a rebuild-to-spec that would otherwise drop the out-of-band deploy env (the same self-refresh rationale as the dev-loop) — it reviews AND merges autonomously (the GATE-FREE objective: no human approves the shipment; the #96 explicit Tier-A arm is RETIRED as pre-ZERO-GATE), the merge-TRUST boundary being the two DISTINCT App-identity gates (auto-merge HARD-REFUSES `--commit` under same-identity fitness, so a default-armed poller still cannot merge without the real independent fitness App), with a deliberate DRY-RUN SOAK (`POLLER_ARMED=0`) + the fitness knobs PERSISTED on the home volume + SAFE-PARSE-restored when the env is absent (never `source`d: the file is core-writable and restored as root), so an operator's deliberate override survives every rebuild; covered by `g7-poller-persist.test.sh`) via `runuser`+`distrobox enter` loops that are RESILIENT to the distrobox-enter RESTART RACE:** `distrobox enter` re-runs a `podman logs -f` follow for the `container_setup_done` sentinel (distrobox-enter:702/734/756) ONLY when the box is not already running, and that follow can HANG FOREVER after a container restart if the sentinel is missed — wedging the supervise loop and forcing manual poller recovery (observed 2026-07-14). Each loop now gates the long-running enter behind a `box_ready` bounded readiness probe (`timeout … distrobox enter -- true`): it returns once setup is done (box running ⇒ the real enter takes the instant fast path) or is KILLED if it hangs, so the enter almost always fast-paths instead of the hang-prone wait — shrinking the wedge from DETERMINISTIC-on-every-restart to a rare sub-second residual (a stop in the gap before the real enter's own inspect; inherent, since a long-running service enter cannot itself be timeout-bounded — disclosed in the code). `timeout` runs without `--foreground` so it group-kills a hung probe's backgrounded `podman logs -f` (no orphan). Box down/stuck ⇒ skip + retry. Covered by `supervise-enter-race.test.sh` |
| spin-up.sh | interactive spin-up wizard (the by-hand entry point): ASKS for `TS_AUTHKEY` (blank = web-login fallback) + `IMAGE` + (optional) a **standing GitHub App credential PASTED at the prompt** (App ID/Installation ID + the PEM → a podman secret via `bin/gh-app-provision.sh`), exports them, then `exec`s run.sh. **`COLLECT_ONLY=1`** gathers the answers + creates the secret and EMITS them as `export` lines WITHOUT launching — so the host `day0.sh` can drive this wizard to ask fedora-dev's OWN questions (single source of truth) instead of duplicating them. Mirrors `fedora-desktop/spin-up.sh` |
| run.sh | manual deploy contract (`podman run -d`-style with --health-cmd, devices, volumes); the env-driven path `spin-up.sh` wraps; fallback for non-systemd hosts. Optional STANDING GitHub credential: `GH_APP_ID`+`GH_APP_INSTALLATION_ID`+ one of `GH_APP_SECRET` (a podman secret — the paste path), `GH_APP_KEY_FILE` (bind a PEM), or `GH_TOKEN`; the key lands read-only in tmpfs `/run/secrets/gh_app_key`. Optional FITNESS-REVIEW credential (the fleet-wide independent reviewer): `GH_APP_FITNESS_ID`+`GH_APP_FITNESS_INSTALLATION_ID`+`GH_APP_FITNESS_SECRET` (or `GH_APP_FITNESS_KEY_FILE_HOST`); key → tmpfs `/run/secrets/gh_app_key_fitness`, consumed by the entrypoint's fitness ferry |
| fedora-dev.container | systemd Quadlet for managed deployment (declarative spec: AutoUpdate=registry, Notify=healthy, HealthCmd, Volume=, SecurityLabelDisable=true; **NO EnvironmentFile** — key-only sshd since v1.1.9, optional TS_AUTHKEY via podman `Secret=`; optional standing GitHub App credential via `Secret=gh_app_key` + `Environment=GH_APP_ID=…`; optional fitness-review credential via `Secret=gh_app_key_fitness` + `Environment=GH_APP_FITNESS_ID=…` — fleet-constant IDs inline, commented until the secret exists) |
| distrobox.ini | claudebox manifest: image pin, pre_init_hook drops Anthropic `latest`-channel `.repo`, additional_packages |
| claudebox-init.sh | post-assemble host bridges (CONTAINER_HOST export + in-box `claudebox-rebuild` flag-writer). Runs over quote-safe `podman exec` (container-root) channel. Also writes the claude-code self-update LOCKOUT (`/etc/profile.d/20-claude-no-selfupdate.sh`: `DISABLE_UPDATES`/`DISABLE_AUTOUPDATER`) and SELF-HEALS any native-build shadow (`~/.local/bin/claude` symlink into `~/.local/share/claude/` + the version store) off the home volume so the package-managed `/usr/bin/claude` always wins |
| claudebox-assemble.sh | called by entrypoint on first boot (via `--if-stale`) + by box-rebuild on every rebuild (unconditional): `distrobox rm -f` (recovery) → `distrobox assemble create` → first-enter retry → bridges + policy stamp. An EXIT trap writes `~/.local/state/claudebox/.assemble-failed` on any non-zero exit (clears it on success) — the single authoritative signal for every assemble path (first boot, `bin/claude` self-heal, box-rebuild) that the health cmd reads so a half-assembled box surfaces UNHEALTHY (#11). **REASSEMBLE-ON-IMAGE-CHANGE (`--if-stale`, 2026-07-19):** an R17 `rebuild-devbox` / monthly base refresh / redeploy RECREATES the fedora-dev CONTAINER onto a new image, but the claudebox Distrobox + its `.assembled` marker live on the HOME VOLUME and OUTLIVE the container — so the new image's baked seed / policy stamp / claude-code would stay FROZEN at the last assembly (the entrypoint's first-boot guard skips because `.assembled` is present; incident #198: the box ran a 6-day-stale claudebox after a fresh-image rebuild). The pure `reassemble_needed` (covered by `--selftest`) compares the RUNNING fedora-dev image id (base `/run/.containerenv`, read at the base level where this + the entrypoint run) to the id recorded in `.assembled-image` at the last assembly: a change (or absent/`.assemble-failed`) forces a fresh assemble; a match SHORT-CIRCUITS the boot call to a fast no-op (before the EXIT trap is even set — a healthy skip has zero teardown side effects). On an image change it CLEARS `.assembled` first so the R17 box-ready gate WAITS for the fresh box instead of restoring sessions onto the stale one. `.assembled-image` is written after `touch .assembled` ONLY when the image id is readable (an empty file would make every later boot look image-changed — a reassemble loop). box-rebuild.sh calls WITHOUT `--if-stale` (unchanged, forced). Covered by `claudebox-reassemble.test.sh`. **The policy stamp lands 644 root and is VERIFIED loadable before READY (#175):** mktemp creates 0600 and in-box `cp` carries the SOURCE mode onto a new dest (an existing dest keeps whatever mode it had) — observed live 2026-07-13: a root-owned 0600 `/etc/claude-code/CLAUDE.md` no session could load, stamped and completely INERT — so the assemble chmods both stamped files 644 (root ownership is the point, readability is the function), then reads the law back **AS the session user** (`podman exec --user core`) and cmp's it against the just-assembled content (managed-settings checked readable as core too): 'stamped' means *readable and current*, not 'a file exists'; any failure aborts BEFORE `.assembled` and surfaces via the #11 marker |
| claudebox-reassemble.test.sh | proves the REASSEMBLE-ON-IMAGE-CHANGE gate (`claudebox-assemble.sh --if-stale`, incident #198). Drives the REAL script against a fixture STATE + a fixture `/run/.containerenv` (`FD_CONTAINERENV` seam) + a throwaway `ASSEMBLE_LIVE`, with STUB podman/distrobox/git on PATH so it NEVER touches the real engine (the gate runs BEFORE any engine call, so its stdout + the `.assembled` clear are the deterministic signals). Rows: `--selftest` PASS (the pure `reassemble_needed` decision) / FRESH (assembled for the CURRENT image) → no-op skip, rc 0, `.assembled` intact, ZERO engine calls / IMAGE-CHANGED (new image id) → `.assembled` CLEARED (box-ready gate waits) + assemble attempted / ABSENT → proceeds (first boot) / prior-FAILED same-image → still reassembles (#115 self-heal). MUTATION RUN IN-SUITE: neutralize the image-change branch (`echo image-changed; return 0` → `echo fresh; return 1`; the sed must genuinely change the copy, else vacuous) and the NEW-image box wrongly SKIPS (frozen-stale — the exact #198 bug), proving the branch discriminates. `bash claudebox-reassemble.test.sh` → exit 0. No GitHub/network/model/real engine. Run after touching the `--if-stale` gate or its decision | **The policy stamp lands 644 root and is VERIFIED loadable before READY (#175):** mktemp creates 0600 and in-box `cp` carries the SOURCE mode onto a new dest (an existing dest keeps whatever mode it had) — observed live 2026-07-13: a root-owned 0600 `/etc/claude-code/CLAUDE.md` no session could load, stamped and completely INERT — so the assemble chmods both stamped files 644 (root ownership is the point, readability is the function), then reads the law back **AS the session user** (`podman exec --user core`) and cmp's it against the just-assembled content (managed-settings checked readable as core too): 'stamped' means *readable and current*, not 'a file exists'; any failure aborts BEFORE `.assembled` and surfaces via the #11 marker |
| box-rebuild.sh | full claudebox rebuild (self-serializing via flock); triggered by daily tick / in-box flag / host-shell command — all converge here |
| bin/rebuild-request.sh | the DEV-SIDE PRODUCER for **R17 REBUILD CONTINUITY** (#174) — the counterpart to fedora-bootstrap's merged `rebuild-devbox` host executor. THE PAIR (see [[pair-is-the-orchestrator]]): a purposeful rebuild KILLS the dev container + every session inside it, rebuilds to spec, then RESTORES + RESUMES the sessions — and code INSIDE the box dies at the KILL step, so the HOST does the whole lifecycle from OUTSIDE (a container destroyed from the host leaves no PID-namespace ghost, which an in-container `distrobox rm -f` cannot avoid). The executor owns KILL→REBUILD→RESTORE→RESUME→VERIFY; the ONE thing it cannot know is what was running on the dev box, which is exactly what THIS producer supplies. It **enumerates the live claude tenant PROCESSES** (D4/#191) — each carries its ASSIGNED `--session-id` in its cmdline (`bin/claude`) and its cwd in /proc, both readable at the base level (shared PID namespace) — into the executor's exact `session <name> <cwd> [<sid>]` MANIFEST grammar (between `%%DEVBOX-MANIFEST-BEGIN/END%%` sentinels) and composes the `host-op: rebuild-devbox fedora-dev` ticket; the **consumer half is fedora-bootstrap#143** (MERGED 2026-07-15 — the executor that recreates each session `tmux new-session -d -s <name> -c <cwd>` and resumes it **BY ID** `claude --resume <sid>` so N tenants sharing one cwd all come back). The sid is ONLY knowable this way (it is NOT in /proc/environ and the process holds no open transcript fd), so ASSIGNING it and reading it from the cmdline is the mechanism. `sid_from_cmd` reads the sid from a FRESH launch (`--session-id <uuid>`) OR a RESTORED session (`--resume`/`-r <uuid>` — what the executor itself types to bring a session back); reading only `--session-id` would miss every restored session, so resume-by-id would survive just ONE rebuild then collapse on the next (rebuild #1 leaves every session as `--resume <sid>`). **V2 ROLLOUT GATE — `manifest_v2_enabled` (`DEVBOX_MANIFEST_V2`, DEFAULT OFF):** the 4-field by-id grammar is understood only by the fedora-bootstrap#143 executor (merged 2026-07-15), whose RUNNING deploy LAGS the merge (a host-apply). So v2 4-field emission is OPT-IN and defaults OFF — by default the producer emits **v1 3-field** lines, safe against ANY deployed executor; even against a not-yet-redeployed 3-field executor a 4-field manifest would be **fail-safe REFUSED** (`parse_manifest` is validated BEFORE the kill — no session stranded, the rebuild simply does not fire), and default-off means it never emits one. **Flipped ON (2026-07-17):** fedora-dev's deploy env (`run.sh` + `fedora-dev.container`) now sets `DEVBOX_MANIFEST_V2=1`, so this apparatus emits v2 by default — N tenants sharing a cwd restore BY ID (the fail-safe REFUSE backstops a host executor still lagging #143). The CODE default stays OFF (safe for any other deployment); set `DEVBOX_MANIFEST_V2=0` to force v1. `valid_sid` + the test oracle are byte-matched to #143's fixed-width 8-4-4-4-12 grammar. Excludes headless `claude -p` + subagents (`CLAUDE_CODE_CHILD_SESSION`); a tenant from an OLD bin/claude (no sid) or a non-UUID sid **degrades to a v1 3-field `claude --continue` line** rather than poisoning the ticket (`valid_sid` byte-copies the executor's UUID grammar — a bad 4th field would rc=2-reject ALL sessions). **DEDUP by sid (`dedup_by_sid`, 2026-07-19):** one interactive tenant runs MULTIPLE live `claude --resume <sid>` procs (observed: TWO per tenant), so the per-proc `enumerate_claude_procs` double-counts the SAME session — left uncollapsed, the manifest lists N sessions for N/2 real tenants and the executor's FINISH tally reports an UNTRUE "restored N/N". A sid is unique per session, so `manifest_block` collapses same-sid lines to ONE (a no-sid line has no key ⇒ passes through) BEFORE the cap, so the cap counts DISTINCT sessions. Placed in the testable pipeline (not the /proc reader) so the `SESSION_SOURCE` seam covers it; mutation-proven in `rebuild-request.test.sh` (remove the stage ⇒ 3 same-sid procs leak as 3 lines). `SESSION_SOURCE` is the test seam; `enumerate_tmux` is kept as an alternative source but cannot carry a sid. **Runs at the fedora-dev BASE level AS CORE (uid 1000)** — the sids/cwds are readable by **core** (1:1-mapped into the claudebox userns, so it is the SAME uid as the session procs; `ptrace_scope=0` lets it read them), NOT by **root** (which sits in the PARENT userns and captures **ZERO** — an empty manifest — proven on erebus 2026-07-21) nor by a bubblewrap-sandboxed claude Bash-tool shell (own process-lineage only). So verify a manifest dry-run **as core** (`podman exec -u 1000 …`), NEVER root or the in-session Bash tool. **COMPLETENESS GUARD (`seen_sids` + `missing_sids`, fail-safe):** because a wrong-context run drops un-readable sessions SILENTLY (`readlink /proc/<pid>/cwd` is ptrace-gated) and the executor treats a partial/empty manifest as "these are all the sessions" (a rebuild then KILLS the omitted ones and never restores them), `file_ticket` CROSS-CHECKS the captured manifest against the live tenant sids read from **world-readable** `/proc/<pid>/cmdline` (NOT ptrace-gated) and **REFUSES to file a manifest that OMITS a live session** — turning silent session loss into a loud refusal; the fix is always the CONTEXT (run as core), never a weaker manifest. **AMBIGUITY GUARD (`ambiguous_v1_cwds`, fail-safe):** a NO-SID (v1) line restores via `claude --continue <cwd>`, which resolves to the MOST-RECENT session in that cwd — so when it SHARES a cwd with any other manifest line it MIS-RESTORES (resumes one session twice, DROPS the other; observed live 2026-07-21 #226: a v2 `s-ebcfa847 /home/core <sid>` + a no-sid `s-p2128 /home/core` dropped this very session). `missing_sids` cannot catch it — a no-sid line's session has no readable sid, so it is never "missing" (a distinct blind spot) — so `file_ticket` ALSO REFUSES a v2-mode manifest whose no-sid line shares a cwd; a LONE no-sid line on a UNIQUE cwd stays valid (`--continue` there is unambiguous, the legitimate old-wrapper fallback). Same fix: capture as core so every session carries its `--session-id` (v2 by-id restore, never `--continue`). tmux is a base package, NOT in the claudebox — like `box-rebuild.sh`. **Skips (loudly) the ephemeral `c<pid>` client sessions** (transient `main`-group views, `destroy-unattached on`) and **any name/cwd the executor's `parse_manifest` would reject** — one bad line rejects the WHOLE ticket (rc=2), so the producer must never emit one; the name/cwd allowlists are byte-copies of the executor's, and the cap is `DEVBOX_MAX_SESSIONS`=32 (a drop is logged, never silent). **`file` MODE (R17 APPROVAL FLOW, 2026-07-19 — pairs with fedora-bootstrap v1.2.69):** the apparatus FILES the approval ticket itself — `file_ticket` captures a FRESH manifest and `gh issue create`s it as the App (title `🔴 APPROVAL REQUIRED: rebuild-devbox …`, labels `host-task` + `rebuild-approval`, an `@`-mention `REBUILD_APPROVER_MENTION` → GitHub-app push); the maintainer's ENTIRE act is ONE **`approved`-label tap** (the executor's v1.2.69 approval gate fires on it — timeline-bound, applier role-checked, App-label inert; his AUTHORIZATION is the role-checked label, never the mention string). IDEMPOTENT (an OPEN same-op ticket ⇒ no duplicate; a closed/rejected one never blocks a re-file) + R16-scoped (out-of-scope control repo ⇒ refuse). Invoked by the POLLER's `rebuild_request_tick` (the sanctioned headless bus-writer — touch `~/.local/state/rebuild-request/requested` and the next firing files it), NOT the interactive agent. The maintainer-authored `request` mode (prefilled URL + `ticket-body.md`) emits the SAME 🔴 title + `host-task`/`rebuild-approval` labels via the shared `ticket_title`/`existing_open_ticket` helpers, so the two filing paths cannot DRIFT (the #242/#243 incident — a hand-filed request-mode ticket looked nothing like the apparatus's own file-mode/host one) and it DEDUPS against an already-open ticket (pointing at it, not presenting a duplicate); authorship is still approval. The R17 red line holds: the destructive act fires ONLY on a maintainer's explicit, role-checked act; what changed is WHICH act (one tap instead of hand-authorship). `--selftest` covers the pure allowlist/filter/compose helpers; covered end-to-end by `rebuild-request.test.sh` (incl. file-mode + poller-wiring rows). **Control-plane (box lifecycle).** |
| claudebox-daily.sh | daily-refresh decision: probe session lock → rebuild now if idle, else write `rebuild.pending` marker (the `claude` wrapper fires it on session exit) |
| bin/claude | host-shell wrapper; holds SHARED session lock for session lifetime so daily refresh + host's monthly fedora-dev refresh defer while live; on exit fires deferred rebuild atomically. **SESSION IDENTITY + TRUST (00-DESIGN.md D4/D5):** on a FRESH interactive launch it mints a UUID and passes `claude --session-id <uuid>` (skipped when the operator already named a session — `--resume`/`--continue`/`--session-id`/`-r`/`-c`, incl. the R17 restore path `claude --resume <sid>`, so a second id never conflicts) — the assigned id is what lets a purposeful rebuild resume THIS session BY ID even when N tenants share one cwd, and it is visible in the process cmdline for `bin/rebuild-request.sh` to enumerate. The inner in-box login shell first pre-accepts the folder-trust dialog for the launch cwd via `bin/seed-claude-trust.py` (D5 — a restored session otherwise stalls on the "trust this project?" prompt, "restored but idle", which R17 RESUME forbids). Both are fail-safe (`\|\| true` / `${SID:+…}`) — a mint/seed failure degrades to the v1 `--continue` / a one-time prompt, never a blocked launch. **SCOPE SELF-SEED (2026-07-17):** before the enter loop it also calls `bin/session-scope-seed.sh <effective-sid>` with `SESSION_HOLDER_PID=$$` (this wrapper blocks on the enter for the whole session, so `$$` is the session-lifetime holder) — binding/refreshing THIS session's objective-backed R27 scope so it survives every rebuild; fail-safe (`\|\| true`), a bare `--continue` (no sid) simply skips it. **ANTI-STALL MARK (2026-07-19):** the inner `bash -lc` `export FD_INTERACTIVE=1` before the `exec` — the one signal that marks THIS as an INTERACTIVE session so the anti-stall Stop hook (`bin/anti-stall-stop.sh`) fires here but NEVER on a headless `claude -p` (which bypasses this wrapper and so never sets it). **Control-plane (box lifecycle).** |
| bin/seed-claude-trust.py | pre-accepts Claude Code's first-run folder-trust dialog for the given cwd(s) (00-DESIGN.md D5) by setting `projects["<cwd>"].hasTrustDialogAccepted=true` (+ `hasCompletedProjectOnboarding`) in `~/.claude.json`. Runs IN-BOX (python3 is not at the base level; `bin/claude` invokes it inside the launch enter). SAFE against racing claude's own writes to that file: writes ONLY when a flag actually needs changing (idempotent no-op after the first launch per cwd), writes ATOMICALLY (temp + `os.replace`), NEVER raises (a parse/IO failure degrades to a one-time prompt, never a crash or a corrupted config). Covered by `seed-trust.test.sh`. |
| seed-trust.test.sh | covers the D4/D5 launch changes: PART A the trust pre-seed (seeds a cwd / idempotent-no-write-when-set / preserves other projects+top-level keys / garbage-file degrades without corruption / relative-cwd ignored); PART B the `--session-id` assignment case in `bin/claude` — a FRESH launch mints a sid, a launch already naming a session (`--resume`/`--continue`/`--session-id`/`-r`/`-c`) does NOT (the restore path stays single-flagged), replicated with a DRIFT GUARD that fails if `bin/claude`'s case pattern changes. `bash seed-trust.test.sh` → exit 0 = all pass. No GitHub/network/model. Run after touching the launch identity/trust path |
| g7-poller-persist.test.sh | the G7 poller ARMING-DURABILITY safe-parse (`entrypoint.sh`). A rebuild-to-spec drops the poller's out-of-band env, so the entrypoint PERSISTS the operator's arming on the home volume + RESTORES it on a later boot; that file is core-writable and the restore runs AS ROOT, so it is PARSED (known keys, value-validated), never SOURCED — sourcing would let `core` inject code root executes (a privilege escalation). Proves the parse honours valid values, REJECTS malicious ones (a `POLLER_ARMED=1 && touch …` or `$(…)` value is neither honoured nor executed — a canary file stays absent), + DRIFT GUARDS pinning the replica to the real entrypoint (it READS the file not sources it, case-validates `POLLER_ARMED` to 0\|1, defaults `POLLER_ENABLED` ON). `bash g7-poller-persist.test.sh` → exit 0. No GitHub/network/model. Run after touching the poller enable/arm persistence |
| bin/session-scope-seed.sh | the SESSION SCOPE BINDER (self-sustaining multi-tenancy, 2026-07-17) — `bin/claude` calls it at EVERY launch/resume with the session's holder pid in `SESSION_HOLDER_PID` (the long-lived wrapper's `$$`). It (a) **REFRESHES** a persisted registry entry's liveness coords (a RESUMED session keeps its durable R27 scope; fresh coords stop a later `register()`/reap from freeing a still-held scope — this is what makes scope survive every rebuild), or (b) **SELF-SEEDS** a session with no entry from its declared objective config (`${SCOPE_REGISTRY_DIR}/<sid>.objective` = one line `<repo> <objective-doc-path>`) by `repo-scope.sh transcribe --objective` (repos DERIVED from the confirmed objective, sha resolved from the clone — never authorizes), or (c) **NO-OP** for an undeclared session (ceiling). FAIL-SAFE by contract: every path degrades to a no-op and NEVER blocks a launch (`bin/claude` calls it `|| true`). Base level, no network. Covered by `session-scope-seed.test.sh` (mutation-proven). **Control-plane (scope/box lifecycle).** |
| session-scope-seed.test.sh | proves `bin/session-scope-seed.sh` against the REAL session-registry + repo-scope + a REAL git objective fixture: SELF-SEED a declared new session · REFRESH a resumed one (new coords, scope preserved, no re-transcribe) · NO-OP undeclared · fail-safe on a missing clone · TWO in-suite mutation rows (neutralize the transcribe → a declared session binds nothing; neutralize the refresh → a resumed session's coords stay stale) · a `bin/claude` wiring drift-guard (`$$` holder). `bash session-scope-seed.test.sh` → exit 0. No GitHub/network/model |
| bin/anti-stall-stop.sh | the INTERACTIVE ANTI-STALL Stop hook (#25, 2026-07-19; rebuilt on FACTS 2026-07-28, #279) — the interactive analog of the headless loop's "no ask path". The headless machinery (dev-author / pr-poller → `claude -p`) CANNOT stall (a would-be prompt becomes a denial it adapts around); an INTERACTIVE session CAN end a turn mid-objective, far below the doctrine's stop-ONLY-on-SHIP-or-genuine-BLOCK bar. Doctrine-as-text (willpower) does not fix a model disposition — a STRUCTURAL forcing function does. Registered as a MANAGED `Stop` hook (`allowManagedHooksOnly` makes user/project hooks inert; the arming lives in `policy/managed-settings.json`), gated `FD_INTERACTIVE=1` (set only by `bin/claude`, so a headless `claude -p` is never touched). **#279 — THE STOP IS DECIDED BY FACTS, NOT BY PHRASING.** The first cut tested the SHAPE of the final message: a CHECK-IN SIGNATURE ("want me to proceed?") blocked, a declared `^DONE:`/`^BLOCKED:` verified against the R30 oracle, everything else ALLOWED. It fired twice in one session and worked exactly as designed — and the model then ADAPTED, writing factual status reports that carry no signature and no sentinel, handing control back and stopping anyway (the maintainer had to push it forward ~6× in one session). A guard on the FORM of a sentence is a guard the author of the sentence can always satisfy. So the ORACLE is now consulted on **EVERY** interactive stop, not only a declared one, and the decision reads: **WORK REMAINS** (`STATUS: OPEN`, or the standing work plan still carries an unchecked `[ ]`/`[~]` item — `OPEN_PLAN_ITEMS`) **and no maintainer DECISION is named ⇒ the stop is a DEFECT however it is worded** — a status report, a summary, a silent hand-back and a `DONE:` sentinel all block identically (`oracle-UNDECLARED` / `oracle-DONE` / `oracle-BLOCKED`, each fed back with the EXACT next drive action). **A `BLOCKED:` must name a MAINTAINER DECISION** (`blocker_is_decision`): "waiting for a gate", "watching a monitor", "letting the queue drain", "the host must validate this", "my tier" are the LOOP'S OWN WORK — the session owns the live-validate round-trip and every verdict it claims to be waiting on — so such a blocker is refused (`nondecision-BLOCKED`) even when the oracle is SILENT, because that is a positive read of the model's OWN text, never an inference from silence; anything unrecognised is treated as a genuine decision (the classifier refuses only what it can positively identify). The check-in SIGNATURE survives ONLY as the fallback for a silent oracle — it can add teeth, never remove them. **FAIL-OPEN BY CONSTRUCTION** — every uncertainty ALLOWS the stop (unparseable input, no python3, missing transcript, unreadable/unwritable state, an unavailable/slow oracle, a missing work plan, ANY error; a malformed block-output is a no-op to claude-code, so even a wrong hook API is safe: worst case the nudge does nothing, NEVER a trap). **ANTI-TRAP BOUND** — a per-session time-windowed consecutive-nudge counter caps runaway at `ANTI_STALL_CAP` (3) then ALLOWS + resets, bounding EVERY block path incl. the fact-driven ones; `stop_hook_active` is a second belt. **DISCLOSED COST** (the trade #279 asks for, not a defect): mid-objective a purely CONVERSATIONAL yield is nudged too — the hook cannot separate "I answered a question" from "I stopped" without reading the phrasing again — so the CAP is the valve, the human can interrupt at any time, and the doctrine's own answer is that mid-objective the session should be driving. The DURABLE fix is architectural, not a better hook: keep the interactive agent OFF the critical path and let the headless loop drive (00-DESIGN.md **D6**). Pure core (`blocker_is_decision`/`stall_signature`/`work_remains`/`stop_mode`) under `--selftest`. Seams: `ANTI_STALL_ORACLE`/`ANTI_STALL_ORACLE_TIMEOUT` + `ANTI_STALL_STATE_DIR`/`ANTI_STALL_CAP`/`ANTI_STALL_WINDOW`/`ANTI_STALL_TIMEOUT` (12s) + a self-`timeout` re-exec. Covered by `anti-stall-stop.test.sh`. **Control-plane (agent stop-behaviour).** |
| anti-stall-stop.test.sh | proves `bin/anti-stall-stop.sh` is a FAIL-OPEN nudge that can NEVER trap a session AND — since #279 — that the stop is decided by FACTS, never by the SHAPE of the message. Drives the REAL hook with fixture Stop-hook JSON on stdin + a fixture transcript `.jsonl` + a STUB oracle (`ANTI_STALL_ORACLE`, which now also emits `OPEN_PLAN_ITEMS`/`PLAN_ISSUE`); asserts BLOCK (`{"decision":"block"}`) vs ALLOW (empty stdout, exit 0). **THE LOAD-BEARING ROW (#279): a FACTUAL STATUS REPORT carrying no check-in signature and no sentinel, while the oracle reads OPEN, must BLOCK** — the previous suite asserted the OPPOSITE (it allowed exactly the message the model adapted to write), so this row is the defect's inversion, not an addition. Further rows: the same report on an INDETERMINATE and on an UNAVAILABLE oracle ALLOW (fail-open — silence never traps, and conversation survives it) / a SHIPPED ship-verdict whose standing work plan still has `[ ]` items BLOCKS and NAMES the plan issue / a `DONE:` over that same open plan BLOCKS / `BLOCKED: waiting for the host live-gate` and `BLOCKED: letting the merge queue drain` are REFUSED even on a silent oracle, while a genuine fork (`ship unsigned, or add cosign back?`) is still HONOURED (no over-blocking) / over-CAP on the UNDECLARED path ALLOWS (the new arm cannot trap either) / plus an **END-TO-END row** running the REAL hook against the REAL `bin/objective-status.sh` (only `gh` stubbed, serving nothing but an open work plan) — every other row stubs the oracle, so none of them would notice the two halves disagreeing about the KV grammar; proven non-vacuous (against the pre-#279 oracle, which emits no `OPEN_PLAN_ITEMS`, the same fixture ALLOWS) / plus the standing fail-open + `--selftest` rows (which assert the selftest RAN, since a bare rc check passes against a script that has none). FIVE MUTATIONS RUN IN-SUITE (each `sed` must genuinely change the copy, else vacuous): neutralize the CAP bound → over-CAP BLOCKS; neutralize the oracle-OPEN tooth → a false-DONE ALLOWS; **neutralize the UNDECLARED arm → the status report ALLOWS again (the #279 hole reopened)**; neutralize the work-plan tooth → an open plan no longer blocks; make every blocker a "decision" → "waiting for a gate" is honoured. PROVEN TO BITE: 12 of 30 rows FAIL against the pre-#279 hook. `bash anti-stall-stop.test.sh` → exit 0. No GitHub/network/model. Run after touching the hook. NOTE: the hook FIRING live (claude-code honoring the managed `Stop` block) is observed only in a real session — the SCRIPT logic is fully unit-tested; the fail direction of any live-contract surprise is a no-op (allow), never a trap |
| bin/objective-status.sh | the R30 WHOLE-OBJECTIVE SHIP ORACLE — an EXTERNAL, read-only FACT source answering, for the session's bound objective, "is there DRIVABLE OPEN WORK, or is the objective SHIPPED?" so the anti-stall Stop hook can VERIFY a self-declared `DONE:`/`BLOCKED:` rather than trust its FORM (the durable fix for the interactive-driver false-stop, #25). Every prior anti-stall defence trusted a MODEL-authored signal; this is the fact that authenticates the sentinel's TRUTH. The facts (all GitHub/clone-derived — NO local state, the `reconcile.sh` precedent): **OPEN BACKLOG** (`backlog`-labelled OPEN issues in the bound repo — a CLOSED one is a proof-shipped feature per `reconcile.sh`); **OPEN DEV PRs** (OPEN PRs authored by `$DEV_LOGIN`, not carrying an escalation label — each is DEV-drivable: label `live-validate` → read the verdict → iterate RED→GREEN → the poller merges; the signal that nails "a pushed-but-unmerged PR is not done"); **ACCEPTANCE PROBE** (optional, side-effect-free — `$OBJECTIVE_ACCEPTANCE` or `<objective-doc-dir>/objective-acceptance.sh`; DECLARED-and-FAILING ⇒ OPEN even on an empty backlog — the "off-backlog work reads SHIPPED" guard; ABSENT ⇒ not required); **THE STANDING WORK PLAN** (#279 — the OPEN issue whose title starts with `$PLAN_TITLE`, default `STANDING WORK PLAN`, DISCOVERED BY TITLE and never by a hardcoded number, the `bin/fleet-halt.sh` precedent; its strict-prefix match is re-checked locally because GitHub issue search is fuzzy): its unchecked `- [ ]`/`- [~]` items are emitted as `OPEN_PLAN_ITEMS` + `PLAN_ISSUE` and are **REPORTED, NEVER FOLDED INTO THE VERDICT** — an unchecked box is a reason not to STOP, not a proof the built product is unshipped, and folding it into `drivable` would let ONE stale checkbox freeze `bin/ship-actuator.sh` out of ever closing an objective; the anti-stall Stop hook reads the KV directly, every other consumer reads `STATUS` and is untouched. Its fail direction is the opposite of the other facts by design: an unreadable/absent plan reports 0 (the gate loses one tooth) rather than INDETERMINATE (which would REMOVE the teeth the other facts already have) — it can only ever ADD teeth, and fabricates no verdict either way. `PLAN_TITLE=''` switches it off (a `-` default, not `:-`, so an explicit empty value really disables it). VERDICT (pure `classify`): `OPEN` iff drivable≥1 OR probe FAILs · `SHIPPED` iff drivable=0 AND probe∈{PASS,ABSENT} AND ship-evidence exists (a backlog EVER, or a passing probe) · `INDETERMINATE` otherwise (no anchor / unreadable GitHub / zero evidence) — the gate treats INDETERMINATE as "no teeth here" and falls back to its heuristic, NEVER trapping on the oracle's silence. FAIL-CLOSED to INDETERMINATE on any unreadable input. Repo resolved from arg > `$OBJECTIVE_REPO` > the session anchor (`$SCOPE_REGISTRY_DIR/<sid>.objective` — the SAME anchor `session-scope-seed.sh` consumes; no new wiring). `objective-status.sh --status [<repo>]` emits the KV STATUS block; `--selftest` covers the pure core (`pr_drivable`/`classify`/`plan_unchecked`/`next_action`). Covered end-to-end by `objective-status.test.sh`. **Control-plane (ship-recognition oracle).** |
| objective-status.test.sh | proves `bin/objective-status.sh`'s LIVE PATH: drives the REAL script with a scenario-driven STUB `gh` (open/all backlog + PR TSV files) + real acceptance probes, asserting the STATUS verdict + drive-action NEXT. Rows: a drivable dev PR → OPEN w/ `drive PR #N` / an open backlog issue → OPEN w/ `author + ship` / empty+evidence → SHIPPED / no-evidence → INDETERMINATE / **fail-closed** — a failed backlog read AND a failed PR read each → INDETERMINATE / an `escalate`-labelled dev PR is excluded (with evidence → SHIPPED) / a foreign-author PR is excluded / an `app/`-prefixed author is normalised to the dev login → OPEN / a failing probe → OPEN / a passing probe (as the sole evidence) → SHIPPED / no repo resolvable → INDETERMINATE. **PLAN rows (#279):** unchecked `[ ]`/`[~]` items are REPORTED (`OPEN_PLAN_ITEMS: 2`, `PLAN_ISSUE: 274`) while the ship verdict stays SHIPPED — the row that pins the deliberate boundary, since folding the plan into the verdict would freeze the ship actuator / an all-checked plan reports 0 / an UNREADABLE plan read reports 0 and leaves the verdict intact (it can only ADD teeth) / `PLAN_TITLE=''` disables it. TWO MUTATIONS RUN IN-SUITE: neutralize the live plan read → the unchecked items vanish (proving the read is what counts them); neutralize the live drivable-PR count → a drivable PR reads SHIPPED (proving the count is what yields OPEN). Per-row env rides `env`, not a shell assignment-prefix (an empty/`"$@"` prefix would become the command word); `$SCOPE_REGISTRY_DIR` is pinned to an empty dir so a leaked in-box session id resolves no anchor. `bash objective-status.test.sh` → exit 0. No GitHub/network/model. Run after touching the oracle |
| bin/claudebox-rebuild | host-shell trigger; starts box-rebuild.sh detached + tails the log |
| bin/build-throwaway.sh | standardized in-box THROWAWAY candidate build: wraps `podman build --isolation=chroot` with the persistent dnf cache (`$HOME/.cache/fd-dnf`→`/var/cache/libdnf5`) + the persistent PINNED-DOWNLOAD cache (`$HOME/.cache/fd-dl`→`/var/cache/fd-dl`, #320 — a build that uses the declared fetch contract serves its pinned vendor assets from the home volume; the bind is INERT for a repo that does not, and no Containerfile is ever rewritten to opt one in) + the home-volume layer cache so churn re-downloads nothing; ENFORCES the download cache's caps from `sweep_orphans()` by calling `bin/fd-fetch.sh --gc` (fail-safe: a missing/failing sibling is logged, never fatal — a build must not die because a GC could not run); tags `localhost/disposable/<name>:val-<sha-or-rand>` (never pushed); trap teardown rmi's the candidate + rm's any temp tree on EXIT (success/fail/INT/TERM/HUP) while the caches persist; sweeps orphan disposable images + temp trees first so a kill-9 leaves nothing on the home volume; builds only the repo's OWN Containerfile (provenance) and never mutates the immutable live tree (`-c <srcdir>` bolts on a separate throwaway COPY tree on the writable home volume). Complements `bin/validate.sh` (the full T1–T4 verdict harness) |
| bin/fresh-tree.sh | start task work on a GUARANTEED-fresh tree: `git fetch` + a `git worktree` checked out on a NEW branch off current `origin/main`, with its OWN HEAD+index, isolated from the persistent clone — the TOOLING for the worktree mandate in `policy/CLAUDE.md` (kills the stale-clone hazard where a long-lived `~/repos/<repo>` silently trails origin/main → wrong-base edits). Usage: `WT="$(bin/fresh-tree.sh <repo> <branch>)" && cd "$WT"`; commit + bare-push from there; `git -C <clone> worktree remove "$WT"` when done. Idempotent (reaps a stale same-named worktree first) |
| bin/gh-app-auth.sh | STANDING, auto-rotating GitHub credential so the autonomous dev loop never stops for auth. Mints a short-lived (<=1h) GitHub **App installation token** from the App private key (RS256 JWT via `openssl`; key read from tmpfs `/run/secrets/gh_app_key`, NEVER layered — Principle 5). `install` wires the token to BOTH consumers via **home-volume paths that exist IN-BOX** (where the agent runs git): git's built-in **`store` helper** + `~/.git-credentials`, and the box `gh` CLI's `hosts.yml` (+ `config.yml` version marker, avoiding gh's multi-account migration). `token` prints a fresh token. The <=1h token is re-minted on the entrypoint 40-min tick. Fail-closed (any missing input/signing/API error → non-zero, nothing on stdout; bounded curl timeouts so init can't hang). **Control-plane (secrets/identity).** |
| bin/gh-app-provision.sh | paste-based GitHub App provisioning helper for the SETUP wizards (a sourceable function library): reads an App private-key PEM **PASTED at the terminal** and streams it STRAIGHT into a `podman secret` (`--replace`, never a loose file on disk) + collects the two PUBLIC ids — feeding the container contract above (`bin/gh-app-auth.sh`, secret at `/run/secrets/gh_app_key`). `prompt_github_app` (by-hand `spin-up.sh`); `read_pem_to_var`/`make_secret_from_var` (the root→core ferry the host `day0.sh` uses). CANONICAL here; mirrored VERBATIM into fedora-desktop + fedora-bootstrap (keep in lockstep). **Control-plane (secrets/identity).** |
| bin/check-build-parity.sh | asserts the Tier-1 (in-box) build invocations carry the SAME parity-critical flags as the Tier-2 host build — the dnf-cache bind (`/var/cache/libdnf5`), the pinned-download cache bind (`/var/cache/fd-dl`, #320) + `BUILD_ARGS` forwarding — so an in-box GREEN reliably predicts a host GREEN. Motivated by #53 (validate.sh lacked the cache bind, so a bind-mount-only failure passed in-box but REDd at the host). **IT READS CODE, NOT PROSE:** a bind is detected as an actual `-v <src>:<path>` flag on a NON-COMMENT line, because the original whole-file `grep` for the path was satisfied by the HEADER COMMENT documenting it — measured when the mutation row that DELETED a real bind still reported it present. A parity check a docstring can satisfy is not a check; this is also why both binds name their in-build path LITERALLY at the call site rather than through a variable. **DIRECTION DECIDES THE EXIT CODE**, because the two directions are different bugs: a Tier-1 build missing a bind the Tier-2 host build HAS (or the two Tier-1 builds disagreeing) is the #53 FALSE GREEN — hard DRIFT, **rc 1**, and `validate.sh`'s T0b FAILS on it; a Tier-1 bind the host half has NOT ADOPTED YET only means the host build re-downloads a pinned asset the in-box build served from cache — a BANDWIDTH gap, not a false GREEN (the fetch contract falls back to fetching), so it is named out loud as DRIFT with **rc 3** and T0b reports it as PENDING without gating. Reporting it as OK would be vacuous; failing on it would wedge EVERY in-box validation in the fleet on a disclosed pending sibling. Today rc 3 has exactly one instance: the `/var/cache/fd-dl` bind, whose host half (fedora-bootstrap's `build-candidate.sh`) is the sibling feature — when that lands this reports `PARITY: OK`. `--isolation=chroot` is the one JUSTIFIED divergence (required in-box for nesting; absent on the host top-level engine). Run after touching any `podman build` line; checks `build-throwaway.sh` + `validate.sh` against fedora-bootstrap's `build-candidate.sh` (when its clone is present) |
| bin/fd-fetch.sh | the CACHE-AWARE PINNED FETCH (#320, objective #311) — the contract that replaces `ADD <url>` for a pinned vendor asset: `fd-fetch.sh <https-url> <sha256> <dest>` in a build, `--prefetch` host-side. **THE AUTHORITY on the contract is `CLAUDE.md`'s Principle 2 *Pinned fetches* section** (the measured table, the cache, the bounds); this row is the file's mechanics. WHY IT EXISTS, measured: buildah reports a layer-cache HIT on `ADD <url>` and **still transfers the whole file** (it downloads the URL to compute the cache key, then discards the bytes) — 10 378 KiB pulled under `--> Using cache e17eb17e7b87`, vs **0 asset bytes** on a contract rebuild whose fetch layer was deliberately re-run. **PROVENANCE STRENGTHENED, NOT LOOSENED:** a MISS fetches once over TLS and verifies the sha256 BEFORE publishing (a mismatch publishes NOTHING, leaves `<dest>` ABSENT and fails the build — a poisoned byte can never be cached); a HIT RE-HASHES the cached bytes against the key and, if they disagree, REMOVES the entry and re-fetches (a cache is not a trust boundary); a non-https URL or a `sha256` that is not 64 hex chars is refused before any byte moves (that 64-hex check is also the PATH-TRAVERSAL guard, since the key becomes a filename). `<dest>` is landed by stage-then-rename, so a refused fetch leaves no partial and never clobbers an existing file. **CACHE RESOLUTION:** `$FD_DL_CACHE` (explicit wins) → `/var/cache/fd-dl` **when it is actually a MOUNT** (read from `/proc/self/mounts`, the mechanism `install.sh` already uses — an empty mountpoint dir left in an image layer is a directory, not a cache) → `$HOME/.cache/fd-dl` → else NO-CACHE mode (a verified fetch, nothing published, said out loud). **INVARIANT: it NEVER CREATES a cache directory** (only `--prefetch` does). That ONE rule is what keeps a build with no bind — the CI base build, the monthly `--no-cache` rebuild — from writing cache entries INTO AN IMAGE LAYER. **BOUNDS** (`--gc`, called from `build-throwaway.sh`'s `sweep_orphans()`): age-prune >`FD_DL_CACHE_MAX_AGE_DAYS` (45) → LRU-evict to `FD_DL_CACHE_CAP_GB` (10) → reap stale `.part-*` partials (a kill-9 mid-fetch leaves one; it can never be served since its name is not a valid key, so it is pure residue). Both caps read LAST-USE time (a HIT touches its entry), so a hot pin is never aged out. `FD_DL_CACHE_CAP_BYTES` is the exact-byte cap the test uses to exercise LRU without a 10 GB fixture. rc 0 served · 1 fail-closed · 2 usage. `--selftest` covers the pure core (`cache_key`/`hash_ok`/`cache_decision`/`url_tls_ok`/`over_cap`/`resolve_cache`); covered end-to-end by `fd-fetch.test.sh` |
| fd-fetch.test.sh | proves the download cache SERVES WITHOUT FETCHING, REFUSES WITHOUT PUBLISHING, and stays BOUNDED — and that it is WIRED (#320). **THE AXIS IS WHETHER A BYTE MOVED**, not whether a log line claims a hit: the defect this feature fixes was invisible to every "a cache exists" assertion, so the transport is a RECORDER on PATH (`curl` AND `wget` — an un-recorded fallback would let a zero-fetch row pass while bytes moved) and a HIT must invoke it **ZERO** times. That stub is also why no row touches the network. Rows: a MISS fetches EXACTLY ONCE and publishes under the content key / a HIT serves with ZERO fetches **while the transport is rigged to FAIL** (so the row cannot pass unless the cache served it) / a MISMATCH asserts three ABSENCES — no entry, no partial, no `<dest>` — and never clobbers a pre-existing `<dest>` / a CORRUPT entry is re-fetched and healed / NO-CACHE mode fetches, verifies, and creates NO cache dir (the no-image-bloat path) / `--prefetch` / age-prune, LRU-evict, stale-partial reap, and that a non-entry file and a FRESH partial (a concurrent in-flight download) are never touched. **PART D tests the WIRING SEPARATELY**, because a pure core cannot see that nothing calls it (#278's first cut shipped a router with zero call sites and a green selftest): it drives the REAL `build-throwaway.sh --sweep-only` (podman stubbed — the engine is not the subject) to prove the bound is enforced from the sweeper, the REAL `check-build-parity.sh` in both directions (all three carry the bind → `PARITY: OK` rc 0; host half absent → rc 3 naming the drift), and the REAL `validate.sh` T0b line (rc 3 → PENDING, rc 1 → FAIL). **FIVE MUTATIONS RUN IN-SUITE**, each vacuity-guarded (a sed that changed nothing FAILS its row): neutralize verify-before-publish → the mismatch fixture publishes a poisoned entry and writes `<dest>`; neutralize the HIT arm → the zero-fetch row goes back to the network; neutralize age-prune → a 60-day-old entry survives; neutralize LRU-evict → an over-cap cache keeps every entry; delete the bind from a `validate.sh` COPY → parity goes hard DRIFT rc 1 and T0b FAILS (the #53 direction must keep biting; only the T2-half-pending direction may be rc 3). `bash fd-fetch.test.sh` → exit 0. No GitHub/network/model/engine |
| bin/fitness-review.sh | the INDEPENDENT fitness harness (Step 4b): headless `claude -p` reviews a GREEN PR against the three questions (Q1 asked-for / Q2 non-contradict / Q3 fit + doctrine) and posts `Fitness review: VERDICT PASS\|RETURN\|ESCALATE` **as the fleet fitness App** (`FITNESS_LOGIN`, default `oso-gato-fitness-claudebox`) — the unforgeable signal `bin/auto-merge.sh` greps. Token via env `FITNESS_GH_TOKEN` or the entrypoint's ferried `~/.config/fitness/env` (explicit env wins). Fail-closed: refuses when the login is unset/== PR author, when the PR isn't host-GREEN, or when no token is available to --post. **THE PROMPT RIDES STDIN, NEVER ARGV (#155)** — Linux caps a SINGLE argv argument at `MAX_ARG_STRLEN` (32 pages = 131072 bytes) regardless of the far larger total `ARG_MAX`, so the old `$FITNESS_CLAUDE "$PROMPT"` failed to EXEC with `E2BIG` on any PR whose prompt crossed ~128 KiB and **the reviewer never ran** (measured: #147 22 KB ✓, #153 50 KB ✓, #154 141 KB ✗) — such a PR could never be reviewed, therefore never merge, while the poller re-attempted it every sweep, forever, in silence. **The reviewer's stderr is KEPT, never discarded:** an exec failure, an auth failure and a model that genuinely emitted no verdict are THREE events and get THREE distinct log lines — a harness that reports "the reviewer produced no verdict" when the reviewer never started is lying to its operator. **EXIT-CODE CONTRACT with the poller:** 0 = verdict posted · 1 = a precondition refused it (RETRYABLE) · **3 = the reviewer could not be RUN / produced no verdict for this head** — and rc 3 CANNOT say WHICH: its cause may be PERMANENT (E2BIG, missing binary, bad credential) or TRANSIENT (model API 5xx, rate limit, timeout). The harness therefore reports the reviewer's real stderr and asserts nothing about repeatability (the first cut of this told the caller *"re-running it unchanged will fail identically"* — true of #154's E2BIG, false of a timeout, and asserted-not-proven in a change whose thesis is that the failure report tells the truth). The CALLER must then do neither wrong thing: never re-spin at sweep cadence with no signal (R4), and never park a host-GREEN PR on the FIRST rc 3 (nothing else re-reviews it, so one blip would strand it forever) — it retries BOUNDED and SPACED, and surfaces only what survives (#156; `bin/pr-poller.sh`'s `review_due`). `FITNESS_DIFF_CAP` (200000) now sits BELOW the ceiling it protects — the old default sat ABOVE the 131072-byte argv ceiling, so it could never do its job — and truncation is LOUD (logged, stated in the prompt with an ESCALATE instruction, and stamped on the posted verdict: a reviewer silently handed a partial diff can PASS on code it never saw). Covered by `fitness-review.test.sh`. **R16 OPERATING SCOPE (#167):** refuses to review an out-of-scope repo, and RETURNs a PR that NET-ADDS a repo to `policy/scope.conf` without a maintainer's NAME-BOUND confirmation — a comment whose FIRST line is exactly `CONFIRMED <repo> [<repo>…]` covering EVERY added name (maintainer confirmations union; a bare or prose `CONFIRMED` confirms nothing, and a post-confirmation head that swaps or extends the adds re-gates unconfirmed — the empty-commit remediation flow survives, a swapped add does not) — DETERMINISTICALLY, before the reviewer model ever runs (a structural blocker needs no judgment, and none could unblock it); detection runs on the FULL diff, before the cap, with a HUNK-STATEFUL parser, so neither truncation nor a crafted added line that mimics a `+++ b/…` header can hide a scope hunk (see the `bin/repo-scope.sh` row); the reviewer prompt names this gate a FIRST layer, never a stand-down — the model still judges scope.conf edits itself. **Control-plane (merge-trust identity).** |
| bin/pr-poller.sh | the DEV-SIDE POLLER (Step 5): plain-shell watcher (NO model in the loop) that routes each open PR by (host verdict, fitness verdict) via the pure `plan()` core — RED → bounded headless fixer on the FEATURE branch; GREEN+unreviewed → `fitness-review.sh`; GREEN+PASS → `auto-merge.sh` for ANY tier (**`--commit` by default — armed with the apparatus per the gate-free objective; `POLLER_ARMED=0` is a deliberate dry-run soak**); **ZERO-GATE (2026-07-10): tier no longer routes to a human — `plan()` merges every host-GREEN + fitness-PASS PR regardless of tier**; only a fitness ESCALATE (reviewer defers) or an unknown fitness token still surfaces. **The fixer is ISOLATED and the HARNESS owns + VERIFIES its push (#152).** `run_fixer` hands the model a THROWAWAY WORKTREE (`fresh-tree.sh` with `FD_BASE_REF=origin/<ref>`) checked out on the PR's OWN gated head — never the shared clone the poller itself runs from (`policy/CLAUDE.md`: *"MUST NOT run PR git in a working tree another box or process may be mutating concurrently"* — the 2026-06-28 cross-branch-leak incident). FAIL-CLOSED: no clone / no worktree ⇒ NO fix is attempted (there is deliberately no shared-clone fallback; the pre-#152 `cd $HOME/.local/share/$POLLER_REPO` existed only for fedora-dev, so on the other two swept repos the cd failed, the fixer never ran at all — and it still logged its cheerful "(if pushed)"). The model COMMITS and nothing else; the SHELL pushes, scoped to an explicit refspec (`HEAD:refs/heads/<ref>` — it can name no other destination, least of all main), then VERIFIES THE LANDING AT ORIGIN (`ls-remote`): a push rc of 0 is NOT proof — a local commit that never reached origin is not progress. The pure, selftested `fix_outcome()` makes every outcome DISTINCT and truthful (`LANDED`/`NO_COMMIT`/`BLOCKED`/`PUSH_FAILED`/`NOT_LANDED`, plus fresh-tree-failed and branch-moved), retiring the old `(if pushed)` shrug that made a fixer which pushed NOTHING indistinguishable from one that succeeded (and parked the PR on a FALSE `blocked`). An UN-GATED head is never fixed (branch moved between the sweep's `$sha` and the checkout ⇒ skip; the new head re-gates itself), and the worktree is reaped on EVERY path (Principle 10). Covered end-to-end by `poller-fixer.test.sh`. **The fixer is told which gate ACTUALLY failed, and is fed THAT gate's own findings** (`fix_cause` — pure + selftested): `plan()` routes FIX from TWO causes, so a host RED feeds the fixer the host's RED **candidate-log detail** and a fitness RETURN feeds it the REVIEWER'S FINDINGS (spec #135 **R6** — *"findings are generative: RETURN reasons feed the fixer's next iteration"*). BOTH reasons are fetched from that gate's **full comment BODY**, bound to this head's sha AND that gate's own login — NOT from the line-1-only verdict stream the routing reads (grepping that stream can only ever yield the verdict HEADER, never the failure detail; the host header is markdown-bold, so its selector matches on `contains`, not `startswith`). TRUST BOUNDARY: a body is **PROMPT material only** — the verdict itself is still read from LINE 1 alone (G2), so neither reviewer prose nor an embedded candidate log can flip a gate; it can only say what to fix. (Before this, BOTH arms were broken by the same line-1-only binding: a RETURN — where the host is GREEN — scraped a host `VERDICT RED` comment that does not exist and fell back to a canned "host live-gate RED", so the fixer hunted a build failure that never happened, made no progress, and parked the PR on a FALSE "blocked"; and a real host RED handed the fixer its own verdict token as "the findings".) A FIX route with no known cause refuses to invent a reason and surfaces instead. **The fixer's prompt rides STDIN, never argv (#155)** — it embeds a gate's own findings (a candidate log, a reviewer's prose), so an argv prompt is a latent `E2BIG` past `MAX_ARG_STRLEN` (131072 bytes) that would silently stop the fixer from EXEC'ing at all; stdin has no ceiling, and it subsumes the old `</dev/null` (the model's stdin IS the prompt pipe, so it can never drain the sweep's PR list). **And the `cd` into that worktree is a FAIL-CLOSED GUARD, not a prefix:** `cd "$wt" && set +o pipefail; <pipeline>` binds the `&&` to `set` ALONE — the `;` ends the list — so the model would run even when the `cd` FAILED, in the POLLER'S OWN cwd (a shared clone), holding a prompt that tells it to commit: the 2026-06-28 cross-branch-leak hazard, re-opened by a bash-precedence slip, inside the merge path. The body is BRACE-GROUPED to the `cd` (no worktree ⇒ no model), and `run_fixer` refuses an unenterable worktree BY NAME — ahead of the branch-moved check, which reads the tree with `git -C` and would otherwise fail on it too and park the PR on a bogus *"the branch moved"*. **The REVIEW arm cannot spin silently — NOR strand a GREEN PR on one blip (#155 R4 + #156):** `fitness-review.sh`'s rc is a contract, and rc 3 (the reviewer could not be RUN / emitted no verdict for this head) covers BOTH a permanent cause and a transient one — the rc cannot tell them apart, so the arm must not treat either as certain. It retries rc 3 a BOUNDED, SPACED number of times (`FITNESS_REVIEW_TRIES`=3 × `FITNESS_RETRY_BACKOFF`=300s; the pure, selftested `review_due` decides RUN/WAIT/PARKED) and only a failure that SURVIVES them SURFACES a question carrying the reviewer's REAL stderr and stops the review on that head (re-running an un-runnable review at 10 s cadence burns a bounded model run per sweep and signals nothing; a new commit re-reviews on its own). Parking on the FIRST rc 3 — the first cut of this — was the mirror-image defect: a single model-API hiccup permanently stranded a host-GREEN PR, since the fixer only runs on RED/RETURN and nothing re-reviews a parked head, where before the rc contract that same blip self-healed on the next sweep. And a parked head is NOT written to the sweep's `acted` marker, so the remediation the question prints (fix the cause, re-run `fitness-review.sh --post`) is one the harness HONOURS: the hand-posted verdict routes to MERGE on the next sweep, rather than hitting the terminal-state skip. Any other refusal stays retryable — and a retryable refusal LOGS the harness's own stderr, which the arm captures to build the rc-3 question: capture-then-discard would leave the one path that REPEATS EVERY SWEEP as the only one that never says why (an unset `FITNESS_LOGIN` / missing `--post` token / SoD misconfig refuses identically forever). **REVIEW-INFRA HEALTH GATE (2026-07-20) — a GLOBAL outage is NOT N per-head failures:** an `rc 3` cannot itself say whether the whole reviewer infra (`claude -p`) is down for EVERY PR or just THIS head is broken — and on 2026-07-20 a >15-min `claude -p` window burned every open PR's 3 strikes and PARKED THEM ALL permanently (the bounded-retry-then-park was absorbing a global outage as N independent per-head failures). The gate adds the missing GLOBAL signal the cheapest way: when a review returns rc 3, a tiny `claude -p` liveness PROBE (`review_infra_ok`; pure `infra_healthy <rc> <out>` classifier trusts it ONLY on exit 0 AND the reply token — NON-empty output is not enough, since a `claude -p` auth failure prints "Not logged in…" to STDOUT and exits 1, the credential-expiry outage) disambiguates: DOWN ⇒ this is global — do NOT strike the head, PAUSE the arm for the sweep (no strikes, no questions; the remaining reviews skip), and it SELF-HEALS the next sweep once the probe passes (the R9-halt PAUSE pattern); a persistent outage past `FITNESS_INFRA_PAUSE_SURFACE` (1 h) surfaces ONE question (never a silent stall) while STILL auto-resuming. UP ⇒ genuinely per-head ⇒ the bounded retry + surface below, now CORRECT (a global outage can no longer masquerade as it). Probe-ON-FAILURE, so a sweep whose reviews all succeed pays nothing; `FITNESS_INFRA_CHECK=0` disables it. `infra_healthy` selftested (the auth-error-to-stdout case classifies DOWN); the pause+self-heal + a mutation (neutralize the gate ⇒ the global outage wrongly strikes) are covered in `fitness-review.test.sh`. Progress-based fixer stop (no silent quit; the signature spans cause+reason, so a host RED and a fitness RETURN on one head are distinct failures), ADJUDICATED flock singleton (#173 — see LOCK LIVENESS below), `--selftest` for the pure core. Every host/fitness verdict is bound to the PR's head SHA and the merge is `--match-head-commit`-pinned (a fresh, ungated head never inherits a prior verdict). **MERGE-rc ROUTING (#211):** auto-merge's exit codes route DISTINCTLY — rc 2 (behind main / conflict) surfaces a `rebase` ask and parks the head (only a rebase push can merge it, and that mints a NEW head which re-gates itself); rc 3 (transient / head moved / BLOCKED mergeStateStatus) surfaces ONCE and does NOT park — the acted marker is the MERGE arm's own terminal-state skip, so writing it would strand a host-GREEN+PASS PR on one blip (#156's stranding class) under a comment claiming it retries; instead auto-merge is re-invoked every sweep (plain shell, no model run) until it lands or the head moves; rc 1/unknown stays the `refused` trust-boundary alarm and parks pending a human INVESTIGATE. Each sweep FIRST retires SUPERSEDED PRs deterministically: a MERGED PR whose body carries a WHOLE LINE that is exactly `Supersedes #N[, #M…]` (same-repo; markdown-aware all-or-nothing parse — backticked/blockquoted/mid-sentence/malformed lines and fenced/code-indented blocks never act) closes a still-OPEN #N with a comment — the sanctioned close path (the interactive agent is classifier-denied `gh pr close` on PRs it didn't create, run-003 lesson b). Runs even DISARMED: the authorizing event is the superseder's human-gated merge; a close is reversible and never touches main (`RETIRE_LOOKBACK` most-recently-updated merged PRs, default 15, so a late-merging parked PR still enters the window; scan-once + retired state markers; a human REOPEN is durable — a prior retire comment on the PR blocks any re-close even after state loss; fail-closed to no-op on issue numbers / cross-repo refs / non-open targets / transient API failures, close failures logged as RETIRE FAILED). Sweep is fetch-BATCHED for the 30s cadence: ONE TSV list call carries number+ref+sha, ONE sha-bound comments call per open PR; a PARKED GREEN PR (acted marker present) is terminal-state-skipped at the same 1 call/sweep; fitness comments are fetched ONLY while a GREEN PR's routing is still pending, rc-checked so a transient fetch failure SKIPS that PR for that sweep (retry next) instead of misrouting (no spurious fitness re-runs); **STEP 5 of #274 removed the second GREEN-moment call** — it fetched the changed-file list to classify a tier that `plan()` reads on no branch, so a GREEN PR pending routing now costs +1 call, not +2 (one fewer call, and one fewer way for a transient failure to delay a merge; NO gate changed — eligibility is host-GREEN + fitness-PASS, re-checked at the trust boundary by `auto-merge.sh`); steady state ≈ 120×(2+N) calls/h, ceiling ~10 sustained open PRs. **SELF-REFRESH (#162) — the running poller picks up its OWN merged code with no human pull + bounce:** at a SAFE POINT (the TOP of a sweep cycle, OUTSIDE `sweep()` — so never mid-fixer/review/merge, which all run synchronously INSIDE it), rate-limited to once per `SELF_REFRESH_EVERY` sweeps (default 30), the poller `git fetch`es and the pure, selftested `refresh_decision()` (`NOFETCH`/`UPTODATE`/`DIRTY`/`DIVERGED`/`RELOAD`) decides. **The staleness baseline is the LAUNCH HEAD (#170) — `LAUNCH_HEAD`, captured ONCE at process start, before the loop — NEVER the clone's momentary HEAD:** the clone HEAD was a proxy for "the code this process runs" valid only while nothing but `poller-service.sh` ever pulled the clone — an invariant nothing enforced, and the FIRST live exercise (2026-07-13) broke it: an orchestrator pulled the live clone to origin minutes after the #168 merge, so every later check read clone==origin ⇒ `UPTODATE` while the process still executed the launch-time build — the running sweeper did NOT carry the HALT check it believed it deployed, and no future merge could ever trigger a reload (the feature silently self-disabled in exactly the condition it exists for). Only cleanliness/divergence are still read from the CLONE, fresh at every check, so a dirty clone that is LATER cleaned resumes reloading — no sticky state. It DETECTS only — never writes the clone: on `RELOAD` (a CLEAN clone whose `origin/main` strictly fast-forwards the launch head) it logs launched→new and EXITS `POLLER_RELOAD_RC` (default 90) so `poller-service.sh` ff-pulls + relaunches on the new code (the relaunched process captures its own fresh launch head); every other verdict (dirty/diverged/failed fetch/up-to-date) LEAVES THE POLLER RUNNING UNCHANGED (req 3 — a refresh that cannot happen never stops the loop). `--self-refresh-check` is a test seam (`POLLER_LAUNCH_HEAD` injects a stale launch head — a fresh seam process's own capture would equal the clone HEAD and hide exactly the defect). Covered by `poller-selfrefresh.test.sh`. **R9 FLEET HALT (#151):** the fleet stop switch (`bin/fleet-halt.sh`) is read ONCE at the TOP of every tick, BEFORE any model run, merge, retire close or comment; rc 0 alone is GO — any other outcome (maintainer HALT, unreadable-signal PAUSE, a dead checker) makes the whole tick OBSERVE-ONLY: routing decisions are still logged (the operator sees the queue) but nothing acts and NO state marker is written (a halted sweep can never park/dedup/no-progress-signature a PR). The poller does NOT exit — un-halt resumes action on the very next sweep. Covered by the HALTED row in `poller-fixer.test.sh` (mutation-checked: neutralize the halt flag and it fails). **HOST-REFRESH (#163) — the HOST half of self-refresh:** at the END of each sweep tick (`host_refresh_tick` — a safe point: all sweep work is synchronous and done — gated by THAT tick's R9 halt read, since filing a ticket is an ACTION), rate-limited to once per `HOST_REFRESH_EVERY` (30) sweeps, the poller runs `bin/host-refresh.sh --once` (see ITS row: a merged IMAGE-BAKED change files a `redeploy <workload>` ticket AFTER CI publishes; a merged control-repo change surfaces its host-apply question). FAIL-SAFE: a scan failure logs and never stops the loop — a missed redeploy degrades to the monthly workload-refresh timer. Covered by the wiring rows in `host-refresh.test.sh`. **DEV-LOOP LAUNCH (self-arm, 2026-07-19) — `dev_loop_launch_tick`:** at the END of a sweep tick (same discipline: R9-halt-gated — launching a service is an ACTION — rate-limited to once per `DEV_LOOP_LAUNCH_EVERY` sweeps, gated `DEV_LOOP_ENABLED` default-ON) the CLONE-side poller kick-starts the authoring loop (`dev-loop-service.sh`) so it self-arms on a running box with NO rebuild — closing the "box idle" gap where an IMAGE-baked entrypoint launch could never arm the loop on a busy multi-tenant box. IDEMPOTENT: it launches ONLY when no live loop already holds it (`dev-loop-service.sh --is-live` adjudicates the holder's liveness the #173 way), DETACHED via `setsid` so the loop OUTLIVES this poller (incl. its own self-refresh exit+relaunch). FAIL-SAFE: any failure is logged + swallowed (a missed launch degrades to the entrypoint's launch on the next rebuild). Covered by `poller-devloop-launch.test.sh`. **REBUILD-REQUEST tick (R17 approval flow, 2026-07-19) — `rebuild_request_tick`:** flag-fired ONE-SHOT filing of the rebuild-devbox APPROVAL ticket — anything in-box requests a purposeful rebuild by touching `REBUILD_REQUEST_FLAG` (a LOCAL write, not a bus write); this tick (END of sweep, R9-halt-gated, once per `REBUILD_REQUEST_EVERY` sweeps) runs `rebuild-request.sh file`, which captures a FRESH manifest + files the ticket AS the App (🔴 APPROVAL REQUIRED + `rebuild-approval` label + @mention → the maintainer's phone; ONE `approved`-label tap fires the host executor's v1.2.69 approval gate). The flag is CONSUMED on rc 0 (filed or already-open — file_ticket is idempotent) and KEPT on failure for a retry. Covered by the poller-wiring rows in `rebuild-request.test.sh`. **R16 OPERATING SCOPE (#167):** the sweep list DERIVES from `policy/scope.conf` (`bin/repo-scope.sh list` — never a hardcoded default: the #165 incident was a one-line edit of exactly that default), an explicit `POLLER_REPOS` can narrow but NOT expand (every repo is re-checked against the scope EVERY tick — out of scope ⇒ skipped with one loud line, no sweep/fixer/review/merge/retire), and `run_fixer` re-checks before cutting a worktree (belt). Covered by `repo-scope.test.sh` (mutation-checked: neutralize the sweep gate and the foreign repo gets swept). **LOCK LIVENESS (#173) — the flock singleton survives a box recreate:** the `--watch` lock (`$STATE/poller.lock`) lives on the HOME VOLUME, which OUTLIVES the process — a `claudebox-rebuild` can orphan a running poller (the box shares fedora-dev's PID namespace, so `distrobox rm` does not reap what it spawned) whose lingering process/FD keeps the flock held while sweeping NOTHING, and the bare `flock -n \|\| exit 0` then read that corpse as a healthy peer: observed live 2026-07-13, 08:27→12:23 — FOUR HOURS of `exited (rc=0) — restarting in 30s`, zero sweeps, zero signal (the singleton exists to prevent TWO pollers; it produced ZERO and reported success). Now the HOLDER records `pid boot-id starttime box-generation` in the lock file and a contender ADJUDICATES it (`lock_verdict` — pure + selftested): DEFER only to a POSITIVELY-confirmed live, same-generation holder (same kernel boot + pid exists + starttime matches within a ±2-tick tolerance — /proc starttime flutters ±1 tick between reads of the SAME process, so an exact match would misjudge a live holder as recycled; a truly recycled pid differs by seconds-to-months); EVERYTHING else — no/garbled record (the bare-flock era wrote none), dead pid, recycled pid, previous kernel boot, previous BOX GENERATION (inode.mtime of the claudebox `.assembled` marker, `touch`ed by every assemble — the orphan-of-a-torn-down-box case, THE incident class) — is a TAKEOVER: the lock file is ROTATED (unlink + re-flock a FRESH inode, so a lingering FD gates nothing; an inode identity check settles a rotation race) and ONLY a provably-live previous-generation orphan is first sent SIGTERM (it traps TERM and exits cleanly; a recycled pid is an innocent stranger and is never signalled). FAIL DIRECTION: liveness in doubt ⇒ START (a brief double-sweep is idempotent + sha-bound + gate-checked; a silently dead poller is unrecoverable). A DEFER is never silent and NEVER rc=0: it exits `POLLER_DEFER_RC` (91 — shared contract with poller-service.sh, distinct from the reload rc 90) logging the holder adjudication, and `LOCK_DEFER_MAX` (10 ≈ 5 min at the 30 s restart cadence) CONSECUTIVE deferrals (mtime-windowed by `LOCK_DEFER_WINDOW` so a stray defer from hours ago never pre-charges the streak — the "consecutive" the question claims stays true) surface ONE `gh issue create` question naming the holder + remediation (marker-gated, re-attempted until posted; count + marker reset on any successful acquisition). Covered by `poller-lock.test.sh` (mutation-checked: restore the bare flock and the dead-holder row fails). **IDLE-WITH-WORK-PENDING (audit 2026-07-18 CAT-42/01 — the kd#23 six-hour silent stall):** the poller's PROCESS liveness is watched (the apparatus-deadman: is it sweeping?) but its WORK progress was NOT — a poller NOOPing on `host=NONE` every 10s reads "healthy" while the workstream is dead. `stall_verdict` (pure + selftested) is the missing WORK-level clock: a `live-validate`-LABELLED open head that has sat at host=NONE (no host verdict produced) past `POLLER_STALL_MAX` (30 m; the gate normally verdicts in ~10 m) — age derived from the head commit's committer date (GitHub truth, never a restart-resettable local first-seen marker; R24 no-proxy) — SURFACES a deduped `[stalled]` question naming the likely cause (a stale host per-`(repo,sha)` `.done` marker, or a transient PR-head fetch-failure deduped as delivered — CAT-01/CAT-04) + the remediation, instead of NOOPing forever. One `gh api commits/<sha>` per stuck head until surfaced, then the surface marker suppresses it; an UNLABELLED host=NONE head is no longer a quiet NOOP — it takes the R39/#278 enrolment self-heal below, because unlabelled does not mean "the gate has not got to it yet", it means the gate will never look. Covered by `poller-stall.test.sh` (mutation-checked: neutralize the clock and the aged head surfaces nothing). **REBASE OWNERSHIP (audit 2026-07-18 CAT-17):** a GREEN+PASS PR merely BEHIND main no longer parks TERMINALLY (which stranded the backlog O(N²) as siblings merged) — auto-merge's **rc 2** arm now OWNS the mechanical rebase via `gh pr update-branch` (a server-side merge of main into the PR branch — NO local clone touched, unlike the fixer): a CLEAN behind → a new head that re-gates (PROGRESS, not parked); only a genuine CONFLICT (update-branch fails) or the bounded `rebase_due` limit (`POLLER_REBASE_MAX`=6, counter cleared on a successful merge) surfaces `[rebase]` for a human. Covered by `poller-rebase.test.sh` (mutation-checked: force the bound to GIVEUP and a clean-behind PR parks terminally). **ANOMALY → BOUNDED SELF-REPAIR (R39/#278) — the unanticipated tail no longer defaults to the maintainer:** enumerating every way the world can surprise a machine does not converge, so "I have no rule for this" used to mean *log it, surface it, wait for a human* — while `run_fixer`, which summons a model to diagnose and repair, was reachable from only TWO anticipated states (host RED, fitness RETURN). `anomaly_route <kind> <attempts> <max>` inverts the default to **REPAIR**, and `surface_or_repair` is the call-site wrapper: bounded self-repair FIRST (`POLLER_REPAIR_MAX`=3 per (pr,kind), cause=`ANOMALY` so the fixer prompt says *the pipeline itself is stuck*, not *a gate failed*), the human only when repair is **INFRA** (inapplicable) or **ESCALATE** (spent). WIRED at the three sweep-level stuck states: `stalled` (a head the host gate will never re-verdict — its own remediation names an act the fixer can perform), the `cannot tell which gate failed` blocked-state (the purest unanticipated state: plan() routed FIX but no gate reason is readable — the poller still refuses to INVENT one and hands the model the TRUE finding), and the rc-2 `rebase` CONFLICT (resolving a conflict on the PR's own branch is ordinary dev work). **A REPAIR IS NEVER PARKED** (`SOR_ROUTE` gates the acted-marker write): a repair mints a NEW head that re-gates, so parking it would strand the PR the repair just unstuck — only a surfaced outcome parks, and then still only on a successful POST. **DELIBERATELY NOT WIRED** (a boundary, recorded in-code beside the function, because "nobody wired it up" is exactly how the first cut shipped): everything inside `run_fixer`/`fix_in_tree` (`surface_or_repair` CALLS `run_fixer`, so wiring those re-enters the fixer from within itself — and each is the fixer reporting the fixer could not run); the `review-*` family (the fitness gate's own machinery); `refused` (the merge-trust boundary — a model must never be summoned to make a trust refusal go away); `merge-failed` rc 3 (not stuck — already self-healing by retry); and `review`/PRESENT (a fitness ESCALATE is the reviewer deferring to the maintainer BY DESIGN). The INFRA list is matched **by PATTERN, not exact name** — an exact-match list is the wrong shape for a default-REPAIR router, since every kind it fails to recognise falls through to the model: the live `review-infra-down` kind (a GLOBAL `claude -p` outage) missed a bare `infra` and would have answered "the model is unreachable" by summoning the model, once per PR. Repair also degrades to `surface()` when it cannot be ISOLATED (no local clone ⇒ `run_fixer`'s own "check the repo clone" refusal would REPLACE the anomaly's diagnosis, telling the maintainer nothing about why the PR is stuck) and charges no budget for an attempt that could never happen. **UNENROLLED-PR SELF-HEAL — the same inversion at the pipeline's front door:** the host live-gate discovers work ORG-WIDE by the `live-validate` label and by NOTHING else, so a PR the loop authored but never labelled can never be verdicted — host stays NONE, `plan()` stays NOOP, and the sweep passes it in silence for as long as it stays open (the maintainer-facing agent forgot the label on THREE PRs in one session — #271, #277, bootstrap#283 — which is precisely why enrolment must not depend on anyone remembering). `enroll_pr` adds the label ITSELF, deterministically and never via a model run: the no-offload doctrine cuts both ways, and `run_fixer` commits code in a worktree — it could not add a label if asked. **CREATE-ON-USE:** `gh pr edit --add-label` HARD-FAILS on a label the repo does not carry, so a failed add is retried ONCE behind a `gh label create` (the `dev-plan.sh`/`host-ticket.sh` precedent) — that exact miss on a brand-new repo is one of the seven stalls behind #278, and it cost 12 SILENT hours. **ONE SHOT PER PR** (`enrolled-<pr>.done`) — a boundary, not a bound: we heal an OMISSION, we do not fight a DECISION, so a label removed AFTER we enrolled it is somebody's choice and is never re-added (a wiped box re-enrols once, harmlessly). Scoped to `$DEV_LOGIN`-authored NON-DRAFT PRs (never a foreign contributor's; never mid-authoring, since dev-author's own R3 flow is draft → ready → label). **Failure is BOUNDED, NOT SILENT (R4):** consecutive failures are counted and at `POLLER_ENROLL_MAX` (3) the state goes to `surface_or_repair` under the kind `enroll-infra`, which the existing `*infra*` family routes STRAIGHT to the human — correctly, because a missing label-write is a credential fact no code change repairs (that naming is load-bearing, so `--selftest` pins it: drop the `-infra` suffix and the poller answers "I cannot label" by summoning a model that cannot label either). The `author`+`isDraft` it needs ride the SAME batched list call at zero extra cost, placed BEFORE `labels`: `IFS=$'\t' read` collapses runs of tabs, so the frequently-EMPTY `labels` must stay LAST or it slides every later field one slot left (the hazard `bin/dev-loop.sh` already records). Covered end-to-end by `poller-anomaly-repair.test.sh` (the wiring is the subject — a pure-core selftest cannot see an uncalled function); routing rows in `--selftest`. |
| bin/poller-service.sh | supervises `bin/pr-poller.sh --watch` as a headless service. OPT-IN via `POLLER_ENABLED=1` (Quadlet/run.sh env), launched by `entrypoint.sh` INSIDE the claudebox (the poller's REVIEW/FIX steps spawn `claude`, absent from the base) as PLAIN shell — no Claude Code → no gate/classifier → the sanctioned deterministic auto-merge path can execute (the interactive agent is deliberately gated FROM merging). Waits for `.assembled` + the standing GitHub credential, then runs the watch loop with restart-on-death. ARMED by default (gate-free objective — no human approves the shipment; the merge-trust boundary is the two distinct App-identity gates, not a human arm); `POLLER_ARMED=0` is a deliberate dry-run soak (the #96 explicit Tier-A arm is retired, pre-ZERO-GATE). **SELF-REFRESH (#162) — the supervisor deploys the poller's OWN merged code, no human pull + bounce:** when `pr-poller.sh` detects (at a safe point) that `origin/main` has fast-forwarded past the code it runs, it EXITS `POLLER_RELOAD_RC` (default 90, a shared contract with the poller); THIS supervisor's `self_refresh_pull` — the ONLY writer of the clone (req 4) — then `git fetch` + `git merge --ff-only`s the live clone (fast-forward ONLY; a dirty/diverged clone is REFUSED and left untouched, logged, then the current code is relaunched — fail-safe toward progress) and relaunches `--watch` on the new code with NO backoff. The flock singleton survives because the old poller fully exited (freeing its lock) before the new one starts. The supervisor is NOT re-exec'd — a `poller-service.sh` change still needs a box bounce; the poller + every `bin/*.sh` it invokes (the bulk of the machinery) reload here. `--self-refresh-pull` is a test seam. Covered by `poller-selfrefresh.test.sh`. **LOCK DEFER (#173):** a poller exit of `POLLER_DEFER_RC` (91, shared default) is named for what it IS — "DEFERRED to a live lock holder, the singleton did NOT start", pointing at poller.log's adjudication — never mistaken for a healthy restart (the 2026-07-13 incident read `exited (rc=0) — restarting in 30s` for four hours while zero pollers swept; the poller now adjudicates the holder and only ever defers, non-zero and loudly, to one it POSITIVELY confirmed live — see the `bin/pr-poller.sh` row's LOCK LIVENESS). |
| bin/auto-merge.sh | the DETERMINISTIC merge executor: re-checks the TWO INDEPENDENT gates fail-closed from GitHub (host GREEN by `$LG_HOST_LOGIN`, fitness PASS by `$FITNESS_LOGIN` ≠ PR author) and only then merges; any missing/unverifiable gate ⇒ REFUSE. `--dry-run` prints the decision. **ZERO-GATE (2026-07-10):** tier is NO LONGER a gate — `decide()` merges on GREEN+PASS for ANY tier (control-plane included); the old `A → HUMAN` click is removed (recoverability rests on the host rollback + git revert + fitness's standing preserve-recoverability rule, not a click). **STEP 5 of #274 (2026-07-29): the tier is no longer COMPUTED either** — it survived only to fill the report line, so the classifier script and both call sites are deleted; `decide()` keeps an ignored first parameter so `--decide A GREEN PASS` still prints `MERGE`, the standing executable proof that the tier gated nothing. HARDENED per #96: **G1** — the two anchors must be DISTINCT logins (equal ⇒ both gates disabled ⇒ REFUSE); **G2** — verdict SELECTION and EXTRACTION both read ONLY line 1 (the machine-owned header) of each anchor comment, bound to the **FULL 40-hex head sha** (embedded candidate logs / reviewer rationale can neither forge a verdict nor plant a future-head anchor; a 7-hex prefix would be grindable); an unreadable PR author is a hard REFUSE (the ≠-author guards must never compare against empty); same-identity fitness (`FITNESS_SAME_IDENTITY=1`) is **DRY-RUN-ONLY** — `--commit` under it is a hard REFUSE, so arming mechanically requires the real fitness App. `--match-head-commit` pin retained. LOCKSTEP: the host verdict header must carry the full sha (fedora-bootstrap change); until it does, the host gate reads NONE ⇒ REFUSE (fail-closed). **R16 (#167):** an out-of-scope repo is a REFUSE before any gate is even read (see the `bin/repo-scope.sh` row). **EXIT-CODE CONTRACT (2026-07-17):** 0 = merged · **1 = a gate REFUSE — the ONLY rc that means the merge-TRUST boundary disagrees** (misconfigured anchors, same-identity armed, out-of-scope, a gate its stricter parse rejects) · **2 = MERGE_CONFLICT** (all gates GREEN+PASS but the `gh pr merge` command failed because the PR is behind main / conflicting — a REBASE, not a refusal; classified by `gh pr view --json mergeable,mergeStateStatus`) · **3 = other merge-command failure** (transient / head moved). Splitting 2/3 off from 1 stops the poller crying "trust boundary broke" at a routine serialized rebase (alarm-fatigue on the one signal class that must stay meaningful). Covered by `merge-conflict-signal.test.sh`. **Control-plane (the merge boundary's executor).** |
| merge-conflict-signal.test.sh | proves the merge core does not mislabel a benign serialized merge-CONFLICT as a merge-trust REFUSAL. Drives the REAL `bin/auto-merge.sh --commit` through the post-gate merge path (all three gates GREEN+PASS+in-scope via a stub gh; the `gh pr merge` command + mergeability controlled) and asserts the EXIT-CODE CONTRACT (0 merged / 1 gate-refuse / 2 conflict / 3 other) + an in-suite MUTATION (restore the old blanket `exit 1` on any merge-command failure → a CONFLICT wrongly exits 1, which the poller surfaces as the trust-boundary alarm) + a `pr-poller.sh` routing drift-guard (rc 2 → a `rebase` surface; rc 3 → a `merge-failed` surface whose arm writes NO acted marker — parking on a transient strands a GREEN PR, the #156 class, so the row fails against any rc-3 arm carrying a `$done` write, the pre-fix code included; rc 1/other → the `refused` surface; `PIPESTATUS` capture). **TIER-RETIREMENT rows (STEP 5 of #274):** `decide()` returns MERGE for EVERY tier value (A/B/C/empty/garbage) on GREEN+PASS — the executable form of "the tier gated nothing", and the issue's own acceptance generalized from one sample to all — while a RED host / a non-PASS fitness still REFUSEs on any tier, so the removal is proven to have loosened no gate. Those two rows pass against the PRE-removal script BY DESIGN (a reporting-field removal must not change what may be merged, so they are the CONTROL); the rows that BITE are the changed-file call-site COUNTS — 2 in `auto-merge.sh` (Trinity guard + the host=SKIPPED re-check) and 1 in `pr-poller.sh` (the SKIPPED gate-relevance read), down from 3 and 2 — plus a bin/-wide reference scan and the deleted-file check. Verified: 4 of 18 rows FAIL against the pre-removal tree. `bash merge-conflict-signal.test.sh` → exit 0. No GitHub/network/model |
| bin/fleet-halt.sh | the R9 FLEET HALT reader (#151; spec #135 R9 *"a pinned HALT control freezes every poller within one sweep"* + P3's *"HALT check in every sweep"*) — the fleet-wide, maintainer-thrown SOFT STOP every sweeper consults at the TOP of every sweep, BEFORE any model run is spawned or merge taken. THE SIGNAL lives on GitHub, read fresh every sweep (spec #135's no-local-state law): the `halt` label on the FLEET HALT CONTROL issue in the control repo (`oso-gato/fedora-bootstrap` — live: #128), DISCOVERED BY TITLE (strict prefix match on `HALT_TITLE`; no hardcoded number; ALL matches read, any HALT wins — a decoy cannot mask a halted control; ABSENT is a definite "no halt asserted" ⇒ RUN, so a tidied-away issue never freezes the fleet). MAINTAINER-BOUND BOTH DIRECTIONS (dev-plan's applier-bound `approved` discipline, reused not reinvented): presence proves nothing — the state derives from the label's own TIMELINE events, walked newest-first, each actor role-checked (admin\|maintain) via the permission API; the first MAINTAINER event decides (`labeled` ⇒ HALT, `unlabeled` ⇒ clear) and a non-maintainer event is INERT both ways (logged loudly), so an App can neither halt NOR un-halt the loop (empirically pinned: the permission endpoint answers 200 + `role_name:""` for `<app>[bot]` actors, so App noise resolves definitively). FAIL DIRECTION — **INVERTED 2026-07-28 (STEP 3 of #274): an unreadable signal is NOT a halt.** An UNREADABLE signal (failed discovery/timeline read, or an UNRESOLVABLE walked actor role) ⇒ **RUN**, logged, and logged LOUDLY past `HALT_FAILS_MAX` (3) CONSECUTIVE unreadable reads; it NEVER escalates to HALT, and a clean read resets the streak (which now governs only how loudly the read logs, never whether work proceeds). The old fail-closed-toward-stopping direction was measured: **935 halts, ZERO maintainer-thrown** — 100% "the API returned garbage" — suppressing 338 concrete actions, one of which BLOCKED THE TICKET THAT WOULD HAVE REPAIRED A SIX-DAY OUTAGE. The reasoning it retires ("a gate that cannot be read cannot be trusted to say go") mistook this label for a safety gate: merge safety is the two INDEPENDENT gates, and R9's hard stop is App-key revocation, so failing this label open weakens nothing about what may be merged. What still HALTS is unchanged: a label READ, PRESENT, and applied by a MAINTAINER. CONTRACT: stdout = one line, first word RUN\|HALT; rc 0 = RUN, 10 = HALT — **rc 20/PAUSE is RETIRED**. Callers still treat EVERY non-zero rc (127 included) as "no new action", so a MISSING or CRASHED checker remains fail-closed BY CONSTRUCTION (the checker itself decides; a checker that cannot run at all stops the sweep). HALT stops NEW action only (in-flight model runs/merges complete; the hard kill is App-key revocation, per R9); un-halt resumes within one sweep, no restarts. Callers here: `bin/pr-poller.sh` (every tick) + `bin/dev-loop.sh` (every pass); the host's `live-gate-watch.sh` is the fedora-bootstrap half of #151 — this file is CANONICAL and meant to be mirrored there (the `gh-app-provision.sh` precedent). Cost: 2 REST calls/check until the label is first touched. `--selftest` covers the pure core (`role_can_halt`/`halt_fold`/`unreadable_action` — which has, deliberately, NO input that returns HALT); `fleet-halt.test.sh` drives the real read paths. **Control-plane (the fleet stop switch).** |
| bin/repo-scope.sh | the R16 OPERATING-SCOPE reader (#167). **R16 AMENDMENT (2026-07-16):** the maintainer-confirmed AUTHORITY is now the **confirmed-objective repo-list** (`00-OBJECTIVES.md`'s "Repositories this objective operates on" table, confirmed ONCE per R1), NOT `policy/scope.conf`. A per-session scope is the session's **transcription** of that list into the R27 registry, **git-VERIFIED at read time** against the objective doc at the session's **backing ref** (`session_scope_verified`: line-3 cache must set-equal the objective list at the line-4 `<repo> <path> <sha>`; else SESSION_UNBACKED, fail-closed — a hand-forged `.session` can never widen scope). New verbs: `objective-adds` (the fitness DOC-boundary detector, port of `diff-adds` over the objective table), `transcribe --backing '<repo> <path> <sha>' <sid>` (derive repos from the objective + register — the agent transcribes, never authorizes), `union`/`owner` (the shared-service actionable set, for the deferred poller cutover). `policy/scope.conf` is RETIRED as the enrollment authority — it survives ONLY as the TRANSITIONAL CEILING the SCOPE_SESSION-unset poller reads until the STEP-10 cutover (a NOTE in 00-DESIGN.md). The historical `scope.conf`-based behavior below still describes the transitional ceiling + the fail directions. — the apparatus acts on the maintainer-confirmed repo set (config-as-code, never a hardcoded script default; the 2026-07-13 incident: #165, a one-line PR, enrolled a repo the maintainer had scoped away and NO layer held a scope to stop it — the poller then swept the foreign repo, pushed a bot commit onto its branch and merged its PR, all "working as built"). CONTRACT: `check <repo>` rc 0 = in scope, the ONLY go — callers treat EVERY non-zero rc (a missing reader's 127 included) as "no action" (the `fleet-halt.sh` rc discipline); `list` = the actionable set (the poller's sweep default derives from it); `diff-adds` = the repo names a unified diff NET-adds to the scope config (added minus removed — a moved/reordered line or a pure removal is never an expansion; path-scoped to the `+++ b/` target, so the same name added to any other file never trips; added lines are held to the same grammar the runtime parser enforces; HUNK-STATEFUL — content lines always ride a `+`/`-`/space prefix, so an added line whose text is `++ b/README.md` arrives byte-identical to a file header: headers are honored only OUTSIDE a hunk and the file state resets only on a column-0 `diff --git`, which no content line can occupy, so a crafted line can neither escape the scope file mid-hunk nor forge an entry into it) — the detector behind `fitness-review.sh`'s deterministic R16 RETURN; `confirm-names <line1>` = the repo names a maintainer's line-1 `CONFIRMED` covers (STRICT, all-or-nothing: `CONFIRMED <repo> [<repo>…]` and nothing else on line 1; bare/prose confirms NOTHING) — the fitness gate's name binding. FAIL DIRECTION: unreadable config ⇒ only the apparatus's OWN two repos (fedora-dev + fedora-bootstrap) act — the loop can still fix/ship itself and reach its control issue, but can never wander; readable-but-EMPTY denies EVERYTHING (an emptied config is the maintainer narrowing, and narrowing is always his right); INVALID config lines are IGNORED (they can narrow the effective scope, never widen it). Callers (R16 rule 4 — every actuator): `pr-poller.sh` (per-repo sweep gate every tick + a `run_fixer` belt before any worktree is cut), `fitness-review.sh` (repo check + the diff-adds gate), `auto-merge.sh` (REFUSE before any gate is read), `dev-plan.sh`/`dev-loop.sh`/`dev-author.sh` (refuse before anything is read or posted), `host-ticket.sh` (target control repo, before filing), `host-refresh.sh` (each scanned workload + the control repo). CANONICAL here; the host live-gate's ORG-WIDE `live-validate` discovery is the same leak from the other end (R16 rule 5) — this file + the control repo's copy of `scope.conf` are meant to be MIRRORED into fedora-bootstrap (the `fleet-halt.sh`/`gh-app-provision.sh` precedent, keep in lockstep); until that lands the host half stays org-wide, a DISCLOSED residual, not a silent one. Zero API calls — a local file read, safe at the 10 s sweep cadence. `--selftest` covers the pure core (`scope_norm`/`scope_parse`/`scope_member`/`scope_decide`/`scope_diff_adds`/`scope_confirm_names`); `repo-scope.test.sh` drives the real read + actuator paths. **Control-plane (the scope boundary).** |
| bin/intake-file.sh | the INTAKE FILING CLERK (R31, STEP 2 of #274) — the front door the apparatus was missing: ~11,000 lines of delivery machinery began at `dev-plan.sh`, which refuses anything but a hand-written spec carrying a hand-applied approval, so the non-programmer maintainer had no way in except to write specs himself. The DIVISION OF LABOUR is the point: the AGENT interviews him and DRAFTS the spec INCLUDING its acceptance criteria (judging a criterion is easy for a non-programmer; authoring one is a skill he does not have), THIS SCRIPT is the deterministic half — a checklist with an exit code, no model, no judgement — and the MAINTAINER confirms with one tap. `--check <spec.md>` validates (rc 1 + the FIRST missing thing, so the agent gets one instruction, not a list); `--file … --repo <r>` validates then files. THE LOAD-BEARING VALIDATION IS THE ACCEPTANCE COMMAND: without a runnable check "looks done" is the only completion signal and a human becomes the verification loop. Also refuses unresolved `[NEEDS CLARIFICATION]` markers, no title, no scope, no OUT-OF-SCOPE bound; both counters emit `unreadable` (never 0) when they cannot count, so the pure core's refusal rows are reachable from the I/O layer too. **THE LABEL IS THE GATE (fitness RETURN on d339476):** it files under `$INTAKE_LABEL` (`objective`, CREATE-ON-USE like `dev-plan.sh`/`host-ticket.sh`, since `gh issue create --label` hard-fails on an unknown label) and NEVER under `backlog` — `backlog` is swept straight into `dev-author` → PR → auto-merge, so filing an unconfirmed objective there routes past R1 entirely and the maintainer's `approved` tap is read by nothing. The issue is filed ASSIGNED to him and says plainly that nothing acts on it until he taps. **R16 SCOPE BELT — WIRED, at the CEILING layer (fitness RETURN on 8ffd4d4):** this row previously recorded the belt as a deferral because "with no `policy/scope.conf` the reader fails closed to the apparatus's own two repos, which would refuse the front door for exactly the tenant repos it serves." That was FALSE and is corrected here: `scope.conf` is RETIRED and unread — `bin/repo-scope.sh` enumerates the App INSTALLATION, so tenant repos are ALLOWED (measured: `check fedora-desktop` → rc 0; an uninstalled repo → rc 3). The belt now runs BEFORE the title/body are composed, so an out-of-scope repo creates nothing and exits a DISTINCT **rc 4** (the spec is fine; the destination is not). WHICH LAYER is the deliberate part: `repo-scope.sh` narrows to a session's ALREADY-declared objective whenever `$SCOPE_SESSION` is set, and an undeclared session is denied every repo (SESSION_UNDECLARED, rc 3) — so copying `dev-plan.sh`'s CLAUDE_SESSION_ID-derivation block would have broken the front door for real, since a NEW objective is by definition not in the current session's scope. The filer therefore pins `SCOPE_SESSION` EMPTY for that one call and checks the App-install ceiling alone; narrowing intake to a session's own scope once every session is objective-backed is a named follow-up, not a blocker. `--selftest` covers the pure core + the counters; covered end-to-end by `intake-file.test.sh` (both directions of the belt, the pinned-empty session layer, a missing reader, and a mutation that neutralizes the belt and files the out-of-scope objective) |
| skills/intake/SKILL.md | the INTAKE SKILL — HOW the conversation runs (the filer is only its exit code). Governing rule: **draft the acceptance criteria FOR him, show them, ask him to confirm or correct** — never ask "what are the acceptance criteria?", which asks him to do the one thing he cannot. Also: TRIAGE FIRST in thirty seconds (a production incident, security/credentials work, genuinely open-ended "what should we even do", and anything whose success cannot be checked by RUNNING something must not enter this pipe); READ BEFORE YOU ASK (every question answerable from the repo is a tax on his attention); "what is deliberately NOT in this?" named the single most valuable question; ask "how would you know it worked?" in HIS terms and translate to a command yourself; unresolved questions become `[NEEDS CLARIFICATION]`, which the filer refuses, so an unfinished interview cannot be filed by accident; and STOP after filing — implementing it in the conversation leaves the work in a transcript with no state row to pick up. Installed into `~/.claude/skills` from the live clone by `bin/claude` on every launch (idempotent, fail-safe, never pruned — a disclosed one-directional install) |
| .github/ISSUE_TEMPLATE/agent-task.yml | the HAND-FILING fallback for the same front door: the fields the loop cannot proceed without (objective, scope, OUT-OF-SCOPE, the acceptance command) are REQUIRED so a by-hand ticket cannot skip them. Labels the issue `objective`, not `backlog` — the same gate the filer holds |
| intake-file.test.sh | drives the REAL `bin/intake-file.sh` against a stub `gh` and asserts WHICH LABEL IT FILES UNDER — the axis `--selftest` is structurally blind to (the validator was correct; the defect was one word in a `gh issue create` argument). Rows: a complete spec files as `objective`, assigned, never `backlog` / the label is CREATED BEFORE the issue that needs it / the filer's label IS the one `bin/dev-loop.sh`'s plan arm queries and is NOT the one it authors from (one contract, read out of both files) / the issue form carries the same label / an incomplete spec creates NOTHING / a failed create exits non-zero / the body no longer claims the loop takes over at filing. MUTATION RUN IN-SUITE: restore `INTAKE_LABEL=backlog` and the filer routes past the maintainer again (the sed must genuinely change the copy, else vacuous). `bash intake-file.test.sh` → exit 0. No GitHub/network/model |
| bin/dev-author.sh | the AUTONOMOUS FEATURE-AUTHOR (apparatus spec #135 R3 / work-plan P3) — the keystone of the dev engine and the symmetric partner of the poller's RED-fixer (the poller FIXES an open PR; this AUTHORS a new one). `dev-author.sh <repo> <issue#>` turns a backlog feature issue into a validated, live-gated, auto-merging PR with NO human in the loop: guard (open + carries the `backlog` label + not-already-authored + no existing PR — all fail-closed, so it only picks up the planner's actionable feature tickets) → ISOLATE via `fresh-tree.sh` (a fresh worktree off current origin/main, never the shared clone) → ONE bounded `claude -p` implements + commits in that tree (NEVER pushes/merges/touches main — the harness owns git; promptlessness is 100% ambient managed-settings, no permission flags; bounded by `AUTHOR_TIMEOUT`; **its prompt rides STDIN, never argv** — it embeds an unbounded ISSUE BODY, and a single argv argument dies at `MAX_ARG_STRLEN` (131072 bytes) with an `E2BIG` that stops the model EXEC'ing at all (#155), which also subsumes the old `</dev/null`: the model's stdin IS the prompt pipe, so the caller's backlog is unreachable by construction, not by hygiene; and the `cd` into that worktree is a FAIL-CLOSED GUARD — the body is BRACE-GROUPED to it, because `cd "$WT" && set +o pipefail; <pipeline>` binds the `&&` to `set` ALONE and would run the author even when the `cd` FAILED, in the CALLER'S cwd — under `dev-loop`, the SHARED CLONE — told to implement and `git commit`, which is exactly the 2026-06-28 cross-branch-leak incident. Reachable here, unlike in the poller: nothing else reads the tree before the model runs. An unenterable worktree therefore runs NO model and surfaces a question, rather than being mistaken downstream for an author that ran and committed nothing) → in-box `validate.sh` gate (build+assembly+lint) BEFORE spending a host build → push + open a DRAFT PR at first push (R3) → mark ready + label `live-validate`, handing off to the host live-gate → fitness → poller pipeline (writes no merge logic itself). The PR carries a NON-closing `Backlog-ticket: #N` linkage (NOT `Closes #N`), so the issue is closed by `bin/reconcile.sh` on OBSERVED proof (merge + host GREEN + CI published + live read-back), never auto-closed at merge before the change is proven live (task #19). Then confirm the ship with ONE best-effort `dev-author → shipped:` comment on the backlog issue (R5 audit loop — the ticket shows its own outcome, symmetric with the BLOCKED question). BLOCKED/no-progress/in-box-RED each surface a dev-task QUESTION on the ISSUE (never an approval request) and open no PR (R13 bounded autonomy). **#177 READS THE DISCUSSION:** the author prompt embeds not just the issue BODY but the issue's COMMENT STREAM — `dev-loop` un-parks a blocked issue on a maintainer REPLY, and an author that re-ran on the body alone would repeat the same failure on UNCHANGED information; the reply (and the prior BLOCKED question's context) now reaches the model so a retry INCORPORATES the feedback that un-parked it (the prompt rides stdin, so an unbounded thread cannot break the EXEC — #155). **#182 COMMIT-RESCUE:** a bounded run that IMPLEMENTS the feature but exits (or hits the timeout) BEFORE its own `git commit` leaves a DIRTY worktree with `HEAD==base_sha` — the harness RESCUE-commits that dirty tree (the work IS the deliverable) and runs it through the SAME in-box validate gate rather than discarding it as "no-progress" (a partial/broken rescue RED-surfaces; the host live-gate + fitness still gate downstream); only a truly CLEAN worktree is the genuine no-progress surface. `FRESH_TREE`/`VALIDATE`/`AUTHOR_CLAUDE` are overridable so `dev-author.test.sh` exercises the REAL control flow with stubs. `--selftest` covers the pure helpers (branch_for/extract_sentinel/should_author). State lives only in GitHub + a per-(repo,issue) idempotent-replay marker (R5). |
| bin/dev-plan.sh | the AUTONOMOUS PLANNER (apparatus spec #135 R2) — the front of the dev loop. `dev-plan.sh <repo> <spec-issue#>` turns a HUMAN-CONFIRMED objective/spec issue into `backlog`-labelled FEATURE issues, one per feature. R1 discipline: refuses a spec that is not MAINTAINER-confirmed (fail-closed — the confirmed set is what fitness grades against, and in a zero-gate fleet this is the loop's ONLY human anchor, so BOTH confirmation paths are maintainer-bound exactly like the auto-merge G2 anchors): the `approved` label acts only if a maintainer APPLIED it (the applier is resolved from the `labeled` timeline event and role-checked — mere presence authorizes nothing, since every fleet App identity holds the write/triage needed to add a label), and a `CONFIRMED` comment acts only if the token OPENS line 1 AND its author holds admin|maintain. Unfetchable label/timeline/role ⇒ unconfirmed. Determinism split (design law): the JUDGMENT ("what features") is ONE bounded `claude -p` that WRITES each feature to a file and creates NO issues; the ACTION (filing the `backlog` issues) is the plain-shell harness reading those files → auditable, model touches no GitHub state. BLOCKED / no-`PLAN_DONE`-sentinel / EVERY-create-failed each surface a dev-task question on the spec issue (never an approval) and file nothing; a plan-summary comment links the filed issues (R5). Creates the `backlog` label on use (the `host-ticket.sh` precedent — dev-plan is repo-agnostic and the label need not pre-exist). **NO LOCAL STATE — the planner keeps none** (spec #135's design law: *no local state anywhere; every component resumes by re-reading GitHub*; R5; R14's E2E-KILL), so both of its durable facts are DERIVED from the spec issue's own comment stream, identity- + line-1-bound (the auto-merge G2 discipline): **already-planned** ⟺ this box's `**dev-plan → planned:**` summary is anywhere in the thread — that comment IS the idempotency tombstone (it cannot lean on the title dedup instead: an exact-title match against a NON-DETERMINISTIC model would let a wiped box re-plan, re-word half the features and file a near-duplicate backlog that dev-loop then authors into duplicate PRs), fetched with a PAGINATED read and fail-CLOSED (an unreadable stream cannot prove the spec unplanned ⇒ refuse); **already-asked** ⟺ the question is the NEWEST comment, so a re-run stays quiet and a reply un-mutes it. Everything INCOMPLETE therefore withholds the summary and stays re-plannable — a PARTIAL create failure DEFERS (title-dedup re-files only what is missing on retry) and `MAX_FEATURES` is a **cap that DEFERS, never drops**: the surplus features are reported ON THE BUS BY TITLE (their `$OUTDIR` is a mktemp deleted on exit, so this is the only thing standing between a maintainer's confirmed objective and silently deleted work) and the spec is left unplanned, so raising the cap and re-running files the rest. Because EVERY deferred exit recovers by RE-PLANNING with some features already filed — and the harness's only dedup is an EXACT-title match — the planner is HANDED the already-filed titles and told to reuse them verbatim (a planner blind to the bus re-words what it filed and the dedup catches nothing). That is an instruction to a non-deterministic model, not a guarantee, so the cap comment says the residual out loud: a re-run may still file a near-duplicate if it re-words one. Its prompt rides STDIN, never argv (#155 — it carries the spec issue's body plus the already-filed titles, so as an argv argument it would eventually fail to EXEC with `E2BIG` past `MAX_ARG_STRLEN`; stdin has no ceiling and subsumes the old `</dev/null`), and the `cd` into `$OUTDIR` is a FAIL-CLOSED guard with the body BRACE-GROUPED to it — `cd … && set +o pipefail; <pipeline>` binds the `&&` to `set` ALONE, so a failed `cd` would run a file-WRITING planner in the caller's cwd (a git clone) and then blame the empty result on a timeout. `PLAN_CLAUDE` overridable for the mock test; `--selftest` covers the pure helpers |
| bin/dev-loop.sh | the DEV-LOOP DRIVER (apparatus spec #135 work-plan P3) — closes the human out of the per-feature loop. `dev-loop.sh <repo>` (ONE pass, then exit — the only mode) enumerates the OPEN `backlog`-labelled issues the planner produced and runs `bin/dev-author.sh` over each; the author + host-live-gate → fitness → poller pipeline do the build/validate/merge, so the driver writes NO such logic — it only SEQUENCES the author across the backlog (it is to dev-author what poller-service is to pr-poller). Deterministic + idempotent: dev-author's own marker/PR guards make a re-run a no-op for anything authored, so it is timer-safe; a BLOCKED/non-zero author is logged and the loop CONTINUES (one stuck feature never wedges the rest). `MAX_PER_PASS` caps the AUTHOR RUNS SPAWNED per pass, and does NOT truncate the enumeration — dev-author leaves an authored issue open + still `backlog`-labelled until its PR merges, so truncating the sorted backlog would spend the whole cap re-skipping in-flight issues and STARVE the tail forever; an in-flight skip (classified from dev-author's own (rc, stdout) contract, never a duplicated guard) costs no slot. An issue whose author run surfaced a dev-task question is PARKED — so a stuck ticket never re-spends a bounded model run, never re-asks into noise, and never holds a slot. **The driver keeps NO LOCAL STATE** (spec #135's design law — *no local state anywhere; every component resumes by re-reading GitHub*; R5; R14's E2E-KILL): park state is DERIVED from the issue's own comment stream — PARKED ⟺ the NEWEST comment is a `$DEV_LOGIN`-authored, line-1-anchored `dev-author` question, i.e. a question is open and nobody has replied. A REPLY on the issue un-parks it. Identity- + line-1-bound (the auto-merge G2 discipline): a stranger pasting the anchor on a public repo cannot freeze a feature out of the backlog. Fail-safe toward ACTIVE — an unreadable list, or a question dev-author FAILED to post, re-offers the issue rather than stranding a feature nobody was told about (the park-marker design it replaces parked such an issue anyway, silently dropping it). The anchor is a contract with `dev-author`, pinned in lockstep by `dev-loop.test.sh`. **THE BACKLOG RIDES FD 3, NEVER STDIN** (the `pr-poller.sh` idiom): `dev-author` spawns a bounded `claude -p`, which DRAINS any stdin it inherits to EOF — fed off FD 0 the first author swallows the rest of the backlog, the next `read` hits EOF, and the pass silently truncates to ONE issue while still logging a tidy summary (it did; the mock only missed it by not touching stdin). Both ends are shut: the list on FD 3, the author run with stdin CLOSED (`dev-author` closes it at its `claude` call too), and `dev-loop.test.sh`'s author stub now DRAINS stdin like the real one, so all its rows discriminate. **The R9 FLEET HALT gates every pass (#151):** one_pass() reads `bin/fleet-halt.sh` at the TOP of every pass, BEFORE any author model run spawns — rc 0 alone is GO; a maintainer HALT or a dead checker (rc 127 ≠ 0) makes the pass OBSERVE-ONLY, still enumerating and logging the queue (`would author #N`) but spawning nothing and filing nothing (fail-closed BY CONSTRUCTION — every non-zero rc is "no new action"). An UNREADABLE signal is NOT one of those outcomes since #274 STEP 3: the checker returns rc 0 (GO) on it, loudly, rather than escalating to a halt nobody threw. HALT stops NEW work only: an author run already in flight completes; un-halt resumes on the next pass, no restart. **The old `--watch` hard-refusal (exit 2) is LIFTED by #151 itself (its req 9), in the same change that landed the check:** `--watch <repo>` now runs a flock-singleton loop (one pass per `LOOP_INTERVAL`, default 300 s; lock on tmpfs via `DEV_LOOP_LOCK`, never `$HOME`) — a clock that spawns bounded model runs is finally stoppable within one sweep, which is what R9 demanded before a timer mode could exist. `DEV_AUTHOR`/`DEV_LOGIN`/`FLEET_HALT` overridable for the mock test; `--selftest` covers the pure helpers. **BOUNDED RELEASE OF THE PARK (R39, #277) — the FIRST stop wired to `bin/stop-release.sh`:** the park used to end only when a human replied, yet much of what parks an issue is ENVIRONMENTAL (dev-author rc 3 worktree / 7 push / 8 PR-create) and never needed one. Each parked issue is now run through the three-state clock: HOLD inside `PARK_COOLOFF` (21600 s) · RELEASE past it — announce, then RE-OFFER the issue on that same pass · ESCALATE once `PARK_MAX_RELEASES` (2) are spent, then stay parked. The BUDGET IS THE BUS: each release posts one `**dev-loop → bounded release:**` comment and "releases so far" is a COUNT of those on the thread, so a wiped box resumes with the same budget (R14) and a release that could not be ANNOUNCED is NOT taken (no record ⇒ no bound ⇒ a re-attempt every pass, the R4 spin the clock exists to prevent). Age comes from the parking comment's own `createdAt` (an EXTERNAL clock, R24) — unreadable ⇒ HOLD, logged loudly, never a release on a broken clock; the escalation comment is itself a park anchor, so a reply un-parks it like any other. R9-HALT-gated (a halted pass announces and spawns nothing). Two parsing hazards are closed and mutation-pinned: the row is SPLIT BY HAND because `IFS=$'\t' read` collapses runs of tabs and would slide the release count into the empty-`createdAt` slot, and a timestamp must match an ISO shape before `date` sees it (GNU `date -d ""` resolves to midnight TODAY at rc 0 — an age of up-to-24h conjured out of nothing). **THE PLAN ARM (2026-07-28) — the step upstream of its own backlog:** `plan_pass` runs at the TOP of every pass (same R16 scope check, same single R9 halt read) and hands each open issue carrying BOTH `$INTAKE_LABEL` (`objective`, what `bin/intake-file.sh` files) and `$APPROVED_LABEL` (`approved`, the maintainer's tap) to `bin/dev-plan.sh`, whose `backlog` output the authoring arm then picks up in the SAME pass. This closes the R1 hole the intake front door fell into: `dev-plan.sh` — the ONLY component that enforces the maintainer confirmation — had NO CALLER anywhere in the fleet, so an objective filed as `backlog` went straight to `dev-author` → PR → auto-merge with the `approved` label read by nothing. **The label filter is DISCOVERY, never AUTHORISATION** (applying a label needs only triage/write, which every fleet App holds): `dev-plan.sh` independently resolves WHO applied `approved` and binds that actor to admin\|maintain, so this arm can only OFFER an objective to a gate it does not control. An objective the planner refused/blocked PARKS on dev-plan's own line-1 anchor (`park_state` gained an optional anchor argument rather than a second copy of the gate) — otherwise one mis-applied label would re-post the identical refusal every `LOOP_INTERVAL` (the R4 spin); a reply un-parks it. `MAX_PLANS_PER_PASS` (2) caps PLANNER RUNS via the pure `plan_class` (rc 0 + filed URLs = PLANNED, rc 0 + silence = an already-planned objective that must cost NO slot, else two planned objectives starve every later one). Wiring the R39 bounded release to this park is a disclosed follow-up. Covered by the plan-arm rows + the un-wire mutation in `dev-loop.test.sh` |
| bin/stop-release.sh | the BOUNDED-RELEASE library (R39 gate resilience, #277) — the pure DECISION shared by every stopping condition, so the loop's stops behave identically wherever they live. `release_verdict <held-seconds> <releases-so-far> <cool-off-seconds> <max-releases>` → **HOLD** (inside the cool-off) · **RELEASE** (elapsed, budget remains — re-attempt) · **ESCALATE** (budget spent, or none configured — tell the maintainer once). A stop is allowed; a stop with NO WAY OUT is a machine that has stopped and cannot restart, which is functionally a phone call to the maintainer. The caller owns both inputs and the escalation's idempotence; this owns only the decision, so it is pure and identical at every site. TWO DELIBERATE FAIL DIRECTIONS: an UNUSABLE cool-off can never ARM the axis (#270's inversion mirrored — `held >= 0` would RELEASE every sweep, an unbounded model-run spin) so it ESCALATES rather than silently holding; an UNREADABLE age HOLDS and never releases on a broken clock (disclosed residual: a permanently unreadable clock holds indefinitely — the right trade, and the caller logs the age it read so it stays visible). `max_releases=0` is the VALID way to declare "this stop has no automatic release". SOURCEABLE — the `--selftest` is `BASH_SOURCE`-guarded, which is load-bearing: `bin/dev-loop.sh` sources it AND has its own `--selftest`, and under `source` `$1` belongs to the caller. WIRED CALLER (one, today): `bin/dev-loop.sh`'s backlog park. NOT YET WIRED, disclosed rather than implied: `bin/pr-poller.sh`'s exhausted fitness review + no-progress fixer stop, and the whole-loop enumeration/audit of stopping conditions. Covered by `--selftest` + the end-to-end release rows in `dev-loop.test.sh`. **Control-plane (the loop's stop/release policy).** |
| bin/dev-loop-service.sh | supervises the AUTONOMOUS AUTHORING loop (`bin/dev-loop.sh`) as a headless IN-BOX service — the FRONT-HALF counterpart to `poller-service.sh`. Until this existed, the whole front-half (dev-plan/dev-author/dev-loop) was built + tested but run by NOTHING (the "arm-less authoring loop", apparatus R3): `dev-loop.sh --watch` exists yet nothing launched it. launched by `entrypoint.sh` INSIDE the claudebox (dev-author spawns `claude` to implement each feature; absent from the base) as PLAIN shell — no gate/classifier → the sanctioned autonomous-authoring path can execute (the interactive agent is deliberately gated FROM authoring, as from merging). **ARMED BY DEFAULT (self-arms with the apparatus, 2026-07-18):** the entrypoint gate defaults ON (`${DEV_LOOP_ENABLED:-1} != 0`), so once the self-refresh deploys the new entrypoint the loop starts with NO host action — because a host-set OPT-IN flag would be a deploy-CONFIG the self-refresh CANNOT set (it deploys the image + live-clone, never the host quadlet env), so a default-off flag would force a MANUAL host arm, breaking "self-arming = no more manual host actions". Set `DEV_LOOP_ENABLED=0` to disable; the R9 fleet HALT is the emergency stop (no redeploy needed). Same trust level as the already-armed poller. Waits for `.assembled` + the standing GitHub credential, then loops: each cycle re-reads the R16 actionable set (`repo-scope.sh list`) and runs ONE `dev-loop.sh <repo>` pass per scoped repo (dev-loop.sh is single-repo; THIS wrapper is the multi-repo supervisor, the ONE place the authoring repo set is decided — NEVER a hardcoded default, the #165 leak lesson: an unreadable/empty scope authors NOTHING). Every PR it opens still faces the host live-gate + the INDEPENDENT fitness review before the poller can merge it, and dev-loop.sh reads the R9 HALT + R16 scope at the top of every pass. **TWO LAUNCHERS → a #173-ADJUDICATED SINGLETON (self-arm, 2026-07-19):** it is launched by `entrypoint.sh` (image-baked) AND by the self-refreshing poller's `dev_loop_launch_tick` (the SELF-ARM path — the poller is CLONE-side, so it reaches a running box with NO rebuild and kick-starts the loop; see the `bin/pr-poller.sh` row). Because a double-launch is now routine, it dedups with a liveness-adjudicated pidfile — NOT a naive `flock -n || exit` (that reintroduces the #173 dead-holder-blocks-forever bug). `service_live` (exposed as `--is-live` for the poller's launch probe) defers ONLY to a holder that is positively alive (`kill -0`) AND whose `/proc/<pid>/cmdline` still names this script — so a DEAD or a RECYCLED pid never blocks a start (fail direction: liveness in doubt ⇒ START; a brief double-run is a cheap no-op — dev-author's per-(repo,issue) idempotency + open-PR guard make it harmless). It probes the SPECIFIC recorded pid, never a `pgrep -f` string-match across all procs (the process-pattern-self-match trap). The pidfile is reclaimed on clean EXIT only while it still names US (a race-loser never unlinks the winner's record). FD-3 repo list + stdin-closed dev-loop (the pr-poller idiom — `claude -p` drains inherited stdin). Best-effort + self-restarting, OUTSIDE the hard watchdog (its death must never take the container down — the poller's discipline). `--one-cycle` + `--is-live` are the test seams. Covered by `dev-loop-service.test.sh`. **Control-plane (autonomous authoring; box lifecycle).** |
| dev-loop-service.test.sh | proves `bin/dev-loop-service.sh`'s `--one-cycle` dispatch against a stub repo-scope + stub dev-loop: it runs EXACTLY ONE dev-loop pass per R16-scoped repo, in order; authors NOTHING (never a hardcoded default) on an empty scope + logs the fail-closed line; and a failing repo does not wedge the rest. **SINGLETON rows (self-arm, 2026-07-19):** `--is-live` reports the holder correctly across no-pidfile / dead-pid / recycled-pid (live but cmdline not naming the script) / live-and-named; a persistent launch EXITS 0 without cycling when a verified peer is live (dedup) and PASSES the singleton to the readiness wait when the holder is dead/absent (a dead holder never blocks — the #173 fail direction). TWO MUTATIONS RUN IN-SUITE (each sed must genuinely change the copy, else vacuous): the empty-scope hardcoded-`fedora-dev` fallback (the #165 leak) makes the empty-scope row FAIL; neutralizing the `/proc` cmdline verify makes a recycled pid read as LIVE (the verify row bites). Plus a DRIFT GUARD that the entrypoint `DEV_LOOP_ENABLED` gate stays default-ON. `bash dev-loop-service.test.sh` → exit 0. No GitHub/network/model. Run after touching the authoring service or its singleton |
| poller-devloop-launch.test.sh | proves `bin/pr-poller.sh`'s `dev_loop_launch_tick` (self-arm, 2026-07-19) — the CLONE-side poller kick-starts the authoring loop so it self-arms on a running box with NO rebuild. Drives the REAL `pr-poller.sh --once` with `DEV_LOOP_LAUNCH_EVERY=1`, an empty `gh` (quiet sweep) + a STUB dev-loop-service that answers `--is-live` and records a launch: launches when no live loop holds it / stays quiet when one is live / respects `DEV_LOOP_ENABLED=0` / skips under R9 HALT (+ says so). MUTATION RUN IN-SUITE: neutralize the `--is-live` gate → the poller launches even over a LIVE loop (double-launch), proving the gate discriminates. `bash poller-devloop-launch.test.sh` → exit 0. No GitHub/network/model. Run after touching the launch tick |
| dev-loop.test.sh + dev-plan.test.sh | MOCK dry-runs of the driver + planner. The gh stub **models the bus**: comments are APPENDED to a per-issue store and read back as the newest comment, so the no-local-state derivations are exercised for real rather than asserted. Loop rows: backlog-drain / continue-past-stuck / cap-x-already-authored-does-not-starve-the-tail / park-a-surfaced-question + un-park-on-a-REPLY / a question that FAILED to post does not park / a stranger's forged anchor cannot park / **E2E-KILL (R14): a WIPED box (fresh `$HOME`) still parks, and the driver leaves that `$HOME` empty** / the anchor it parks on is the one `bin/dev-author.sh` actually emits (three-way lockstep). Planner rows: maintainer-bound label-applier + comment-author / file→issue / BLOCKED / no-sentinel / partial-defer / total-create-failure-asks-ONCE + **a wiped box does not re-ask** + a reply un-mutes it / **a wiped box does not re-PLAN a planned spec** (the `planned:` summary is the tombstone — no `.planned` file exists) / an all-deduped recovery run still records that tombstone / **`MAX_FEATURES` DEFERS rather than drops**: the surplus is surfaced on the bus BY TITLE, the spec is left unplanned, and raising the cap on a re-run files exactly the deferred features. Every row asserts `$HOME` ends EMPTY (the no-local-state law, on every path — not just the wiped-box ones). **The stubs are FAITHFUL where it counts: both fake model runs DRAIN stdin, as the real `claude -p` does** — the axis on which the driver actually broke, and the one the first mock diverged on (a stub that never touched stdin passed a suite whose subject truncated the backlog after one issue; with the drain, 26 of the 30 loop rows fail against that pre-fix script). **R9 HALT rows (#151, the mutation detectors requirement 8 demands):** a HALTED pass spawns NO author run while still logging the queue (`would author` per issue) / un-halt resumes the same backlog next pass / a HALT thrown MID-PASS kills no in-flight work (the flag-file stub lets the first author run flip the switch; the pass still completes) / a MISSING checker is never a GO (rc 127 ⇒ observe-only) / `--watch` loops HALT-gated (rc 124 from the row's `timeout` IS the pass condition — it must not exit; under a standing HALT it stays up and spawns nothing, so un-halting needs no restart). Structural pins: the driver feeds FD 3, both it and `dev-author` run the model with stdin closed, and a re-plan's prompt carries the already-filed titles. Every gate row carries a DISCRIMINATOR so it cannot pass vacuously — each was proven to FAIL against the pre-fix script (the halt rows against a halt-check-neutralized driver: 7 FAILs). **BOUNDED-RELEASE rows (R39, #277)** drive the whole arc off the modelled bus — the stub now carries the CLOCK (a comment is written backdated by `FAKE_COMMENT_AGE`, so a park ages past its cool-off deterministically, no sleeps) and serves `issue comment`, so the driver's own release/escalation posts land on the same store and are counted back off it: a park HOLDS inside the cool-off / RELEASES past it and is re-attempted / exhausts its budget → ESCALATES exactly once → the escalation itself parks / a REPLY still un-parks an escalated issue / a wiped `$HOME` neither re-releases nor re-asks (R14) / an announcement that FAILS to post means the release is NOT taken / R9 HALT announces and spawns nothing, and the release is still due after un-halt / an unreadable `createdAt` HOLDS / `PARK_MAX_RELEASES=0` escalates at once / an EXHAUSTED park escalates even on an unreadable clock (the bound is read before the clock). TWO MUTATIONS RUN IN-SUITE (each `sed` must genuinely change the copy, else the row is vacuous): neutralize the RELEASE arm → an aged park is TERMINAL again (the pre-#277 defect); restore the collapsing `IFS=$'\t' read` → the empty-`createdAt` field folds away, the budget is read from the wrong column and an exhausted park NEVER escalates. Anchor drift-guards pin the driver's release/escalation literals and `bin/stop-release.sh`'s `BASH_SOURCE` selftest guard (sourcing it with the caller's `$1` = `--selftest` must define the function and NOT exit). No GitHub/network/model |
| dev-author.test.sh | MOCK end-to-end dry-run of `bin/dev-author.sh` (stubs gh+git+claude+fresh-tree+validate on PATH; asserts the full author→validate→push→draft-PR→ready→label→shipped-comment sequence + the BLOCKED/no-progress/in-box-RED/guard paths). Pins the #155 transport (the prompt reaches the model on STDIN; nothing but the flags rides argv) and the ISOLATION at its weakest strand: a fresh-tree that SUCCEEDS but yields an UNENTERABLE worktree must run NO model at all — the stub `claude` records every invocation, so the row fails the moment the `cd` stops guarding the body and the author runs in the caller's cwd (under `dev-loop`, the shared clone). Both were proven to FAIL against the pre-fix script. **The caller's cwd is itself a git repo whose HEAD is asserted UNMOVED after EVERY row** — the shared-clone leak is thereby a test failure rather than a silent corruption, and the suite cannot inflict it on the tree it validates: run from the repo root against the pre-fix script, the stub author (told to implement and commit) obeyed a failed `cd`, landed in the tester's OWN worktree and committed it onto the branch under test. That happened. **#182 COMMIT-RESCUE rows:** a `dirty` author (WROTE the work, exited before `git commit` — dirty tree, no sentinel) is RESCUE-committed + ships (PRCREATE), while a CLEAN-tree no-op still surfaces no-progress; MUTATION RUN IN-SUITE — neutralize the dirty-tree check (`if false`) and the SAME dirty work is LOST (surfaced as no-progress), the exact bug #182 removes (the mutant sits BESIDE the real script so it resolves its sibling repo-scope.sh and dies for the RIGHT reason, not a scope refusal). **#177 DISCUSSION rows:** a maintainer-reply comment reaches the model (the reply token appears in the prompt the author receives) + a MUTATION (neutralize the discussion inclusion → the reply is INVISIBLE to the model, the exact bug #177 removes). `bash dev-author.test.sh` → exit 0 = all pass. No GitHub/network/model. Run after touching the author |
| poller-fixer.test.sh | MOCK end-to-end dry-run of `bin/pr-poller.sh`'s RED→FIX arm (#152) — the coverage whose ABSENCE let the fixer ship both defects (`--selftest` only ever exercised the pure core). Drives the REAL sweep (`--once`) against a REAL bare origin + REAL clones + the REAL `fresh-tree.sh`, stubbing only `gh` and `claude`, so the push and the origin-side landing check are REAL git; `git` is intercepted ONLY to make a push LIE (rc 0, pushes nothing) or fail — the two ways a landing silently doesn't happen. Asserts the OUTCOMES: landed (origin advanced) / no-commit / BLOCKED / push-failed / did-not-land / branch-moved / fresh-tree-failed / **worktree-UNENTERABLE** (a tree that EXISTS — so `-d` passes — but cannot be ENTERED: the one isolation failure where everything rests on the `cd` in front of the model run; the row asserts NO model ran, nothing pushed, both shared clones untouched, that the refusal names its REAL cause — `notlogs 'BRANCH MOVED'` is the discriminator, since the pre-fix script reached its `git -C` branch check first and parked the PR on a bogus "the branch moved" while surfacing nothing — and that the REFUSED tree is nonetheless REAPED: refusing to USE a worktree is not a licence to LEAVE it on the home volume, and the first cut of this refusal returned before the reap, leaking one tree per unenterable head while the file still claimed every path reaps) / **R9-HALTED** (#151: a halted tick OBSERVES the RED PR — the routing decision is logged — but runs no model, pushes nothing, posts nothing; `FLEET_HALT=false` stands in for every non-GO checker outcome at once, `FLEET_HALT=true` pins the gate open for every other row; mutation-checked — neutralize the poller's halt flag and the row fails 5 ways), plus the standing invariants (the fixer's cwd is the isolated worktree on the PR's gated head, BOTH shared clones stay untouched on main, the worktree is reaped, `main` never moves, no `(if pushed)`). MUTATION-CHECKED: restoring the shared-clone `cwd` or deleting the origin landing verification each make it FAIL (both mutations were run). The `claude` stub reads its prompt from STDIN, as the real one does, so the prompt rows also pin the #155 transport: restore the argv form and the recorded prompt is EMPTY and every one of them fails. `bash poller-fixer.test.sh` → exit 0 = all pass. No GitHub/network/model. Run after touching the fixer |
| poller-stall.test.sh | the R18 IDLE-WITH-WORK-PENDING suite (audit 2026-07-18 CAT-42/01; the kd#23 six-hour silent stall) — drives the REAL `pr-poller.sh --once` against a stub `gh` serving ONE `live-validate`-labelled open PR with NO host verdict (host=NONE) and a CONTROLLABLE head-commit age, and asserts the poller SURFACES a `[stalled]` comment once the head has sat past `POLLER_STALL_MAX` (was NOOP-forever), stays quiet when the head is FRESH (< bound) or UNLABELLED (no gate expected). **MUTATION RUN IN-SUITE:** `stall_verdict` is neutralized to never return STALL (the sed must genuinely change the copy, else vacuous) and the aged head must then surface NOTHING — proving the WORK-age clock is what bites, not the surface plumbing. `bash poller-stall.test.sh` → exit 0. No GitHub/network/model. Run after touching the stall detection or the sweep's label read. **Runs with `POLLER_REPAIR_MAX=0`** — since R39/#278 a STALL routes to bounded self-repair first, so with repair enabled the clock's outcome is a fixer run rather than a comment; disabling repair pins the outcome back to the observable surface these rows were written to assert (the documented repair-disabled mode = the pre-#278 behaviour). The repair-ENABLED path over the same clock is `poller-anomaly-repair.test.sh` |
| poller-rebase.test.sh | the CAT-17 REBASE-OWNERSHIP suite (audit 2026-07-18) — proves the poller owns the mechanical rebase of a GREEN+PASS PR behind main instead of parking it terminally. Copies `bin/` so a stub `auto-merge.sh` (always rc 2 = behind main) sits beside the real siblings, drives the REAL `pr-poller.sh --once` on a GREEN+PASS labelled PR with a controllable `gh pr update-branch`: a CLEAN behind → update-branch called, NO `[rebase]` surface, NOT parked (the new head re-gates); a CONFLICT (update-branch fails) → tried, then surfaced `[rebase]` + parked. **MUTATION RUN IN-SUITE:** force `rebase_due` to always GIVEUP (the sed must change the copy, else vacuous) and even a clean-behind PR gets NO update-branch and parks — the old terminal behaviour the fix removes. `bash poller-rebase.test.sh` → exit 0. No GitHub/network/model. Run after touching the rc-2 merge arm. **Runs with `POLLER_REPAIR_MAX=0`** — since R39/#278 the rc-2 give-up path routes a genuine conflict to bounded self-repair BEFORE the maintainer, so disabling repair keeps these rows asserting their own subject (WHO owns the rebase); the repair-first road out of the same arm is `poller-anomaly-repair.test.sh` |
| poller-anomaly-repair.test.sh | the R39/#278 ANOMALY→REPAIR WIRING suite — proves the unanticipated tail reaches the FIXER rather than the maintainer, which is the ONE fact `--selftest` is structurally blind to. #278's first cut shipped `anomaly_route` + `surface_or_repair` with a green selftest and **ZERO CALL SITES**: a pure core no sweep could reach, so the running poller behaved byte-identically to before while the change claimed self-repair had landed (a pure-core test cannot see that nothing calls the function; one `grep` for a call site could). So this suite tests no router — it drives the REAL sweep (`pr-poller.sh --once`) against a REAL bare origin + REAL clone + the REAL `fresh-tree.sh` (only `gh` + `claude` stubbed) and asserts THE MODEL ACTUALLY RAN. Rows: an aged `live-validate` head at host=NONE → the fixer runs in an ISOLATED worktree on the stuck head, gets the **ANOMALY** prompt framing, lands a real commit at origin, charges the budget, and does NOT surface `[stalled]` / a SPENT budget → escalates to the human naming the attempts, no model run / `POLLER_REPAIR_MAX=0` → the pre-#278 surface-only behaviour exactly. **MUTATION RUN IN-SUITE:** the `stalled` call site is un-wired back to plain `surface()` — restoring the shipped dead-code state — and the model must then never run; the mutant is copied BESIDE the real `bin/` siblings, because a mutant in a bare tmpdir fails the R16 scope read and SKIPS the repo, which would satisfy "no model ran" for the wrong reason and make the row vacuous. Its vacuity guards use `grep -F` (GNU grep's BRE does not match these `$`-bearing literals, so a plain `grep -q` would report "not vacuous" unconditionally). Proven to BITE: 8 rows fail against the returned pre-fix head. **UNENROLLED-PR SELF-HEAL rows (#278 task 5)** drive the same real sweep with an UNLABELLED PR (the stub's row is the real query's SIX fields — `${FAKE_LABELS-…}`, not `:-`, so an explicitly empty value means "no labels" while unset still means labelled): an unenrolled dev PR gets `--add-label live-validate` with no surface and NO model run / an already-enrolled PR is NOT re-labelled (we heal an omission, we do not fight a human's removal) / a FOREIGN author and a DRAFT are both left alone / a repo lacking the label gets `gh label create` then enrols (the 12-hour stall) / a PERMANENT add failure surfaces `[enroll-infra]` to the human, runs no model, and does NOT write the enrolled marker / a FIRST failure under the bound surfaces nothing (a blip must not page anyone) but IS counted. A SECOND MUTATION RUN IN-SUITE un-wires the `enroll_pr` call site and demands the label then never lands — the same discipline that makes the repair rows mean something, applied to the arm that would otherwise be the next piece of wired-shut dead code. `bash poller-anomaly-repair.test.sh` → exit 0. No GitHub/network/model. Run after touching `anomaly_route`, `surface_or_repair`, `enroll_pr`, or any of its call sites |
| poller-selfrefresh.test.sh | the SELF-REFRESH suite (#162) — proves the running poller deploys its OWN merged code with no human `git pull` + bounce. Drives the REAL `bin/pr-poller.sh` (`--self-refresh-check` seam + a REAL `--watch` loop) and `bin/poller-service.sh` (`--self-refresh-pull` seam) against a REAL bare origin + working clone; only `gh` is stubbed (so a sweep is a cheap NOOP) — the fetch/merge under test is REAL git. Decision rows: advanced+clean → signals a reload and WRITES NOTHING (detection only; the supervisor pulls) / dirty → left untouched + logged / diverged → left untouched + logged / failed fetch → continue + logged / up-to-date → SILENT no-op / `SELF_REFRESH=0` disables it. Supervisor rows: `self_refresh_pull` advances a clean+ff clone, REFUSES a dirty one (ff-only, never clobbers), no-ops an already-current one. **Loop rows (req 6, MUTATION-CHECKED):** (a) an advanced origin makes the `--watch` loop exit `POLLER_RELOAD_RC` at a loop boundary AFTER a full sweep — `sweeps_before_refresh` pins the rate-limit and an awk check asserts NO self-refresh line falls MID-SWEEP (sweep()'s fixer/review/merge are synchronous, so *never mid-sweep* IS *never mid-fixer*); (b) a dirty clone leaves the loop running (it keeps sweeping, clone untouched); (c) a failed fetch does not stop the loop. **LAUNCH-HEAD rows (#170 — the clone HEAD is a FALSE PROXY for the running code; the FIRST live exercise proved a third-party pull of the clone reads clone==origin as UPTODATE forever and silently self-disables the reload):** a seam row injects a stale launch head (`POLLER_LAUNCH_HEAD`) against an externally-pulled clone and demands RELOAD; the proxy (`running=$(git rev-parse HEAD)`) is then RESTORED MECHANICALLY IN-SUITE (the sed must genuinely change the copy or the row fails as vacuous) and the same fixture must read UPTODATE — proving the row discriminates; a backgrounded `--watch` row pulls the clone MID-RUN (after the first sweep line, before the first check) and the loop must still exit `POLLER_RELOAD_RC` naming the LAUNCH head it advanced past; and a dirty-clone-LATER-CLEANED row cleans the clone while the SAME loop runs and demands the reload resume (no sticky state — req 3 of #170). Proven biting: disabling the loop check yields `rc=124` (timeout) not 90 → the reload row FAILS; calling the check inside `sweep()` puts a self-refresh line mid-sweep → the never-mid-fixer row FAILS (both mutations were run); against the pre-#170 poller the external-pull seam row reads rc=0/UPTODATE, the mid-run-pull loop row times out at rc=124, and the in-suite mutation row reports itself vacuous (all three run). `bash poller-selfrefresh.test.sh` → exit 0 = all pass. No GitHub/network/model. Run after touching the self-refresh path |
| poller-lock.test.sh | the LOCK-LIVENESS suite (#173) — proves the flock singleton survives a box recreate: a dead lock-holder can no longer silently down the poller for hours (2026-07-13: a claudebox-rebuild orphaned the running poller; its lingering FD held the home-volume lock while the fresh poller exited rc=0 every 30 s, 08:27→12:23 — zero sweeps, zero signal). The fixture is REAL where it must be: the flock is genuinely held by a lingering background process (an inherited fd — the incident's exact shape), the recorded holders are real processes with real `/proc` starttimes, and every row drives the REAL `bin/pr-poller.sh --watch`; only `gh` is stubbed (sweeps are cheap NOOPs, issue creates recorded). Rows: a lock whose RECORDED holder is DEAD → the new poller rotates the lock, STARTS and SWEEPS while the innocent lingering FD-holder is never signalled (the issue's req-4 anchor row) / a genuinely LIVE same-generation holder → still DEFERS (the discriminator: adjudication must not decay into take-always), rc=`POLLER_DEFER_RC` and never 0 (req 2's exact lie), no second poller, holder untouched / a LIVE PREVIOUS-GENERATION orphan (THE incident) → SIGTERMed, lock taken, sweeps resume / a RECYCLED pid (live stranger wearing the recorded pid with a starttime past the flutter tolerance) → takeover and the stranger is NOT killed / `LOCK_DEFER_MAX` CONSECUTIVE deferrals → exactly ONE surfaced `gh issue create`, marker-gated against re-posts, and a later acquisition RESETS count + marker (the next incident's question is not swallowed) / a deferral older than `LOCK_DEFER_WINDOW` never pre-charges a fresh streak (the "consecutive" claim stays true). **THE MUTATION IS RESTORED MECHANICALLY AND RUN IN-SUITE** (issue req 4: *"restoring the bare-flock behavior must fail the dead-holder row"*): a copy gets the bare `flock -n 9 \|\| exit 0` back (the sed must genuinely change the copy or the row fails as vacuous) and against the SAME dead-holder fixture it must exit rc=0 WITHOUT sweeping — the incident's behaviour, proving the real row discriminates. The starttime ±2-tick flutter tolerance is pinned by `--selftest` rows (/proc starttime measurably flutters ±1 tick between reads of the same process). `bash poller-lock.test.sh` → exit 0 = all rows pass. No GitHub/network/model. Run after touching the lock path or the deferral surfacing |
| host-refresh.test.sh | the HOST-HALF SELF-REFRESH suite (#163) — proves a merged image/bootstrap change reaches the running host promptly, and that the two forbidden behaviours the issue names cannot come back. Only `gh` is stubbed (scenario fixtures answered at gh's own `-q` output level); the ticket producer is the REAL `bin/host-ticket.sh` (the recorded ticket pins the consumer's `host-op: redeploy <workload>` line-1 grammar) and the wiring rows drive the REAL `bin/pr-poller.sh --once`. Rows: an image-baked merge + published CI files EXACTLY ONE redeploy ticket (idempotent across re-scans, audit anchor posted, and the publish gate demonstrably checked THIS merge's commit) / a live-clone-only merge (bin/+policy/) files NONE ever / a PENDING publish files none and is RETRIED — not parked — then files exactly once when CI lands / a FAILED publish files none EVER, logs the MISS, and stays terminal / a WIPED box re-derives from the PR's own `filed:` anchor and never files twice (req 4) / a stale merge parks to the monthly timer / control-repo rows (#133/#187): a host-path bootstrap merge FILES ONE `apply-bootstrap` ticket (line-1 grammar `host-op: apply-bootstrap`, no arg) + the `apply-filed:` anchor and is idempotent across re-scans, doc-only files nothing, a wiped box re-derives from the `apply-filed:` anchor and never re-files, and the TRANSITION PR (a pre-#187 `host-apply needed:` question present) is left alone / wiring rows: the poller fires the scan at `HOST_REFRESH_EVERY`, an R9-HALTED tick skips it and says so, the default cadence never fires on a single `--once`, and `HOST_REFRESH_EVERY=0` disables. **The issue's two named mutations are RESTORED MECHANICALLY AND RUN IN-SUITE** (each sed must genuinely change the copy, else its row fails as vacuous): "file a redeploy on EVERY merge" (classifier neutralized) makes the live-clone fixture file, and "file BEFORE publish" (gate forced PUBLISHED) makes the pending fixture file — proving both real rows discriminate. `bash host-refresh.test.sh` → exit 0 = all pass. No GitHub/network/model. Run after touching the host-refresh trigger or the poller's tick wiring |
| rebuild-request.test.sh | the R17 PRODUCER suite (#174 + D4/#191) — drives the REAL `bin/rebuild-request.sh` against a STUB `SESSION_SOURCE` (a fixture emitting `name<TAB>cwd<TAB>sid` lines the way `enumerate_claude_procs` does) and validates every emitted manifest through the **EXECUTOR'S OWN `parse_manifest` awk, copied VERBATIM from fedora-bootstrap `host-agent-watch.sh` (post-#143, the 4-field grammar)** — so byte-compatibility with the consumer is PROVEN, not asserted (this embedded copy is the cross-repo parity anchor; if the executor's grammar ever changes, this changes in lockstep). Rows: a tenant WITH a valid UUID sid + an ephemeral `c<pid>` → the executor parses rc=0, a **v2 4-field `name cwd sid`** line is emitted, the ephemeral dropped / a tenant with NO sid (old bin/claude) → a **v1 3-field** line / a non-UUID sid **DEGRADES to v1** (never a bad 4th field) / an unsafe cwd (a space) is SKIPPED and the rest (incl. their sid) survive (executor still rc=0, never the whole-ticket rc=2) / a non-allowlisted NAME is skipped / >32 → capped at exactly 32, executor accepts (never the >MAX rc=2) / only-ephemeral → a well-formed EMPTY block (rc=0), never the missing-block rc=3 / default `request` mode composes a body whose LINE 1 is the exact `host-op: rebuild-devbox fedora-dev` and whose 4-field manifest parses rc=0 carrying the sid. **TWO MUTATIONS RUN IN-SUITE** (each `sed` must genuinely change the copy, else the row fails as vacuous): neutralizing `valid_sid` makes a non-UUID sid LEAK a 4-field line → executor rc=2; neutralizing `valid_cwd` makes an unsafe cwd (space) LEAK → executor rc=2 — proving BOTH guards bite and that any leaked bad field rejects the whole ticket. A v2-OFF default row + a v2-ON path prove the `DEVBOX_MANIFEST_V2` rollout gate. **FILE-MODE rows (R17 approval flow):** `file` files the ticket (APPROVAL-REQUIRED title, `host-task` + `rebuild-approval` labels, @mention, line-1 op exact, manifest parses via the executor oracle with the sid) / IDEMPOTENT (an OPEN same-op ticket ⇒ rc 0, no create) / R16 out-of-scope ⇒ rc 1, no create / a failed create ⇒ rc 1 / **AMBIGUITY (#226)** — a NO-SID session sharing a cwd with another ⇒ REFUSE (rc 1, no create, names the shared cwd + cites `--continue`), while a LONE no-sid on a UNIQUE cwd still FILES (the guard does not over-refuse); a MUTATION neutralizing the guard makes the shared-cwd manifest FILE (proving it bites), and `--selftest` covers `ambiguous_v1_cwds` (shared-cwd flags, unique/all-sid does not). **POLLER-WIRING rows** drive the REAL `pr-poller.sh --once`: flag → files once + consumes the flag / no flag → nothing / R9 HALT → skipped + flag kept + says so / failed filing → flag kept for retry. `bash rebuild-request.test.sh` → exit 0 = all rows pass. No GitHub/network/real processes. Run after touching the producer or the manifest grammar |
| fitness-review.test.sh | the FITNESS GATE's TRANSPORT + FAILURE-HONESTY suite (#155) — the coverage whose absence let a **single argv argument** silently make the gate UNRUNNABLE on any PR above ~128 KiB (`MAX_ARG_STRLEN`), then blame the model for it, then re-attempt forever. The sizes are REAL: rows drive the REAL `bin/fitness-review.sh` with a diff big enough that an argv prompt genuinely cannot EXEC on this kernel, against a stub reviewer that asserts on the bytes it ACTUALLY RECEIVED (head + tail sentinels, byte count, and which CHANNEL they came on) — not a mock that never exercises the transport. Rows: a >`MAX_ARG_STRLEN` prompt reaches the reviewer INTACT and produces a verdict / the DEFAULT `FITNESS_DIFF_CAP` is carried WHOLE (read out of the script, so cap and proof cannot drift — *a cap must sit below the ceiling it protects*) / **the argv form is mechanically RESTORED and RUN in-suite** (the sed must genuinely change the file, else the row fails as vacuous) and must yield NO verdict with the reviewer's inbox EMPTY / the three failure events (could-not-exec · non-zero exit, carrying its own stderr and its OWN rc — not a printf SIGPIPE masquerading as it · ran-but-no-verdict · ran-but-said-nothing) each produce a DISTINCT line and post NOTHING / truncation is announced to the operator, to the reviewer (with an ESCALATE instruction) AND on the verdict, while line 1's grammar + sha-binding (G2) stays byte-stable / and the poller's REVIEW arm is driven for real, sweep by sweep: a PERSISTENT rc 3 is retried exactly `FITNESS_REVIEW_TRIES` times, then SURFACES the real cause ONCE and re-runs no model on that head; a TRANSIENT rc 3 that clears on the retry is REVIEWED and never asks anyone anything (the row that fails against a harness which parks on the first rc 3 — the #156 defect: one blip stranding a host-GREEN PR forever); the retries are SPACED (a sweep inside the backoff spends no model run and says so); a PARKED head still ROUTES a hand-posted PASS to MERGE (the remediation the question prints is proven honoured, not claimed); with rc 1 (retryable) as the DISCRIMINATOR that neither surfaces nor stops — and that RETRIES OUT LOUD, logging the harness's own stderr, because the arm captures that stderr to build the rc-3 question and a capture-then-discard would make the one path that repeats every sweep the only one that never says why. `bash fitness-review.test.sh` → exit 0 = all pass. No GitHub/network/model. Run after touching the fitness gate or any harness that hands a model a prompt |
| fleet-halt.test.sh | the R9 FLEET HALT reader's contract suite (#151) — drives the REAL `bin/fleet-halt.sh` against a stub `gh` (answering at gh's own `-q` output level) and asserts BOTH caller channels: the rc (0 RUN / 10 HALT — **rc 20/PAUSE retired by #274 STEP 3**) and the stdout token. The rows that bite: a MAINTAINER-applied `halt` HALTS and names its maintainer / an APP-applied one is INERT + loudly logged (the self-halt hole — presence-only reading — closed) / an App REMOVING a maintainer's halt does NOT un-halt (the mirror hole; the fold decides by the newest MAINTAINER timeline event, never presence) / a maintainer's removal DOES un-halt within one read / ABSENT ⇒ RUN (a tidied-away control issue never freezes the fleet) and so does an UNREADABLE signal — quietly under the bound, LOUDLY (saying it is "not a halt", on stdout AND stderr) past K CONSECUTIVE failures, and NEVER escalating to HALT however long the outage runs; a clean read resets the streak / an unreadable TIMELINE and an unresolvable actor ROLE each RUN too — and the role case then HALTS the moment the role reads again, so the signal still bites / a decoy title-match cannot mask a halted control / a missing checker exits non-zero, which every caller reads as "no action" (fail-closed by construction). The unreadable rows DISCRIMINATE: 10 of them FAIL against the pre-#274 fail-closed checker (verified). `bash fleet-halt.test.sh` → exit 0 = all rows pass. No GitHub/network/model. Run after touching the halt reader; the sweeper-side halt rows live in `poller-fixer.test.sh` + `dev-loop.test.sh` |
| repo-scope.test.sh | the R16 OPERATING-SCOPE suite (#167) — proves the #165 incident class is dead, with the issue's own "tests that bite": a swept repo ABSENT from scope produces ZERO actions and ONE loud log line (the stub `gh` records every call — none may name the foreign repo); an UNREADABLE scope config FREEZES foreign-repo action while the apparatus's own repos continue (the rule-4 fail direction, driven through the REAL poller); the sweep list DERIVES from the config (no `POLLER_REPOS` set ⇒ exactly the configured repos are swept); the fitness gate RETURNs an unconfirmed scope-expansion PR DETERMINISTICALLY — the stub reviewer's inbox stays EMPTY, line 1 keeps the G2 grammar + full-sha binding, and the rationale carries the remediation — while a maintainer's name-listing `CONFIRMED` proceeds to the model and a NON-maintainer's `CONFIRMED` (role `write`, the fleet-App case) stays inert; the confirmation's NAME BINDING bites (98e1194 finding 2): a bare and a prose `CONFIRMED` each confirm NOTHING, a post-confirmation head that SWAPS the add re-gates unconfirmed, partial coverage RETURNs naming exactly the uncovered repo, and two maintainer comments UNION to cover two adds; the crafted-hunk ESCAPE is dead (98e1194 finding 1): an added `++ b/…` line inside the scope hunk cannot hide the add behind it — driven through the REAL fitness gate, model not consulted; a removal-only and a moved-line diff never trip the gate (narrowing needs no ceremony); and every other actuator (auto-merge, dev-plan, dev-loop, dev-author, host-ticket, host-refresh) refuses an out-of-scope repo with NO gh write recorded. MUTATION-CHECKED IN-SUITE (the `fitness-review.test.sh` discipline — each sed must genuinely change the copy or its row fails as vacuous): neutralizing the poller's sweep scope-gate makes the foreign repo GET SWEPT (the pre-#167 hardcoded-default behavior restored ⇒ the suite fails, per issue req 7), neutralizing the fitness diff-adds detector makes the unconfirmed expansion PASS on the stub reviewer's verdict, and de-STATING the diff parser makes the crafted escape hide the add. `bash repo-scope.test.sh` → exit 0 = all rows pass. No GitHub/network/model. Run after touching the scope reader, the sweep gate, or the fitness R16 gate |
| bin/host-ticket.sh | the DEV-SIDE PRODUCER for the dev→host ticket bus (apparatus fedora-dev#131 R5) — the counterpart to the host's `host-agent-watch.sh` CONSUMER. The dev box is walled off from host ops, so to get one done it FILES a ticket: `host-ticket.sh [--wait] <verb> [args]` opens a `host-task`-labelled issue in the control repo (`oso-gato/fedora-bootstrap`) whose line 1 is `host-op: <verb> [args]` (lazily create-on-use for the label), prints the URL, and with `--wait` BLOCKS polling the issue's comments for the host agent's `host-agent: DONE\|FAILED` verdict (exit 0/1/2). Plain shell, no model; uses the standing App `gh` credential (needs Issues:write on the control repo). `--selftest` exercises the pure `op_line`/`outcome_of` helpers. Meant to be called by the poller/service, by `bin/host-refresh.sh` (#163 — the self-refresh host half's trigger) or by a human — NOT the interactive agent (whose outward `gh issue create` is classifier-gated by design). |
| bin/host-refresh.sh | SELF-REFRESH, the HOST HALF (#163) — the trigger that makes a merged change ONLY THE RUNNING HOST can apply reach erebus promptly + autonomously (the dev half is #162; before this, an image-baked merge waited on the MONTHLY workload-refresh timer or a human runbook). Plain shell, invoked by `bin/pr-poller.sh`'s `host_refresh_tick` (once per `HOST_REFRESH_EVERY`=30 sweeps, at the END of a tick, gated by THAT tick's R9 halt read; a lone `--once` fires only under `HOST_REFRESH_EVERY=1` — the manual catch-up / test seam). WIRES THE PROVEN SEAM, builds no host machinery: per enrolled WORKLOAD repo (`HOST_REFRESH_WORKLOADS`, mirroring the host agent's KNOWN_WORKLOADS arg allowlist) it scans recently-merged PRs and files ONE `redeploy <workload>` ticket via `bin/host-ticket.sh` — the host agent + `workload-refresh@` + `container-refresh.sh` (busy-probe deferral · digest-compare · health-gate · digest AUTO-ROLLBACK; R10 recovery-before-power stays where it already lives, untouched) do the rest — iff BOTH gates hold: (1) the diff touches an IMAGE-BAKED path (pure `image_relevant`: root `Containerfile*`/`install*.sh`/`entrypoint*.sh` — what only a rebuilt image delivers to the running container; the live-clone bin/+policy/ half belongs to #162 + the daily box rebuild, the deploy-contract `run.sh*`/`spin-up.sh`/`*.container` the host reads from its own setup-managed clone — container-refresh does not git-pull, verified — and docs/tests/CI never redeploy; fail direction = NOT-relevant: a miss degrades to the monthly timer, a spurious redeploy bounces a live box for nothing), and (2) the merge commit's CI `build.yml` run CONCLUDED SUCCESS (`gh run list --commit` → pure `publish_state`): file AFTER publish, so the host pulls the NEW digest and never a stale `:latest` — PENDING retries next scan (deliberately NO marker: its absence IS the retry), FAILED/CANCELLED parks with the MISS logged (that merge's image will never exist; the next image-baked merge or the monthly rebuild carries its paths). Idempotent + no-local-state where it matters (req 4): every DECISION input is GitHub-derived (PR files + run state + a `mergedAt` catch-up window `HOST_REFRESH_MAX_AGE`), the merged PR is stamped `host-refresh → filed:` (audit trail + wiped-state dedup anchor), the scan-once markers are pure COST bounding (the retire-scan precedent), and a duplicate filing is a safe no-op anyway (the host agent's claim/outcome markers + the digest-compare). Per CONTROL repo (fedora-bootstrap): a merged change touching host-executed paths (`host_relevant` — anything but docs/tests/CI) now FILES exactly ONE `apply-bootstrap` ticket via `bin/host-ticket.sh` (the bounded verb has landed, #133/#187) — the host FF-pulls the control clone to merged `main` + re-runs setup.sh as root (health-gated with rollback + a fail-closed live readback; idempotent, same-sha ⇒ no-op). apply-bootstrap takes NO arg (it applies pinned, merge-gated `main`). CONFIG-CONVERGE ONLY: it re-installs the Quadlet FILE + daemon-reload but does NOT recreate a running container, so a changed Quadlet `Environment=`/`Secret=` takes effect on the NEXT recreate (the approved-gated Tier-2 recreate is the disclosed follow-on). Dedup anchor `host-refresh → apply-filed:` (a duplicate filing is a harmless no-op — host idempotency + the agent's per-ticket claim/outcome markers); TRANSITION: a PR already carrying the pre-#187 `host-refresh → host-apply needed:` question is left alone (surfaced for a human when no verb existed — only FRESH merges are filed). FAIL-SAFE (req 5): every fetch/create/post failure logs and degrades to the status quo — nothing can stop the merge loop, and nothing can redeploy an unpublished digest. RESIDUAL (disclosed): the three image-baked bin/ wrappers (`bin/claude`, `bin/claudebox-rebuild`, `bin/gh-app-auth.sh`) miss the prompt redeploy — #163's own rule keeps bin/ wholesale on the live-clone side; they ride the monthly rebuild. `--selftest` covers the pure core (`image_relevant`/`host_relevant`/`publish_state`); covered end-to-end by `host-refresh.test.sh`. |
| bin/reconcile.sh | the RECONCILER's first slice — **PROOF-GATED CLOSURE** of backlog issues (task #19; Arthur's decision 2026-07-18). Replaces `dev-author`'s old `Closes #N` (which auto-closed the issue THE INSTANT the PR merged — before the change was built, published, or proven live: "merged ≠ live", the audit's structural pattern #2). dev-author now stamps a NON-closing `Backlog-ticket: #N` linkage; THIS plain-shell actuator (headless, bus-write-capable — the sanctioned autonomous closer, exactly as the poller's retire_superseded is the sanctioned PR-closer; the interactive agent is classifier-walled from issue writes) scans recently-merged PRs carrying that linkage and closes #N ONLY when the WHOLE proof chain holds, posting the proof links as the closing comment: (1) MERGED · (2) HOST-GREEN (`$LG_HOST_LOGIN` posted VERDICT GREEN) · (3) CI-PUBLISHED (the merge commit's build.yml run CONCLUDED SUCCESS — reuse of host-refresh's publish fold; PENDING⇒WAIT, FAILED⇒the artifact never exists) · (4) LIVE READ-BACK. Fail-closed at every link (a missing/unverifiable link WAITS or SKIPS, never closes — an unclosed issue stays OPEN, the safe direction). FIRST SLICE covers the LIVE-CLONE class (bin/**, policy/**, tests, docs — what the running box executes from its live clone): live ⟺ the merge sha is an ANCESTOR of the deployed clone HEAD (the poller self-refresh advanced the box onto code including it). The IMAGE-baked class (Containerfile/install/entrypoint — only a rebuilt+redeployed image delivers it) needs the host redeploy-DONE read-back — a DISCLOSED FOLLOW-UP; such merges WAIT here, never wrongly closed. NO LOCAL STATE (the host-refresh precedent): every input is GitHub/clone-derived, the dedup anchor is a `reconcile → closed:` comment on the PR, an already-CLOSED #N is a no-op. R16-scoped, R9-halt-gated (called from the poller's `reconcile_tick`, once per `RECONCILE_EVERY` sweeps, at the END of a tick — a close is an ACTION). **"NOT YET" vs "NEVER" — an UNTAKEABLE link is `NA`, not a forever-WAIT:** three links were unsatisfiable for whole classes of repo yet encoded as retryable WAITs, so the reconciler waited 147 times for events that were never coming (fedora-bootstrap ships no `build.yml` at all · e2e-beta #14/#15 merged BEFORE `build.yml` landed · the live read-back is a git-ancestor check against `$DEV_CLONE`, so for a repo the apparatus merely DEVELOPS there is no deployed instance of it ANYWHERE to read back from — `NA` means the absence of any INSTANCE, never a missing READ). **THE DEPLOY SET IS THE CARVE-OUT — `PENDING`, never `NA`, for every repo the apparatus RUNS AN INSTANCE OF** (`deployed_p` over `RECONCILE_DEPLOYED`, whose default MIRRORS `host-refresh.sh`'s own deploy set: its `WORKLOADS` — `fedora-dev fedora-desktop`, the repos it files `redeploy <workload>` tickets for — plus its `CONTROL_REPO`, `fedora-bootstrap`). For all of them a real delivery signal ALREADY EXISTS on the bus (the host App's `**host-agent: DONE|FAILED**` on the redeploy/apply ticket the merge files, which `host-refresh.sh` `ticket_outcome()` already reads); only the READ is unwired here, so they WAIT — a permanent wait by design, not a fixed one — until that is a DISCLOSED FOLLOW-UP made good. **The carve-out is a SET, not a slug, because keying it on the literal `fedora-bootstrap` mis-sorted `fedora-desktop`** — a live in-scope workload the apparatus demonstrably deploys (`redeploy fedora-dev` #255 → `host-agent: DONE` proves the signal works) — as "never", closing its tickets the instant CI published, i.e. on merge alone: the very "merged ≠ live" pattern this actuator exists to kill, re-armed inside the fix for it. Measured stakes on the control repo: all 27 `apply-bootstrap` tickets (#239…#317) read FAILED, none DONE, so an `NA` there would have closed #187's issue #133 as shipped for a feature that has never once run on the host. FAIL DIRECTION is deliberately the mirror-image of `host-refresh.sh`'s: an EMPTY `RECONCILE_DEPLOYED` takes the default instead of emptying the set (`:-` at BOTH the assignment and in `deployed_p`), because empty here would mean "nothing is deployed" ⇒ every repo `NA` ⇒ close with no delivery link, whereas empty there is a safe no-op; and the set is deliberately NOT read from `$HOST_REFRESH_WORKLOADS`, which answers a different question ("should I file tickets?") and would import that opposite contract — `reconcile.test.sh` pins the two enumerations in lockstep instead. `close_decision` accepts `NA` on those two links exactly as it already did for an image-baked change; `NA` is only ever concluded from POSITIVE evidence (`publish_applicable_p`: a cleanly-read git tree AT that merge commit with no workflow in it) — an unreadable or truncated read yields "applicable" and keeps waiting, so a failed read can never manufacture a close. The two links that CARRY the proof (MERGED, HOST-GREEN) can never be `NA`. **AND THE CLOSING COMMENT REPORTS EACH LINK'S REAL STATE (`proof_summary`/`proof_tag`):** a link that was not taken says `N/A` and why, instead of being folded into a blanket "CI published, live read-back OK" — that comment is this actuator's PERMANENT audit record AND its dedup anchor (never rewritten), for a component whose whole purpose is proof-gated closure, so claiming a link that was skipped would put a false proof on the one artifact that must not lie. `--selftest` covers the pure core (`close_decision`/`backlog_ref`/`change_class`/`deployed_p`/`live_readback`/`proof_summary`/`proof_tag`); covered end-to-end by `reconcile.test.sh`. **Control-plane (the closure boundary's executor).** |
| reconcile.test.sh | proves `bin/reconcile.sh`'s proof-gated closure: drives the REAL `reconcile.sh --once` against a scenario-driven stub `gh` + stub `git` (the live-clone ancestor check), flipping ONE proof link per row — FULL chain → issue CLOSED with proof / CI-PENDING → NOT closed (the core: never close before the artifact exists) / no host-GREEN → not closed / IMAGE-baked → WAITs (the disclosed follow-up) / live-read-back FALSE → not closed / already-CLOSED → no-op / dedup anchor present → skip / no `Backlog-ticket:` trailer → ignored. **HONEST-REPORT rows (the artifact the feature exists to produce):** the closing comment is asserted BY CONTENT, since a `--selftest` of the composer is structurally blind to whether `scan_repo` uses it — a publish-`NA` close (no run AND no `build.yml` in the tree at that commit) and a live-`NA` close (any repo but fedora-dev) must each CLOSE while reporting that link as `N/A`, and must NOT claim `published` / `read-back OK`; the FULL-chain row asserts the converse, so the fix cannot blanket-`N/A` a link that WAS taken. The stub's `PUB`/`TREE_PATHS` use `-` not `:-` (an explicitly EMPTY value means "no run" / "a tree with no paths"; `:-` folds the N/A scenario back into a successful run and the rows pass vacuously). **DEPLOYED-WORKLOAD rows (the `deployed_p` fix):** the blocker's inversion, driven through the REAL scan rather than `--selftest`, because the defect was in what the LIVE path closes — a full-chain `fedora-desktop` merge must NOT close (an instance exists; the host redeploy still has to deliver it), nor its IMAGE-baked variant, while a develop-only repo (`e2e-beta`) still DOES close, so the `NA` unblock cannot regress into the blanket wait it replaced. Plus a **DRIFT GUARD** reading the deploy set out of BOTH files — `RECONCILE_DEPLOYED_DEFAULT` vs `host-refresh.sh`'s `WORKLOADS`+`CONTROL_REPO` — since one fact enumerated in two places silently restores this defect for any workload added to only one (it self-FAILS as vacuous if either read comes back empty, rather than passing on a broken regex). THREE MUTATIONS RUN IN-SUITE (each sed must genuinely change the copy, else vacuous): neutralize `close_decision` to always CLOSE and the CI-PENDING scenario then WRONGLY closes (proving the proof-gate withholds closure, not the plumbing); restore the BLANKET proof text in `proof_summary` and the N/A close claims `published` again (proving the honesty rows discriminate); DROP `fedora-desktop` from the deploy set — the pre-fix defect exactly — and the workload closes on CI alone again. Verified: 4 rows FAIL against the returned head `d90f1bf` (the two workload rows on behaviour; the guard + deploy-set mutation correctly self-reporting VACUOUS, since that script has no deploy set to read or mutate). `bash reconcile.test.sh` → exit 0. No GitHub/network/model. Run after touching the reconciler, its proof chain, the deploy set, or the closing comment |
| bin/recoverability-drill.sh | the R38 UNIVERSAL-RECOVERABILITY per-action reversibility DRILLS (#207) — **the R10 rollback drill GENERALIZED**. R10 already proves recoverability for ONE mutation ("Autonomous deploys arm only after a rollback drill has demonstrably FIRED GREEN"); everywhere else recoverability was only ASSERTED (the NFR) + REVIEW-enforced (fitness's preserve-recoverability rule), never DRILLED. This harness DRILLS the REVERSE operation of each autonomous mutation the way R10 drills the digest rollback: it PERFORMS the reverse against a DISPOSABLE fixture and asserts it restores the pre-mutation state — a GREEN is a PROOF, not a claim. THE FOUR: **merge → `git revert`** (FAITHFUL, offline: a throwaway repo, a real squash-merge onto main — auto-merge.sh's actual `gh pr merge --squash` verb — then a real `git revert`; GREEN iff the reverted tree EQUALS the pre-merge tree AND the merge had actually changed it, so a vacuous no-op revert is not a proof) · **scope → `transcribe-narrow`** (FAITHFUL, offline: transcribe an EXPANDED scope then re-declare/narrow it via the REAL `bin/repo-scope.sh transcribe` + `bin/session-registry.sh` against a throwaway git objective; GREEN iff the added repo was present before and is GONE after while the kept repo survives — a faithful rollback, not a wipe) · **close → `reopen`** (the actuators close REVERSIBLY — `gh issue/pr close` with a comment, never a destructive delete; the reverse is `gh … reopen`, which has no offline analog, so it is **STAGED** by default — a DISCLOSED, surfaced not-yet-fired state, NEVER a fake GREEN per the ANTI-THEATER doctrine; `RECOVERY_DRILL_LIVE=1 RECOVERY_DRILL_REPO=<scratch>` fires a real create→close→reopen→assert-OPEN→close round-trip) · **rebuild → `restore-from-registry` (R17)** (STAGED by default; `RECOVERY_DRILL_LIVE=1` fires a real digest-restore round-trip in the local image store — a faithful stand-in for the registry: build LKG → "rebuild" → restore the retained LKG reference → assert the restored digest matches; PROVEN GREEN in-box with the engine). CONTRACT: `<mutation>` runs one drill (rc 0 GREEN · 1 RED · 3 STAGED); `all` folds to GREEN/PARTIAL/RED (rc 0 unless a drill RED — STAGED is surfaced, never a silent pass, R37); `guard <m>` is the ARM GATE — rc 0 IFF the drill FIRES GREEN (the R10 "arms only after the drill fires GREEN" contract, RED and STAGED both BLOCK). **#207 lands the DRILLS + the guard primitive; per-actuator arm-WIRING (having each actuator call `guard` before it arms) is the disclosed FOLLOW-ON** — R38 keeps that backlogged, and the merge gate is deliberately NOT touched here. `--selftest` covers the pure verdict core (`reverse_op`/`overall_verdict`/`guard_ok`); covered end-to-end by `recoverability-drill.test.sh`. **Control-plane (the recoverability boundary's prover).** |
| recoverability-drill.test.sh | proves `bin/recoverability-drill.sh` (#207): the merge + scope drills FIRE GREEN driving the REAL reverse ops (a real `git revert` restores the pre-merge tree; a real `transcribe-narrow` rolls the scope back) — no stubs, because the axis under test is that the reverse operation ACTUALLY RUNS; close + rebuild report STAGED offline (disclosed, never faked — R37/ANTI-THEATER); `all` → PARTIAL/rc0; `guard` arms ONLY on a fired GREEN and rc-2's an unknown mutation. MUTATION RUN IN-SUITE: a RED drill is forced FAITHFULLY (an isolated copy with NO sibling `repo-scope.sh` → the scope drill's reverse op cannot run → RED, a real broken dependency, no backdoor) and the REAL `overall_verdict` must surface `verdict: RED` (rc 1); neutralizing `overall_verdict` (the anchor) on the SAME fixture then HIDES the RED — the sed must genuinely change the copy, else the row fails as vacuous. `bash recoverability-drill.test.sh` → exit 0 = all rows pass. No GitHub/network/model. Run after touching the drill harness or its verdict fold |
| bin/residue-witness.sh | the INDEPENDENT RESIDUE WITNESS (#312, feat-01 of objective #310 "prove both boxes stay immutable") — a standalone, READ-ONLY enumerator of build residue, and the one component every later residue verdict in that objective rests on. The objective rules out the alternative explicitly: *"asserting immutability from the code that implements it — a teardown trap cannot be its own witness, and the two shipped objectives that closed on unmeasured evidence are why."* A probe whose witness is BLIND always exits 0, which is that same unmeasured-evidence failure, so this ships WITH its detection proof. **INDEPENDENCE IS STRUCTURAL, NOT ASSERTED:** it sources/calls/imports NOTHING from the teardown machinery it witnesses (the throwaway builder + its orphan sweeper / dnf-cache GC, `bin/validate.sh`) — it has no siblings at all and observes the SYSTEM (podman's own object lists, procfs, the filesystem), never the implementation's own bookkeeping; those components are named DESCRIPTIVELY in it and nowhere literally, so the issue's mechanical scan comes back EMPTY in code AND comments. **READ-ONLY, because a witness that cleans up launders the evidence:** no enumerator or diff path holds a removal verb; the ONE exception is `negctl_cleanup`, reversing exactly the four artifacts the negative control injected (each pid-scoped, so a concurrent run's objects are unreachable) with the narrowest verb for each — `podman untag <ref> <ref>` **in the two-argument form, because the single-argument form removes ALL of that image's names** and would destroy a pre-existing tag (verified live), `podman container rm` on a container this run created, and `rmdir`, which CANNOT recurse. **THE FOUR CLASSES:** `image` (one line PER TAG — a re-tag of an already-present image must be a new line, which is how the injection is visible at all; across ALL repositories, never narrowed to the disposable namespace the builder happens to use; `podman images` not `-a`, since intermediate layers ARE the deliberately-persistent layer cache) · `tree` (`$HOME/.cache/fd-throwaway.*`, `$TMPDIR/fd-throwaway.*`, `$HOME/.cache/fd-worktrees/*`, `$FD_THROWAWAY_TMPDIR/*`) · `container` (`podman ps -a`, running or exited) · `mount` (`/proc/self/mountinfo` AND the rootless build namespace, keyed by ns + mountpoint + source; the mountinfo parse finds fstype/source relative to the `-` separator, since the optional-field COUNT varies and a fixed index silently reads an option string as the source on any shared mount). **DETERMINISM IS A CONTRACT:** no volatile field enters a line (no container STATUS, no size, no mount/parent id or options — existence is the residue fact, state is not) and output is `LC_ALL=C sort -u`, so a snapshot is a SET and `diff` is exact set difference. VERBS: `snapshot [file]` · `diff <before> <after>` (rc 0 zero residue · 1 residue, each survivor as `RESIDUE <class> <key>`; vanished lines print `GONE …` and never affect rc — a build's own start-of-run orphan sweep legitimately removes things; rc 2 on unusable input, never a clean verdict) · `--negative-control` · `--selftest`. **THE INTENTIONAL-PERSISTENCE ALLOWLIST** (four individually-justified rules, applied ONLY in `diff` — laundering the snapshot would destroy the record — and every suppression PRINTED as `ALLOWED … [rule]`, never dropped silently): A1 the persistent dnf package cache (BP5's one durable input) · A2 the live spec clone · A3 the home volume mount + its `/run/host` twin · A4 the podman layer store's OWN mount and ONLY that exact mountpoint — the worked example of "never swallow a class wholesale", since a leaked per-container `…/overlay/<id>/merged` under the same root is still reported. The image + container classes have NO entry: a new one surviving a cycle is never intended. **THE NEGATIVE CONTROL IS WHAT MAKES IT CREDIBLE:** it injects one artifact of each class, snapshots, and asserts the witness REPORTS each (rc 0 all four DETECTED · 1 a class was injected and NOT seen, i.e. BLIND · 3 no blindness but a class could not be injected here, disclosed as `SKIP <class> — <reason>` — non-zero because a witness with an unproven class is not a proven witness). The mount injection is the subtle one: a rootless bind mount exists ONLY inside the namespace that made it (which is also why it cannot leak), so the injection rides a pre-hook INSIDE the enumerator's own namespace read — passed as a FUNCTION ARGUMENT from within the file, never an env seam — and `set -e` there is load-bearing: a failed injection yields NO output, which is what lets the harness tell "could not inject here" (SKIP) apart from "injected and missed" (BLIND). Two namespace mechanisms in order: `podman unshare` (correct where the engine is local) then util-linux `unshare --user --map-root-user --mount` (the in-box path — with `CONTAINER_HOST` set podman is a REMOTE client and refuses `unshare` outright). Covered by `residue-witness.test.sh`. **NOT a residue-producing probe** (the build cycle, the host half, the cadence are later features of #310) — this ships the instrument and the proof the instrument works |
| residue-witness.test.sh | proves `bin/residue-witness.sh` is an independent, read-only witness that is DEMONSTRABLY ABLE TO SEE RESIDUE (#312). The axis is not "does it run" but "does each class's enumerator detect a REAL artifact of that class", so every engine row drives the REAL boundary — real `podman tag`/`podman create`, a real bind mount in a real user namespace, a real `/proc/self/mountinfo` — and never a stub (BP8: a stub would assert what the stub was told). **PART A** (bash only): the pure core through the CLI (residue → rc 1 naming the survivor, idle → rc 0 and silent, unreadable input → rc 2) · the issue's independence/read-only scan is EMPTY whole-file, comments included, so a reader never has to adjudicate which hits are "functional" · imports and removal CALLS are scanned on CODE ONLY (comment lines blanked with the LINE NUMBERING PRESERVED, since the removal-verb row locates hits inside `negctl_cleanup`'s line range) · the two-argument `podman untag` is pinned mechanically (the one-argument form would destroy a pre-existing tag) · every injected name is pid-scoped · `snapshot` to a file equals `snapshot` to stdout (the default path an operator uses first). **PART B** (a reachable engine WITH ≥1 local image — the negative control re-tags an already-present image and pulls NOTHING, per Principle 2): two snapshots byte-identical and covering all four classes · the negative control rc 0 printing `DETECTED image|tree|container|mount` · it leaves NONE of its own artifacts behind (strictly attributable — every injected object carries `negctl`) plus the acceptance's literal whole-diff zero-residue row, which prints what it found and states that another actor writing to the box inside the window is a TRUE observation by the witness rather than a cleanup defect · no negctl tag/container/tree/temp file remains in the engine or filesystem. **CAPABILITY GATE:** off such a host PART A still RUNS (a genuine PART A regression exits 1) and only then does the file exit 77 with its `SKIP:` reason, so the uncovered half is announced by name instead of passing quietly; the pure core is covered in EVERY environment by `bin/residue-witness.sh --selftest`, which `.github/workflows/tests.yml` runs separately. **FIVE MUTATIONS RUN IN-SUITE**, each vacuity-guarded (`cmp` — a sed that changed nothing fails the row rather than passing over an unmutated file): neutralize the image / tree / container enumerator and the negative control must report `NOT DETECTED <class>` and exit non-zero; the mount class gets TWO, because its enumerator fails in two distinguishable ways the witness deliberately separates — silenced wholesale → SKIP (disclosed, unproven, still non-zero) and key-blinded while the enumerator still runs → NOT DETECTED (blindness proven). `bash residue-witness.test.sh` → exit 0 = all rows pass · 77 = PART B unrunnable here. No GitHub/network/model |
| residue-parity.test.sh | proves CHECK 7 of `bin/fleet-guard-parity.sh` actually CATCHES residue-witness drift between the two boxes (#318). The axis a `--selftest` is structurally blind to is whether the SCRIPT reaches the comparison at all — at the right per-repo path, with a drift making the WHOLE CHECK exit non-zero — which is exactly how #278 shipped a pure core with a green selftest and ZERO call sites. So every row drives the REAL script over a REAL fixture fleet via the `PARITY_RAW` `file://` base (laid out as raw.githubusercontent serves it, so `fetch()`, the per-repo `witness_path` mapping and the participation logic are the shipped ones) and asserts its EXIT CODE, the thing CI acts on. The non-residue payload is copied from THIS repo into BOTH fixture repos so CHECKS 1-6 pass and the rc under test is CHECK 7's alone — otherwise an rc-0 row would be unfalsifiable and an rc-1 row could pass for the wrong reason. Rows: identical copies → rc 0 printing the three report lines / the host drops the `mount` class → rc 1 NAMING it / an UNMARKED extra allowlist rule → rc 1 / the same rule with `# PARITY-EXEMPT(host): <why>` → rc 0, counted / an EMPTY justification → rc 1 / a changed verdict rc → rc 1 / the host copy ABSENT → rc 0 with a skip that NAMES who is missing and claims no parity (this PR lands before feat-03 and starts enforcing as each copy arrives) / **the PRE-MERGE self-overlay**: a HOST PR whose own copy quietly widens its allowlist → rc 1 while both repos are clean at raw@main (nothing but `PARITY_SELF` can see that) / the LOCKSTEP ESCAPE and its BOUND: the canon proposing a NEW standard → rc 0 as named porting debt (not the #296/bootstrap#307 deadlock), but an ALREADY-drifted host → rc 1 anyway. MUTATION RUN IN-SUITE (BP8, vacuity-guarded with `cmp`): neutralize `wit_classes` so both copies always report the same set, and the dropped-`mount` fixture WRONGLY passes while the real script still catches it — the difference is attributable to the comparison alone. `bash residue-parity.test.sh` → exit 0 = all rows pass · 77 = no curl here. No GitHub/network/model/engine |
| bin/immutability-probe.sh | the IMMUTABILITY PROBE (#313, feat-02 of objective #310) — the objective's ACCEPTANCE COMMAND, and the first thing to MEASURE the BP5/Principle-10 claim that a throwaway build leaves NOTHING behind (it was previously asserted by the teardown trap and never measured). It WITNESSES NOTHING ITSELF: every observation is delegated to `bin/residue-witness.sh` (feat-01, #312), because the objective rules the alternative out by name — *"a teardown trap cannot be its own witness"* — so this file holds no enumerator and no definition of residue, only the SEQUENCING of cycles and the FOLD of the witness's verdicts. THE DEV HALF'S THREE ARMS: **clean** (snapshot → a REAL `build-throwaway.sh -c <ctx>` cycle whose EXIT trap fires normally → snapshot → diff; GREEN iff ZERO residue) · **kill-9** (the trap-LESS path, which is the real leak mode: a second cycle SIGKILLed the moment its throwaway tree exists — deterministically BEFORE `podman build` can tag anything, so the leak is tree-class and no half-built candidate is stranded; its residue is REPORTED under its own label, expected rather than a failure) · **reap** (a THIRD cycle, because the documented orphan sweeper runs at the START of every build; GREEN iff every staged leak is GONE). **IT MEASURES, IT DOES NOT TIDY** — no sweeper, no image removal, no recursive delete between a cycle and its AFTER snapshot, and deliberately NO `--reap` mode: reporting residue IS the job, and tidying it would destroy the only evidence the objective wants. The one removal in the reap arm is performed by the SWEEPER UNDER TEST, never by this file. **A BUILD FAILURE IS A VALID MEASUREMENT AND IS NEVER FOLDED IN** — a failed build must also leave nothing (the exact path BP5 cares about), so the build rc is reported BESIDE the residue verdict and neither masks the other. **STAGING THE SWEEPER'S PRECONDITION, DISCLOSED:** the sweeper is AGE-bounded by design (`FD_STALE_MIN`, 720 min) so a concurrent in-flight build is never reaped — a seconds-old leak is therefore not yet its business — so the reap arm AGES the probe's own leaked tree past the threshold (`touch -d`) and lets the sweeper decide; that stages the precondition, it does not perform or stand in for the reap. **AND IT KEEPS ITS BLAST RADIUS TO ITS OWN ARTIFACTS:** `FD_STALE_MIN` is pinned HIGH (a year) rather than to 0 — lowering it is the shorter route to the same assertion and wrong twice over, since the sweeper reaps by IMAGE ID (so `rmi -f` on a disposable tag destroys EVERY name that image has; on this box one such ID also carries the non-disposable `localhost/fd-greenfield:slice1`) and a 0-minute threshold reaps a CONCURRENT build's in-flight candidate — the accident the age bound exists to prevent. A measurement tool that damages what it measures is not one. **A STAGED-NOTHING KILL IS `SKIP`, NEVER A VACUOUS GREEN** (the witness's own "an unproven class is not a proven witness" discipline), and an unusable snapshot comparison is ERROR folding to RED — "I could not compare" must never read as "nothing survived", which is the unmeasured-evidence failure the objective exists to end. CONTRACT (mirrors `bin/recoverability-drill.sh`): line 1 machine-readable `immutability-probe: <overall> dev=<v> host=<v>`; rc 0 GREEN · 1 RED · 3 STAGED/PARTIAL · 2 usage/harness. `dev` runs the dev half only; `--selftest` covers the pure fold with no engine. **The HOST half prints STAGED and forces a non-zero rc until feat-05** — deliberate and honest per R37/ANTI-THEATER: a half never measured must never print GREEN, and the objective's "exits 0" is completed by feat-05, not by this file pretending. Covered by `immutability-probe.test.sh`. **Control-plane (the immutability boundary's prover).** |
| immutability-probe.test.sh | proves `bin/immutability-probe.sh` MEASURES residue rather than asserting its absence (#313) — the axis a `--selftest` is structurally blind to, since a probe whose comparison is broken prints GREEN on a box carrying residue and a green pure-core fold cannot tell you so. The HEALTHY row drives the REAL probe, the REAL builder and the REAL witness at their DEFAULT paths (the production resolution every other row overrides) against a tiny OFFLINE `FROM scratch` context: rc 0, line 1 `dev=GREEN`, zero residue, the build rc reported separately, and the kill-9 leak staged AND reaped. The LEAK rows stand in a deliberately BROKEN builder — the defect itself, which the real builder cannot produce without editing it, and #313 forbids that: an IMAGE leak (a disposable tag never removed) and a TREE leak (a throwaway tree never removed) must each make the dev half RED rc 1 and NAME the survivor verbatim, and the tree row additionally pins the REAP arm going RED, since a builder that never sweeps must not be reported as one that did. MUTATION RUN IN-SUITE (BP8): `residue_verdict`'s RED arm is neutralized and the SAME injected leak must stop being RED, stop naming its survivor, and exit differently from the real probe — the sed must genuinely change the copy, else the row self-fails as vacuous. Structural rows pin the no-tidy contract by scanning the probe for a sweep/removal verb (which is why the probe names those verbs descriptively and never literally — a scan that trips over the sentence promising restraint teaches a reader to distrust the scan) and that the host half never prints GREEN. THE TEST OWNS ITS OWN INJECTIONS and reverses exactly those, by name and never by a glob (a glob over the throwaway root would reap a CONCURRENT build's in-flight tree), using `podman untag <ref> <ref>` in the TWO-argument form. **PART A (the pure contract + the no-tidy scans) RUNS EVERYWHERE**, so a genuine regression in it exits 1 even on a host that cannot run the rest — a real failure is never laundered into a skip. PARTS B–D declare **THREE** requirements, each named for itself, and skip by name (`SKIP: <reason>`, exit 77) when one is missing: the WITNESS (`bin/residue-witness.sh`, feat-01/#312 — without it the probe has no eyes and every engine row would fail for that one reason), a usable ENGINE (`podman info`, not `command -v`), and — the requirement `podman info` does NOT imply — an engine that can **COMPLETE the probe's own `build-throwaway.sh -c <ctx>` cycle**, PRE-FLIGHTED by running one and reading its rc rather than reasoning about which engine can host one (measured: on the GitHub runner `podman info` succeeds and that cycle exits NON-ZERO, so a guard on the engine alone excused nothing and 13 rows failed for one environmental reason — the "excuse wider than the reason" `.github/workflows/tests.yml`'s own header forbids; the skip carries the engine's real error). **AND THE LEAK STAND-INS BUILD THEIR OWN IMAGE:** re-tagging whatever `podman images` listed first was a HIDDEN environmental requirement — on a fresh engine that list is EMPTY, the injection silently no-ops and the RED rows assert against a builder that leaked nothing — so the suite builds a tiny OFFLINE fixture itself (Principle 2: it fetches nothing; pid-unique content ⇒ a unique image id carrying no other name, deliberately NOT the content the probe's own cycle builds, since an id shared with a disposable tag would put the real builder's teardown `rmi -f` on the fixture) and removes it by that id on exit. `bash immutability-probe.test.sh` → exit 0 = all rows pass · 77 = PARTS B–D unrunnable here (PART A ran and passed) · 1 = a real failure. No GitHub/model; builds are local-only and offline. Run after touching the probe |
| bin-exec.test.sh | asserts EVERY `bin/*.sh` is git-tracked executable (mode `100755`). The poller/entrypoint/services invoke these by DIRECT EXEC (`"$SCRIPT" --once`, `exec …/dev-loop-service.sh`), so a `100644` script dies at runtime with "Permission denied" — it shipped for real (`bin/reconcile.sh` #216 + `bin/dev-loop-service.sh` #214 committed 644: the reconciler could never run, and arming the authoring loop would have died at the entrypoint exec). The gap it closes: unit tests invoke via `bash <script>` (mode-agnostic), the host live-gate doesn't run opt-in/periodic services in its smoke, and a mode change is near-invisible in a diff — so nothing else caught it. Authoritative on the GIT-TRACKED mode (a local uncommitted `chmod` must not pass), with a filesystem-`-x` fallback for a non-git tarball build context. `bash bin-exec.test.sh` → exit 0 = all executable. No GitHub/network/model. Run after ADDING any `bin/*.sh` (a Write-created script is 644 by default) |
| policy/CLAUDE.md | runtime law for the in-claudebox agent (its role, do/don't, propose-and-commit, validation discipline) |
| policy/managed-settings.json | deny-rule guardrails (defense-in-depth) + bypass-permissions disabled (managed tier) + `env` lockout (`DISABLE_UPDATES`/`DISABLE_AUTOUPDATER`) so claude-code never self-updates in-place (paired with the profile.d lockout in `claudebox-init.sh`) |
| policy/scope.conf | **DEPRECATED as the enrollment authority (R16 amendment 2026-07-16)** — the authority is now the confirmed-objective repo-list in `00-OBJECTIVES.md` (see the `bin/repo-scope.sh` row). This file survives ONLY as the **transitional ceiling** the SCOPE_SESSION-unset poller reads until the STEP-10 cutover deletes it. Historically (R16 #167): the ONE authoritative, maintainer-confirmed list of repos the apparatus may act on — versioned config, never a script default; scope is per-objective, not permanent. ADDING a repo merges only with a maintainer's NAME-BOUND confirmation on the PR — line 1 exactly `CONFIRMED <repo> [<repo>…]`, role-checked admin\|maintain; it covers exactly the repos it names, so a post-confirmation head that swaps or extends the adds re-gates unconfirmed (`bin/fitness-review.sh` RETURNs an uncovered addition as UNSAFE — a blocker, never a NOTE); REMOVING one needs no ceremony (narrowing is always safe). Read by `bin/repo-scope.sh` (its row = the full contract + fail direction). **Control-plane (the scope boundary's authority).** |
| validate-t2.test.sh | regression suite for `bin/validate.sh`'s T2 startup check (#160) — the check whose fleet-only shape wedged the live E2E-A run: the unconditional rootfs grep for `usr/local/bin/entrypoint*.sh` (a fedora-dev-family CONVENTION) RED'd a correct, minimal, CMD-only image whose build/lint/smoke all passed. T2 now gates on the repo-agnostic OUTCOME — the image DECLARES a startup process (`podman inspect`: non-empty `.Config.Entrypoint` OR `.Config.Cmd`; podman 5's `create` happily creates a container from a no-startup image, so the inspect is the ONLY thing standing between one and a GREEN) — and holds the entrypoint*.sh path only against a tree that SHIPS one (a fleet image that LOST its entrypoint is still a real regression). Rows drive the REAL script (byte-identical copy in a bare dir, so the out-of-scope T0/T0b adjacency tiers skip as info) against REAL offline `FROM scratch` builds in the nested engine — the axis under test is podman's actual inspect/create/export semantics, which a stub would assert rather than prove: CMD-only + no convention → GREEN with the fleet check SKIPPED / neither Entrypoint nor Cmd → RED whose line names the remedy / a convention tree whose image lost the file → RED naming the expected path / the fleet-positive image stays GREEN / MUTATION ROW RUN IN-SUITE: the unconditional path-grep is mechanically RESTORED (a sed that must genuinely change the copy, else the row fails as vacuous) and the CMD-only repo must then go RED — proven: 11 of 19 rows fail against the pre-#160 script. No GitHub/network/model; podman builds are local-only (no pulls, no RUN steps). `bash validate-t2.test.sh` → exit 0 = all rows pass. Run after touching T2 |
| health-marker.test.sh | regression test for the #11 assemble-health marker: asserts the two health-predicate copies (quadlet `HealthCmd` + `run.sh` `--health-cmd`) stay byte-identical and carry the `.assemble-failed` clause, exercises the verbatim assemble EXIT trap, and drives the exact shipped predicate through the SUCCEEDED / IN-PROGRESS / FAILED states (incl. the stale-`.assembled` failed-rebuild case); `bash health-marker.test.sh` → exit 0 = all rows pass. Run after touching the health cmd, the assemble marker, or the trap |
| assemble-stamp.test.sh | the #175 STAMP-LOADABILITY suite — proves the assembled law lands READABLE BY THE SESSION USER and that 'stamped' means readable AND current (the incident: mktemp's 0600 rode the in-box `cp` onto `/etc/claude-code/CLAUDE.md`, root-owned and unreadable by core — assembled, fresh mtime, completely INERT). Drives the REAL `claudebox-assemble.sh` (via its `ASSEMBLE_LIVE` seam) against a fixture live-spec + fake box rootfs, stubbing ONLY podman + distrobox — faithful where it counts: the stub's `exec claudebox cp` runs REAL cp (the incident's mode-carry is reproduced, never asserted) and a `--user core` read enforces the other-read bit exactly as the real box's root:root files do for uid 1000. Rows: a normal assemble stamps law + managed-settings 644, verifies AS core, and only then marks `.assembled` / a pre-existing 0600 stamp (every already-deployed box — in-box cp KEEPS an existing dest's mode, so fixing only the mktemp mode would never heal one) converges to 644 + current / a 0600 SOURCE managed-settings still lands readable / **MUTATIONS RUN IN-SUITE** (issue req 3; each sed must genuinely change the copy or its row fails as vacuous): DROP THE CHMOD and the assemble must FAIL its verify — the 0600 artifact reproduced, no `.assembled`, `.assemble-failed` written (#11), temp law reaped on the failure path too / DROP THE LAW CP against a readable-but-STALE stamp and the verify must fail on CURRENCY (readable is not stamped). `bash assemble-stamp.test.sh` → exit 0 = all rows pass. No GitHub/network/model/real podman. Run after touching the assemble stamp or its verify |
| supervise-enter-race.test.sh | proves the `entrypoint.sh` poller + deadman supervise loops survive the **distrobox-enter RESTART RACE** (the `container_setup_done` log-follow that HANGS FOREVER after a restart when the box is stopped, distrobox-enter:702/734/756 — it wedged the poller twice on 2026-07-14). Drives a REPLICA of the loop's one-iteration decision (the `box_ready` bounded probe → real enter) against a STUB `distrobox` (BOX_STATE fixture: up = enter succeeds; down = enter HANGS via `exec sleep`) with the REAL coreutils `timeout`, so the bounded behaviour is exercised not asserted. Rows: box UP → probe passes → the real enter runs / box DOWN (setup hangs) → the probe is KILLED, the real enter is SKIPPED and the iteration returns bounded (<12s, never hangs) / **MUTATION RUN IN-SUITE** — remove the `box_ready` gate and the unguarded enter HANGS on a down box (outer `timeout` fires, rc=124), while a non-vacuity check confirms it reached the real enter — proving the probe is what prevents the hang / **DRIFT GUARD** — the real `entrypoint.sh` carries the `box_ready` probe + `if box_ready; then` gate on BOTH loops (2/2). `bash supervise-enter-race.test.sh` → exit 0 = all pass. No GitHub/network/real podman. Run after touching the entrypoint supervise loops |
| bin/fleet-guard-parity.sh | fleet guard-parity check: fetches the SHARED claude-code guard payload (managed-settings deny-list + `env` lockout, the #45 self-update lockout/self-heal in `claudebox-init.sh`, claude-code provenance in `distrobox.ini`) from every fleet claudebox repo (public, raw, no auth) and FAILS if any drifts from fedora-dev (the canonical anchor). The "one source of truth" for a self-contained-repo fleet — it ENFORCES the duplicated copies stay identical instead of physically merging them (which would break dev's offline seeded-no-git path). Catches the #45-class silent drift automatically. **CHECK 7 — THE RESIDUE WITNESS (#318, feat-04 of objective #310):** `residue-witness.sh` is duplicated for the same self-containment reason (fedora-dev `bin/residue-witness.sh` ↔ fedora-bootstrap `residue-witness.sh`), and drift there is invisible BY DESIGN — a host witness that quietly stops enumerating one class, or grows one extra allowlist entry, still exits 0 forever, i.e. *"a lower bar for being the thing under protection"*, the unmeasured-evidence failure #310 was filed against. It is compared STRUCTURALLY, not hashed: byte-parity would be a PERMANENT FALSE RED (each box legitimately observes its OWN anchors — the dev box's live spec clone and nested-engine graph store are not the host's paths) and a check that cries wolf gets switched off. Three gated comparisons, in the issue's order: (1) **the residue CLASS SET** — a class counts only when BOTH declared (`enumerate_<class>()` at column 0) and emitted (`printf '<class>\t…`), so a declared-but-gutted enumerator is not an enumerated class; the sets must be EQUAL. (2) **the intentional-persistence ALLOWLIST** — each rule keyed by ID **plus its guard PATTERNS** (the pattern is the bar: an ID-only compare would miss the same `A1` broadened to admit more), differing only by entries declaring themselves box-specific with a `# PARITY-EXEMPT(<box>): <justification>` marker that must carry a NON-EMPTY reason and must name the box whose copy it lives in (else a copy could exempt itself by proxy); marker hygiene (empty / foreign-box / malformed / attached-to-nothing) is checked independently of the set diff, and scoped to `allow_rule`'s body so a header comment DOCUMENTING the grammar can never false-RED. (3) **the VERDICT/EXIT CONTRACT** — the diff output grammar (`RESIDUE %s %s\n` and siblings), the diff rc mapping, the `negative-control:` verdict→rc map, and the DISPATCH VERB set (a copy shipping no `--negative-control` is an UNPROVEN witness, i.e. a lower bar, however well its enumerators match), all because feat-05 PARSES the host's output and rc. FAIL-CLOSED throughout: a copy whose standard cannot be READ is a FAILURE, never a pass — a read that failed must not be the thing that relaxes a guard. The rest is DISCLOSED, not implied away: a non-gating normalized-code line-diff count is reported per follower. **Participation is per-copy and the skip is LOUD** (fewer than 2 copies ⇒ named skip + an explicit "nothing is asserted about parity yet"), so this check lands before feat-03 exists and starts enforcing by itself as each copy arrives. **The LOCKSTEP ESCAPE is ported from CHECK 1** (the #296/bootstrap#307 deadlock: a canonical PR proposing a new payload can never pass, and neither can the follower PR that would resolve it): when `PARITY_SELF` IS the canon, a follower matching canon@`$REF` is reported as PORTING DEBT, establishing an order instead of a deadlock — and it is BOUNDED, firing only while the follower matches the landed canon exactly, so an already-drifted host is still RED. Seams: `PARITY_RAW` (the fetch base — a `file://` base lets `residue-parity.test.sh` drive this REAL script with no network) and `--selftest` (the pure comparison core, incl. a DRIFT GUARD that the SHIPPED canon still satisfies the extraction contract, so a witness refactor cannot silently make every copy "unreadable") |
| .github/workflows/build.yml | CI: on push + 15th monthly (--no-cache) + manual; build-push-action (UNSIGNED — signing dropped fleet-wide; no cosign step, no `id-token` permission, nothing verifies it) |
| .github/workflows/fleet-guard-parity.yml | CI: runs `bin/fleet-guard-parity.sh` on push/PR/manual + daily (04:30 UTC) so guard drift surfaces even with no PR activity; built-in token only (fleet repos are public) |

## NESTED BUILDS — CONTAINER_HOST BRIDGE (reference)

As `core` inside claudebox: `podman build/run/exec/healthcheck` works because `CONTAINER_HOST=unix:///run/user/1000/podman/podman.sock` is exported in `/etc/profile.d/10-host-podman.sh` (written by `claudebox-init.sh` post-assemble). That socket is **fedora-dev's** rootless podman API socket, served by `podman system service` supervised in the entrypoint. distrobox bind-mounts `/run/user/1000/` from fedora-dev into the box at the same path.

Result: `podman build .` inside the box runs in **fedora-dev's engine** (one level of nesting, fuse-overlayfs storage on the home volume at `~/.local/share/containers/`), NOT in another nested engine inside the box. Storage, layer cache, built images, and stopped containers persist across box rebuilds AND across fedora-dev container recreations.

Subuid/subgid: `core:10000:55000` (sized to fit the outer rootless 65536-ID map; inner chowns to uid ≥ 55001 will fail — enlarge the host range first if ever needed).

No systemd inside fedora-dev: cgroupfs manager + file events logger preconfigured; XDG_RUNTIME_DIR provided by the entrypoint; the rebuild-trigger machinery is supervised by the entrypoint's pgrep watchdog instead of systemd units.

### In-box candidate validation — capability boundary + `bin/validate.sh`

The nested engine's RUN support is **narrower** than the "build/run/exec works" line above; `bin/validate.sh` gates exactly the tiers that are faithful at this nesting depth (autonomously established):

- **BUILD — requires `--isolation=chroot`.** A default-isolation build RUN step fails with `mount 'proc' to 'proc': Operation not permitted` (the build container can't mount a fresh `/proc` at this depth). `--isolation=chroot` runs RUN steps in a chroot (no new mount/pid ns) and succeeds.
- **ASSEMBLY — works.** `podman create` + `podman export | tar -t` + content inspection (no run): the reliable structural check.
- **ISOLATED LIVE RUN — blocked.** A container with its OWN netns hits `/proc/sys/net/ipv4/ping_group_range: Read-only file system`; its OWN pidns hits the same `mount proc` denial. Only a **degraded** `--network=host --pid=host` run works (shared namespaces → ambiguous probes + flaky teardown), so live-run is **best-effort / non-gating** in-box.
- **Faithful live validation belongs on the host** (own namespaces, one less nesting level) — the existing post-merge `container-refresh` health-gate + digest-rollback. The in-box harness gates build+assembly+lint; the host gates live.

`bin/validate.sh <repo-dir> [Containerfile] [build|nobuild]` → per-tier PASS/FAIL + a GREEN/RED verdict the agent iterates on, with no host and no human in the loop. **Repo-agnostic** (validates fedora-dev AND workload repos): auto-detects a **systemd-PID-1** lineage (`ENTRYPOINT /sbin/init` / `STOPSIGNAL SIGRTMIN+3`) and skips the degraded smoke for it (→ assembly-only in-box; its live gate is the host); lints **every shipped `*.sh`**; passes per-repo build-args via `$BUILD_ARGS`. **Host-immutable:** builds into an ephemeral image tree in the dev box's OWN nested engine (never the host live tree), tears down smoke containers on exit, and `DISCARD=1` removes the candidate image after testing. Tested GREEN on fedora-dev + both fedora-desktop lineages (xrdp + grd).

## CADENCE REFERENCE

| Layer | Cadence | Trigger | Source |
|---|---|---|---|
| Base image (RPM updates) | Monthly (15th @ 04:00 UTC) | CI cron `--no-cache` | Fedora + tailscale repos |
| Base image (spec changes) | On push to `main` | CI `on: push` | merged PRs from in-box agent |
| Claudebox (CLI + tools) | Daily (~04:00) | in-container `claudebox-daily.sh`; defers if session active | Anthropic `latest` channel + Fedora repos |
| Claudebox (ad-hoc) | On demand | in-box `claudebox-rebuild` OR host-shell `claudebox-rebuild` | same as daily |
| fedora-dev container itself | Operator-driven (or host cron) | safe-refresh on monthly cadence; SESSION-LOCK PROBE defers if claude is busy | new fedora-dev image from GHCR |

Base bump (Fedora 44 → 45): `ARG FEDORA_VERSION` in Containerfile is the single source of truth. The box's `image=quay.io/fedora/fedora-toolbox:N` line in distrobox.ini must bump in lockstep. Fedora releases EOL ~13 months — plan for twice a year, re-verifying vendor repos per Build Principle 4.
