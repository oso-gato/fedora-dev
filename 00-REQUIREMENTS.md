# Apparatus — FUNCTIONAL REQUIREMENTS (spec of record)

> **Confirmed set as of 2026-07-14.** Companion to [`00-OBJECTIVES.md`](./00-OBJECTIVES.md). This is
> the numbered, testable requirement set the apparatus builds to and the fitness gate grades against.
> Revised from spec [#135](https://github.com/oso-gato/fedora-dev/issues/135) (R1–R20) by the
> objective-alignment review: **R8 deleted**, R2–R20 amended to the confirmed objective, **R21–R33
> added**. **R8 (THE HUMAN GATE) and R19 are RETIRED — numbers are never reused.** Amendment is a new
> maintainer confirmation (R1), never a silent edit. Capabilities, not implementations.

## R1 — SINGLE-TOUCH INTAKE
An objective becomes a spec document whose numbered requirement set the maintainer confirms exactly once. The confirmed set is tamper-evident and immutable; fitness grades only the human-confirmed version; amendment is a new confirmation. (The dev-driven authoring of that document is R31.)

## R2 — AUTONOMOUS PLANNING
Confirmed spec → a numbered backlog of feature issues, one per feature, each closing only with proof links — the merged PR, the host GREEN verdict(s), and a **verified-live read-back** (the live artifact re-read and matched against merged source; merged ≠ live, so a deploy not confirmed live does not close the issue).

## R3 — AUTONOMOUS AUTHORING
Bounded judgment runs author each feature in a fresh, isolated worktree scoped to the session's declared repo-set; the first push opens a draft PR immediately. **No durable authoring/progress state persists outside GitHub — the loop is fully crash/kill/restart-resumable from the PR/issue stream — the sole exception being the per-session coordination namespace (lock, markers, scope, verdict-routing), local container state by design that constitutes the code-enforced isolation boundary.** All mutable local state (worktree roots, lock/flock paths, claim/progress markers, scratch) is namespaced per session-id, and the checked-out branch is re-verified to belong to the session's own namespace before every commit and push.

## R4 — TIERED VALIDATION
**Scenario A (default):** Tier-1 in-box iteration to the nested engine's proven ceiling (build + assembly + lint), engaging the host **only for the live validation the container cannot perform itself** (at minimum the final pre-production shipment); on the host's RED verdict the session fixes and re-tickets **until GREEN**. **Scenario B (conditional fallback, not a choice):** entered only when the container cannot validate the build within itself at all (e.g. a systemd-PID-1 image that cannot boot in the nested engine), so Scenario A is unavailable; then every iteration round-trips the host against the draft PR's head sha from iteration zero.

## R5 — FIRST-CLASS TICKET BUS
GitHub issues/PRs are the sole IPC, WAL, and audit log. Typed tickets, line-1 machine grammar with a fixed verb allowlist and an explicit **per-session scope/routing token** on every ticket, claim, and terminal-outcome comment; an App/author-**authorization** check (App vs maintainer vs stranger) before parse — an authorization guard, **not** a session-isolation mechanism, since all sessions share one dev App identity. Full 40-hex sha binding on everything gating; verdict routing and idempotent claim/closure match on the **sha AND the session-scope key together**; a ticket whose scope key is undeclared/unreadable is consumed by nothing (fail-closed); the host echoes the originating session's scope key verbatim on its verdict. Claim comments for idempotent replay, terminal `outcome:` comments as closure, stale-ticket reopening. The sha-bound bus comment is the canonical verdict; check runs are its server-side mirror.

## R6 — INDEPENDENT SELF-CHECK
Every PR head sha is judged (a)(b)(c) by a non-author identity on the other trust domain (erebus), grounded in the confirmed spec — never the PR's own body. Check (b) includes a mechanical red-line scan and fails any PR modifying its own `.live-gate` alongside product code (a PR may not carry both the product and the gate that passes it). Findings are generative: RETURN reasons feed the fixer.

## R7 — CLICK-FREE, SERVER-ENFORCED MERGE
Every PR in an **in-scope** repo (the maintainer-confirmed set in `policy/scope.conf`, apparatus repos included) merges by GitHub native auto-merge the instant the required live-gate and fitness checks (postable only by the host validator identity, sha-bound) are green for the head sha. A PR in an out-of-scope repo gets **no auto-merge (fail-closed)**. No custom merge executor; a compromised author box cannot merge anything.

## R8 — *RETIRED (deleted 2026-07-14)*
THE HUMAN GATE is deleted. The objective mandates exactly one interaction, no further human interaction, and self-development in-scope through the gate-free loop; a surviving codeowner/human review would be the forbidden second interaction. Its protective purpose is carried by R6 (independent cross-check), R10/R17 (fail-closed live read-back), and R24 (empirical-proof rule). Consistent with the in-force zero-gate decision (2026-07-11). *Number never reused.*

## R9 — HALT AND REVOCATION
A pinned, maintainer-thrown HALT is the single **global soft stop** across all tenant sessions in the multi-tenant box (no approval semantics; must never re-introduce an approval gate). Because all sessions share one dev App identity, the **hard stop is revocation of that single shared key**, which necessarily stops all tenant sessions at once — there is no per-tenant hard stop via identity (isolation is by scope, code-enforced; per-session soft HALT is R20). Push protection + org-wide secret scanning protect the shared credential; a leak triggers the kernel rotation runbook.

## R10 — AUTONOMOUS DEPLOY WITH RECOVERY BEFORE POWER
Post-merge: CI publishes; the host deploys the digest and confirms success by **reading the live deployed artifact back and verifying it matches the merged-source digest** (fail-closed: not done until verified live on the target) — a liveness/health probe being only one component of the read-back; on any read-back failure (unhealthy probe OR live-digest ≠ merged source) it auto-rolls back to the retained last-known-good digest and files a regression ticket. Autonomous deploys arm only after a rollback drill has demonstrably fired GREEN.

## R11 — CHURN DISCIPLINE
Every build a throwaway. Teardown (EXIT-trap) covers **every throwaway tree, image, and run container**; an orphan sweeper reaps crash/kill leaks. The one durable input — the bind-mounted package cache — holds re-downloadable inputs, **never build output, and is never an image layer** (0-byte re-download standard across iterations). That cache is **garbage-collected by age then size to a hard ceiling** so it can never grow without limit or exhaust host storage — a scheduled, self-verifying actuator distinct from EXIT-trap teardown and the orphan sweeper.

## R12 — RED LINE
Builds of non-apparatus/workload work structurally cannot mutate the host, fedora-dev, or fedora-bootstrap: throwaway trees on both boxes, no repo-mutating host verbs, pull-only deploys from CI digests, plus the fitness (b) scan. Building the apparatus itself is the sanctioned exception, expressed as spec requirements through the same gates.

## R13 — BOUNDED AUTONOMY
Every judgment run has a timeout, a no-progress stop (same failure signature + unchanged head sha), a **heartbeat** (a positive liveness signal whose absence is treated as a failure to surface, not evidence of progress), and a BLOCKED surfacing path (a dev-task question, never an approval request). Zero permission prompts; a prompt is a defect.

## R14 — END-TO-END PROOF
Acceptance = **E2E-A** (clean Scenario-A objective in a virgin repo), **E2E-B** (systemd-PID-1, every iteration host-run), **E2E-KILL** (both boxes killed mid-work, resume from the bus alone), **E2E-ISO** (≥2 concurrent sessions on disjoint declared scopes: each verdict routed only to its originating session, no session reads/acts on another's ticket, zero cross-session worktree/lock/marker collision, an undeclared-scope session fails closed to zero action — each objective shipping with human-interaction-count = 1), and **E2E-SELF** (an apparatus-scoped change merges and the pair staggers its own refresh — host rebuilds/restores the dev container from outside, dev instructs the host's agent to refresh the host — passing only when the live artifact read back matches merged source, fail-closed); each with fault injection (one injected RED recovered, one broken deploy auto-rolled-back). Pass = **human interaction count = 1** (spec confirmation), audited from the event stream. Commissioning + monthly canary.

## R15 — CONFIG AS CODE (defense-in-depth)
A supporting **defense-in-depth** control on the merge-and-deploy boundary (revisit post-MVP). The branch-protection ruleset enforcing require-PR on `main` (and, if used, the App permission/gate-identity config) is mirrored as versioned in-repo JSON; **CODEOWNERS is optional** (not load-bearing in a zero-gate, code-enforced-isolation model). A scheduled audit compares live platform config to the mirror and, on drift, files a regression ticket onto the bus for the owning apparatus session to self-correct.

## R16 — OPERATING SCOPE (per-session)
Each claudebox session declares its own repo/repo-set and acts exclusively within it (scope is one facet of per-session state namespacing — a code-enforced, not identity-enforced, boundary under one shared dev App identity). Scope additions are maintainer-gated (role-checked `admin|maintain`, not label/App); fitness treats an unauthorized net-add as (b) UNSAFE RETURN. Every actuator checks scope before acting, **fail-closed to NOTHING when a session's scope is undeclared/unreadable** (acts on no repo). The host live-gate honors the same scope. Session discipline is law (a foreign-repo request is surfaced, never self-provisioned around). Scope is per-session/per-objective, not permanent.

## R17 — REBUILD CONTINUITY
A purposeful rebuild is a complete lifecycle: **KILL** all processes/sessions (by construction, no ghost) → **REBUILD** to the new spec → **RESTORE** every prior session on the new container → **RESUME** (kick-started and actively working, not merely present) → **VERIFIED — fail-closed, not done until proven live on the target: (1) the live dev container's running image/spec is read back and matches the newly-merged dev-container repo source, AND (2) every restored session is alive AND active, container identified by ID never name**; the routine tick still defers to session-lock. Orchestrated by the **pair**: the host executes the dev-container KILL/REBUILD/RESTORE from outside via the ticket bus + podman (neither box rebuilds itself). Depends on liveness-bound locks (R26).

## R18 — LIVENESS / STALL DETECTION
Monitor for the **ABSENCE** of progress, not just presence: every long-running component and orchestration has a deadman that fires when expected progress does not occur within a bound; a per-stage progress fingerprint; **IDLE-WITH-WORK-PENDING** is a first-class alarm; a completion is an announced event; quiet when healthy. Additionally each long-running process **emits a periodic heartbeat that an independent/supervising context reads, so liveness is observed positively, not merely inferred from the deadman staying silent; the watchdog decides liveness from the real heartbeat, never from a proxy (log mtime, worktree write, a lock's existence); absence of a heartbeat OR of an expected completion signal past its bound is surfaced as a failure, never read as progress.** Coverage spans the poller, supervisor, host watcher, host live-gate build, dev-author/fixer, and refresh orchestrations. The watchdog is not defeatable by the process-pattern self-match bugs it hunts (probe binaries/PIDs, never strings).

## R19 — *RETIRED*
*Number never reused.*

## R20 — MULTI-SESSION ISOLATION
N isolated claudebox sessions per dev container, each bound to its own declared **disjoint** repo-set; one shared host validator over the union of all sessions' repos; **session-exclusive ticket pickup** — a session acts on and even reads only its own session's tickets, never another's. Isolation is by per-session state namespacing (lock, markers, scope, verdict-routing) — a **code-enforced boundary (not identity-enforced) under a single shared dev App identity, fail-closed to nothing if a session's scope is undeclared.** Session-aware control: **HALT can pause a named session's loop independently** (a container/fleet HALT cascades to all resident sessions, resolved through the session registry, maintainer-bound both directions); but **dev-container refresh/rebuild is a whole-container operation** on the single shared dev container — it rebuilds all resident sessions together; there is no per-session `rebuild-devbox`.

## R21 — SOFTWARE PROVENANCE
Every artifact entering ANY tree the apparatus builds — host image, claudebox `additional_packages`, dev container, **and every throwaway validation build alike** — is admitted **fail-closed and version-pinned** at the strongest level it admits of a three-level hierarchy: **L1** Fedora's own dnf repos; **L2** the vendor's/developer's own RPM or dnf `.repo` with `gpgcheck=1`; **L3** last-resort official-upstream binary with no L1/L2 source, itself provenance-graded (**c1** GPG signature > **c2** published checksum > **c3** resolve-log, strongest first) and **disclosed per artifact**. It is a defect to descend to a lower level a higher one would satisfy. Forbidden outright and enforced by a mechanical scan gate: COPR/third-party repos, language package managers onto PATH (pip/pipx/npm-g/cargo/go/gem/brew), tarballs onto PATH, curl-pipe-sh, mirror/aggregator binaries, flatpak, snap. Disposability grants no exemption. Fitness treats a source below the strongest available level, an unpinned artifact, or any forbidden source as **(b) UNSAFE**.

## R22 — IMMUTABLE-HOST / CONTAINERISE-EVERYTHING INVARIANT
The platform is built as an **immutable custom host image** (the Fedora Bluefin custom-image principle) with **every application — claudebox, the dev container, and all workloads — running in a container**. Nothing is installed mutably onto the running host; all host change is by **image rebase/redeploy through the merge-and-deploy path**. A mechanical/fitness check fails any change that installs to or mutates the live host filesystem outside the image-build path.

## R23 — RECIPROCAL PAIR SELF-REFRESH (HOST DIRECTION)
Merged is not live, in **both** directions. When a change to the **host/bootstrap repo** merges, the **dev container drives the host** to that merged development by filing a host-refresh ticket instructing the host's agent over the bus — the dev box never touches the host directly, the reciprocal of R17's host-driven dev-container rebuild (neither box rebuilds the very agent doing the work). A merge to **either** apparatus repo **automatically arms** the matching refresh with no human step; the ordering is staggered/pair-driven. Every refresh, host and dev-container alike, is confirmed by reading the live artifact back against merged source and is fail-closed — on failure it rolls back and re-tickets.

## R24 — STANDING EMPIRICAL-PROOF / NO-PROXY RULE
No gating outcome anywhere — live-gate verdict, fitness pass, deploy/health confirmation, refresh confirmation, config-drift audit, planning-close proof, or liveness decision — is accepted on an agent's assertion, from memory, or against a baseline that has shifted under development. Each is proven **at check-time by reading the REAL live artifact** bound to the exact state judged (the running container's live image digest, the actual process/PID, the real file/config on target, the merged source), **cross-checked by a non-author context** where it gates a merge or deploy, and **never satisfied by a proxy** — clone-HEAD for running-HEAD, a lock's existence for a live holder, a CI/build log for the deployed digest, a merge event for a live deployment, "looks green" for the artifact read. A known-proxy substitution is a reviewable defect; an outcome asserted-but-not-read is treated as a failure to surface, not a success.

## R25 — VERDICT BASE-DRIFT VOIDING
Every gating verdict (live-gate GREEN/RED, fitness PASS) is bound to **BOTH the head sha AND the merge-base (target `main`) sha** it was computed against, and is **automatically void once the base branch advances past that merge-base** — the verdict must be recomputed against the current base before it can gate a merge. A judgment issued against a since-shifted baseline is never honored; a PR rendered unmergeable by base drift is routed to the fixer rather than left to spin. Acceptance: GREEN on head H over base B, advance `main` past B, assert the merge does not proceed on the stale verdict and re-validation is required.

## R26 — LIVENESS-BOUND LOCKS & MARKERS
Any lock, marker, or claim that gates whether work proceeds is **liveness-bound**: a lock held by a dead or absent holder is detected and reclaimed, and a lock's mere existence is **never** accepted as proof its holder is alive. A stale lock must not silently turn a restart/restore/sweep into an `rc=0` no-op; holder identity is verified against a **live PID/heartbeat (no proxy)**, and a would-be no-op caused by a stale lock surfaces as a fault. Acceptance: leave a stale lock from a killed holder, start a new instance, assert it detects the dead holder, reclaims the lock, proceeds, and never silently no-ops.

## R27 — SESSION REGISTRY
The apparatus maintains a **durable, versioned SESSION REGISTRY** — the single source of truth enumerating each declared session (**session-id, declared scope/repo-set, container id**) — persisted on the home volume so it survives box recreation. Every session-aware actuator (rebuild/restore, HALT targeting, verdict routing, scope-disjointness check, undeclared-scope fail-closed) reads the registry; a session absent from the registry is **undeclared** (and therefore acts on nothing, per R16). Acceptance: kill and rebuild the container, then assert every registry-listed session is restored and resumed, each identified by container id (never name).

## R28 — SCOPE DISJOINTNESS ENFORCEMENT
Session scopes across the dev container are **pairwise disjoint**. Before acting on any repo a session verifies (against the R27 registry) that no other declared session's scope contains that repo; an overlap (two sessions claiming the same repo) is a detected fault that **fail-closes BOTH claimants off the contested repo** and surfaces a BLOCKED question — never a first-come race, double-author, or double-merge. Acceptance: declare two sessions whose scopes intersect on repo X; assert neither authors, validates, or merges any PR in X and the overlap is surfaced.

## R29 — CONCURRENT SHARED-VALIDATOR SAFETY
The single shared host validator processes concurrent tickets from multiple sessions **without cross-contamination**: each validation runs in its own throwaway tree/candidate keyed by **BOTH ticket sha AND session-id** (no shared build state between two sessions' candidates), the shared package cache and orphan sweeper are **concurrency-safe**, and scheduling is **fair** so no session can starve another's tickets indefinitely. Acceptance: two sessions file live-validate tickets simultaneously; assert both receive correct GREEN/RED verdicts routed to the right session and neither build reads or clobbers the other's tree.

## R30 — WHOLE-OBJECTIVE COMPLETION RECOGNITION
The run recognizes **whole-objective completion structurally**: the spec/objective closes only when **every feature issue in its R2 backlog is closed with its proof links AND the aggregate product is verified shipped end-to-end (live/deployed)**, with **no human sign-off** on the final shipment. Until that condition holds the loop keeps iterating; when it holds the objective is marked shipped autonomously and the run ends.

## R31 — REQUIREMENTS ELABORATION / DEV-DRIVEN INTAKE
Intake is **dev-driven within the single R1 interaction**. On the maintainer's raw objective the dev container **authors a tightened AND expanded objective set plus a derived numbered functional-requirement set**, and settles it through discussion, **before** the one confirmation. The confirmed, tamper-evident, immutable document records **BOTH the objective and the functional requirements**, and it — not a transcription of the maintainer's words — **becomes the build objective**. The whole exchange (objective stated → discussion → single confirmation) counts as **exactly one human interaction** for R14's audit, after which the run is autonomous to ship.

## R32 — CAPABILITY-RELATIVE MINIMALISM
Every package/artifact is installed at the **minimal leaf footprint for its decided capability, not to an absolute count**: no package enters any built tree without a **recorded justification**; dnf runs with **`install_weak_deps=False`**; the **most specific leaf package** is chosen over any convenience metapackage; the **irreducible hard-dependency closure** of a chosen capability is accepted and disclosed, not fought. Minimality is **capability-relative** — between equal-capability options prefer the smaller-footprint, built-in, higher-provenance one; **dropping a capability to shrink the footprint is a recorded capability trade-off, never scored a minimalism win**. Enforced as a fitness check on every package-adding change.

## R33 — THREE DECOUPLED REBUILD CLOCKS + CLAUDE-CODE SELF-CADENCE
The platform maintains **three independent rebuild cadences — host image, dev-container image, and the claudebox-resident Claude Code — each advanceable without forcing the other two**. Because Claude Code advances daily and is containerised, claudebox carries its **own update actuator** that refreshes it to the latest official-channel release on **its own clock**, decoupled from dev-container recreation (R17) and host refresh (R23), admitted per the R21 provenance hierarchy and **verified by reading the live installed version back** (no proxy). None of the three clocks forces a rebuild of the others; each is separately triggerable and separately verified.
